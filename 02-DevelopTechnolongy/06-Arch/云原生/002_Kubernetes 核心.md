| #     | 知识点                         | 重要度 | 三层笔记建议                                                                                                                                                                                                                                                                               | 面试追问                                        | FlowPulse 结合                                      |
| ----- | --------------------------- | --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------- | ------------------------------------------------- |
| A1-09 | **K8s 架构与组件**               | ★★★ | L1: Control Plane(API Server/etcd/Scheduler/Controller Manager) + Worker Node(Kubelet/Kube-proxy/Container Runtime)；L2: API Server是唯一入口(etcd是唯一存储) → Scheduler分配Pod到Node → Kubelet管理容器生命周期 → Kube-proxy维护网络规则；L3: etcd集群(奇数节点3/5/7) + Raft共识保证一致性                                    | etcd挂了整个集群还能工作吗？为什么API Server要三个副本？         | **FlowPulse K8s架构总览**                             |
| A1-10 | **Pod 生命周期与探针**             | ★★★ | L1: Pod = 最小调度单元(1+共享网络/存储的容器) → Pending → Running → Succeeded/Failed；L2: 三种探针：livenessProbe(存活，失败重启容器) / readinessProbe(就绪，从Service摘除) / startupProbe(启动，成功后停止)；L3: 探针配置：httpGet/tcpSocket/exec + initialDelaySeconds/periodSeconds/failureThreshold                                | readiness和liveness的区别？startupProbe解决什么问题？   | **FlowPulse所有服务都配置liveness + readiness探针**        |
| A1-11 | **Controller 工作模式**         | ★★★ | L1: 声明式API —— 用户声明期望状态(Desired State)，Controller不断调和(Reconcile)使实际状态=期望状态；L2: 控制循环(Control Loop)：观察(Observe) → 决策(Decide) → 执行(Action)；L3: Deployment → ReplicaSet → Pod 的层级关系(滚动更新通过新建RS实现)                                                                                         | Deployment和ReplicaSet的关系？Rollback怎么实现的？     | **FlowPulse Rolling Update零停机发布策略**               |
| A1-12 | **Deployment 滚动更新与回滚**      | ★★★ | L1: rollingUpdate: maxSurge(新增Pod比例25%) + maxUnavailable(不可用Pod比例25%)；L2: 更新策略：Recreate(全量替换) vs RollingUpdate(渐进式)；L3: rollout history / rollback undo —— revision记录每次更新的PodTemplate                                                                                                | maxSurge和maxUnavailable怎么配？回滚会断连吗？          | FlowPulse灰度发布的底层机制                                |
| A1-13 | **Service 负载均衡与服务发现**       | ★★★ | L1: Service定义一组Pod的稳定访问入口(ClusterIP/NodePort/LoadBalancer/ExternalName)；L2: kube-proxy(iptables/ipvs模式)实现负载均衡——将Service VIP转发到后端Pod；L3. Headless Service(clusterIP=None) → 直接返回Pod IP(用于StatefulSet有状态服务)                                                                            | iptables模式和ipvs模式的区别？Service和Ingress的区别？    | **FlowPulse微服务间通过Service名做服务发现(Nacos替代部分)**       |
| A1-14 | **ConfigMap & Secret**      | ★★☆ | L1: ConfigMap(非敏感配置，环境变量/挂载卷/命令参数注入) / Secret(敏感数据base64编码，支持kubernetes.io/docker-configjson类型)；L2: 热更新(ConfigMap变更后挂载卷自动更新，环境变量需重启Pod)；L3. 不可变ConfigMap(immutable: true)减少kube-apiserver压力                                                                                          | Secret真的安全吗？怎么对接外部Vault？                    | **FlowPulse配置外部化：Nacos远程配置 + K8s ConfigMap本地兜底**  |
| A1-15 | **PV/PVC 存储抽象**             | ★★☆ | L1: PersistentVolume(管理员创建的存储资源) / PersistentVolumeClaim(用户申请的存储需求)；L2. 访问模式(RWO/ROX/RWX) + 回收策略(Retain/Delete/Recycle)；L3. StorageClass动态供给(Provisioner:阿里云disk/ceph-rbd/local-path)                                                                                                | RWO和RWX的区别？Local PV和云盘怎么选？                  | FlowPulse日志/数据持久化存储方案                             |
| A1-16 | **StatefulSet 有状态应用**       | ★★☆ | L1: 有序部署/删除(web-0 → web-1 → web-2...) / 稳定网络标识(固定DNS: web-0.headless-svc) / 稳定存储(PVC绑定顺序不变)；L2: vs Deployment(无序/无固定标识)；L3. 适用场景：MySQL Cluster / Redis Cluster / ZooKeeper / Kafka                                                                                                   | 为什么数据库用StatefulSet不用Deployment？扩缩容时的数据安全？   | **FlowPulse MySQL/Redis集群用StatefulSet部署**         |
| A1-17 | **DaemonSet 与 Job/CronJob** | ★☆☆ | L1: DaemonSet(每个Node上运行一个Pod副本——日志agent/监控agent)；L2: Job(执行完即退出，支持重试) / CronJob(定时任务)；L3. 失败处理(backoffLimit / activeDeadlineSeconds)                                                                                                                                                 | DaemonSet和Deployment的区别？CronJob的时间精度？       | FlowPulse日志收集器(Filebeat as DaemonSet) + 定时备份(Job) |
| A1-18 | **HPA 水平自动伸缩**              | ★★☆ | L1: HorizontalPodAutoscaler基于Metrics(CPU利用率/自定义Metrics)自动调整Replica数；L2. 算法：期望副本数 = ceil(currentMetricValue × currentReplicas / desiredMetricValue)；L3. KEDA(Kubernetes Event-driven Autoscaler)——基于队列长度/自定义事件触发伸缩                                                                    | HPA的伸缩延迟多久？冷启动怎么优化？                         | FlowPulse根据QPS自动扩容                                |
| A1-19 | **K8s 调度器(Scheduler)**      | ★★☆ | L1: 调度流程：过滤阶段(Predicates/NodeAffinity/PodAffinity/Taints Toleration/ResourceFit) → 打分阶段(Priorities/LeastRequestedPriority/BalancedResourceAllocation/ImageLocalityPriority)；L2. 调度策略：默认调度器 / 自定义调度器(Scheduling Framework/volumeBindingScheduler)；3. 抢占机制(Preemption：低优先级Pod被驱逐给高优先级) | 怎么让Pod调度到特定节点？Taint和Toleration的使用场景？        | FlowPulse关键服务指定Node亲和性(高配节点)                      |
| A1-20 | **K8s 网络模型(CNI)**           | ★★☆ | L1: CNI(Container Network Interface)插件：Flannel(VXLAN overlay) / Calico(BGP路由) / Cilium(eBPF)；L2: Pod间通信(同一Node通过cni0网桥/不同Node通过隧道或路由)；3. 网络策略(NetworkPolicy)：Pod级别入站出站白名单(Flannel不支持需Calico/Cilium)                                                                                  | Flannel和Calico的性能差异？eBPF为什么更快？              | FlowPulse集群内网络选型                                  |
| A1-21 | **K8s 安全(RBAC)**            | ★★☆ | L1: RBAC(Role-Based Access Control)：Role/ClusterRole(权限集合) + RoleBinding/ClusterRoleBinding(绑定到User/Group/SA)；L2: ServiceAccount( Pod身份认证) + Token(挂载到/var/run/secrets/)；3. Pod Security Standards(PSS)：Privileged/Baseline/Restricted三级策略                                           | 怎么限制某个Namespace只能读？ServiceAccount Token安全吗？ | FlowPulse K8s权限最小化配置                              |
| A1-22 | **K8s 故障排查**                | ★★★ | L1: kubectl describe pod(看Events)/kubectl logs(看容器日志)/kubectl exec(进容器排查)；L2: 常见错误：ImagePullBackOff(镜像拉取失败) / CrashLoopBackOff(容器反复崩溃) / OOMKilled / Pending(资源不足)；L3: 排查工具链：kubectl debug临时调试容器 / metrics-server监控 / event-exporter告警                                               | OOMKilled怎么看原因？Pending最常见的原因？               | **FlowPulse线上故障排查必备技**                            |