|序号|知识点|笔记写什么|重要度|
|---|---|---|---|
|1.1|倒排索引完整结构|Term Dictionary（词典）+ Term Index（词项索引，FST）+ Posting List（倒排表：docID+词频+位置+偏移量）；画一张完整的四层结构图|🔥🔥🔥 **绝对核心，必考**|
|1.2|FST（Finite State Transducer）压缩原理|Trie → 压缩前缀 + 后缀共享 → FST；内存占用对比；为什么用 FST 不用 B+/Hash|核|
|1.3|分词器(Analyzer) 工作流程|Character Filter → Tokenizer → Token Filter 三阶段流水线；举例 "The Quick Brown-Fox" 走完三阶段的完整输出|热|
|1.4|内置分词器对比|Standard / Simple / Whitespace / Keyword / IK（中文）的输出差异表；什么时候用 keyword 不分词|🔥🔥🔥 **高频考点**|
|1.5|中文分词（IK Analyzer）|ik_max_word（细粒度）vs ik_smart（粗粒度）差异；自定义扩展词典/停用词典；ik_max_word 对搜索的影响|热|
|1.6|相关性打分算法|TF-IDF（ES < 5）→ BM25（ES 5+ 默认）；BM25 公式及参数（k1, b 含义）；`_score` 是怎么算出来的|核|