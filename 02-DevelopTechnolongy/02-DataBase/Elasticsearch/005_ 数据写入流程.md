|序号|知识点|笔记写什么|important|
|---|---|---|---|
|5.1|Near Realtime 近实时原理|写入 → Refresh Interval（默认1s）→ 可被搜到；`_refresh` API 手动触发；`refresh_interval=-1` 关闭刷新提升批量写入性能|🔥🔥🔥 **高频考点**|
|5.2|Write 完整链路|Client → Coordinating Node → Router 到 Primary Shard → Translog → Lucene Index（In-Memory Buffer + Segment）→ 同步到 Replica → ACK；画出完整时序图|🔥🔥🔥 **核心中的核心**|
|5.3|Translog 持久化机制|类似 MySQL redo log；每条写操作先写 translog 再响应 ACK（保证持久化）；translog flush 触发 segment 落盘；`sync` vs `async` 模式|核|
|5.4|Segment Merge 合并策略|Segment 文件只追加不改（Immutable）；后台 merge 策略（tiered / log_byte_size / exponential_decay）；merge 对写入性能的影响|热|