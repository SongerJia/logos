|号|知识点|笔记写什么|重要度|
|---|---|---|---|
|1.1|为什么需要连接池|TCP三次握手 + MySQL认证握手开销；连接复用 vs 每次新建的性能对比数据|热|
|1.2|连接池的核心组件模型|连接对象(Connection) + 池(Pool) + 分配策略(Allocation) + 回收/验证(Validation) 四大组件协作关系图|核|
|1.3|JDBC Connection 的生命周期|创建 → 激活(active) → 使用 → 归还(return) → 空闲(idle) → 销毁(evict) 完整状态机|核|