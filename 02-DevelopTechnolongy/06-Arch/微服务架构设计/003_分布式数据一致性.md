|#|知识点|核心机制|适用场景|面试追问|FlowPulse 实践|
|---|---|---|---|---|---|
|**3.3.1**|**Seata AT 模式实战**|一阶段：解析SQL→查Before Image→执行业务SQL→查After Image→生Undo Log→提交本地事务+注册全局锁；二阶段：提交(删Undo Log) / 回滚(Undo Log反向执行)|短事务(秒级)、简单CRUD、无需补偿逻辑|"AT模式的脏写问题怎么解决的？全局锁的性能开销？"|**默认方案**：大部分跨服务操作(如创建流程实例+初始化任务+发通知)用AT|
|**3.3.2**|**Seata TCC 模式实战**|Try(预留资源/冻结金额) → Confirm(真正执行/扣款) → Cancel(释放资源/解冻)；三阶段幂等/空回转/悬挂防护|强一致要求、涉及资金/核心资产、有明确补偿动作|"TCC的悬挂异常是什么？怎么检测？"|资金类操作(如果有计费模块) / 库存扣减等必须强一致的场景|
|**3.3.3**|**Seata Saga 模式实战**|长时间运行的业务流程(分钟~小时级)；正向执行(T1→T2→T3...) 或反向补偿(...→C3→C2→C1)；状态机编排|长事务(审批流程)、可补偿业务、多步骤编排|"Saga的补偿失败怎么处理？人工介入机制？"|**★ 工作流本身就是天然Saga！** 流程每一步都是一个本地事务，驳回=补偿回滚|
|**3.3.4**|**可靠消息最终一致性**|上游：本地事务+写消息表(原子) → 定时任务轮询发MQ → 下游消费+幂等处理；或者用RocketMQ**事务消息**(Half Message → Commit)|对一致性要求不高、允许短暂不一致、高吞吐场景|"事务消息的Half Message机制详细说一下？"|消息发送(msg_record)：写DB记录+MQ发送保证至少一次成功|
|**3.3.5**|**幂等性设计模式**|幂等的三大层次：(1)查询天然幂等 (2)唯一索引防重复 (3)Token/状态机防重复提交；分布式锁兜底|"怎么设计一个通用的幂等框架？"|所有写操作接口都设计幂等：TaskId唯一索引 + Redis分布式锁 + 请求去重Token||
|**3.3.6**|**一致性方案选型决策**|维度：一致性强度要求 / 业务容忍延迟 / 是否有补偿能力 / 开发成本 / 团队熟悉度|综合选型|**★ 创新点二的自动选型框架**：声明场景特征 → 框架自动匹配AT/TCC/Saga||