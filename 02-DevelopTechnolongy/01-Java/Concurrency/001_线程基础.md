  
线程的创建方式

Thread vs Runnable vs Callable

线程生命周期与状态转换

线程优先级与调度策略

守护线程 daemon 特性

sleep / wait / yield / join 区别

interrupt 中断机制

线程上下文切换开销



|#|知识点|重要度|三层笔记建议|面试追问|FlowPulse 结合|
|---|---|---|---|---|---|
|B2-01|**线程生命周期六状态**|★★★|L1: New→Runnable(Ready+Running)→Blocked→Waiting→Timed_Waiting→Terminated；L2: 各状态转换的触发条件(sleep/wait/join/lock/park/IO)；L3: Runnable≠运行中！JVM的Runnable包含OS层面的Ready和Running|wait()和sleep()的状态区别？Blocked和Waiting怎么区分？|工作流任务线程池中线程状态监控|
|B2-02|**Thread.start() vs Runnable.run()**|★★☆|L1: start()创建新线程执行run()；直接调用run()在当前线程同步执行；L2: start()底层：注册线程→调用native start0()→JVM创建内核线程→回调run()；L3: 同一线程能否start两次？抛IllegalThreadStateException的原因|start()的源码流程？线程创建后立即开始执行吗？|流程异步执行必须用start()|
|B2-03|**Thread.join() 原理**|★★☆|L1: 等待目标线程终止后才继续；L2: 底层基于wait/notify——join()内部调wait(0)在thread退出时this.notifyAll()由JVM调用；L3: join(long millis)超时返回|join()死锁场景？多个线程join顺序？|批量流程处理等待所有子任务完成|
|B2-04|**Thread.interrupt() 机制**|★★★|L1: 设置中断标志位，不强制终止线程；L2: 对阻塞方法(sleep/wait/join/I/O)抛InterruptedException，对非阻塞方法仅设标志位；L3: 正确响应中断的模式：catch后恢复标志(Thread.currentThread().interrupt())或优雅退出循环|interrupt()能停止正在运行的线程吗？为什么catch InterruptedEx后要再interrupt？|**FlowPulse工作流取消操作 — 中断信号传递链路设计**|
|B2-05|**Daemon 守护线程**|★☆☆|L1: setDaemon(true)必须在start()前设置；L2: JVM退出时守护线程不管是否执行完都会被终止；L3: GC线程就是守护线程的应用|什么时候用守护线程？主线程结束后守护线程一定会停吗？|FlowPulse后台心跳检测线程可设为Dae|



