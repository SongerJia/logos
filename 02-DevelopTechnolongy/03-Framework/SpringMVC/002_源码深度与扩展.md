doDispatch() 核心流程源码分析（getHandler → getHandlerAdapter → handle → processDispatchResult）

核HandlerMapping 工作原理（RequestMappingHandlerMapping 初始化 URL 映射表）

核HandlerAdapter 工作原理（RequestMappingHandlerAdapter 调用 HandlerMethod）

ServletInvocableHandlerMethod invokeAndHandle 方法调用链

MultipartResolver 文件上传原理（CommonsMultipartResolver / StandardServletMultipartResolver）

异步请求处理（DeferredResult / Callable / WebAsyncTask）

CORS 跨域配置（CorsProcessor / @CrossOrigin）