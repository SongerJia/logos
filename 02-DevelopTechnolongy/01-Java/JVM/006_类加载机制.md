  
类加载过程五步（加载 → 验证 → 准备 → 解析 → 初始化）

热双亲委派模型 Parent Delegation

热三种类加载器（Bootstrap/Extension/Application）

打破双亲委派的方式（SPI/Tomcat/OSGi）

类加载的触发时机（new/reflect/static/main）

准备阶段 vs 初始化阶段赋值差异

解析阶段符号引用到直接引用

热ClassLoader 的 loadClass 与 findClass

自定义 ClassLoader 实现热部署


|#|知识点|重要度|三层笔记建议|面试追问|FlowPulse 结合|
|---|---|---|---|---|---|
|B1-21|**双亲委派模型(Parent Delegation)**|★★★|L1: Bootstrap → Extension → Application → Custom；L2: 打破场景：SPI(JDBC)/Tomcat/OSGi热部署；L3: loadClass()源码解析——findLoadedClass→parent.loadClass→findClass()|为什么要双亲委派？打破后如何保证安全？Tomcat的WebAppClassLoader怎么实现的？|FlowPulse自定义类加载器加载流程插件|
|B1-22|**类加载过程(加载→连接→初始化)**|★★★|L1: 加载(.class→二进制流)→连接(验证+准备+解析)→初始化(clinit)；L2: 主动引用vs被动引用的5种情况(NEW/getstatic/putstatic/invokestatic/反射子类)；L3: 接口初始化规则——只有父接口中使用的变量才触发父接口初始化|forName和loadClass的区别？类初始化的线程安全性？|Spring Boot启动时FlowPulse Bean的懒加载vs饿汉式|
|B1-23|**验证阶段(Verification)详解**|★★☆|L1: 文件格式验证→元数据验证→字节码验证→符号引用验证；L2: StackMapTable在JDK7后的作用(类型检查推导)；L3: -Xverify:none关闭验证的风险(可能加载非法class)|关闭验证能提升多少启动速度？什么情况下要开？|生产环境不建议关闭验证|
|B1-24|**准备阶段(Preparation)细节**|★★☆|L1: 为静态变量赋初始值(int=0, boolean=false, ref=null)；L2: final static常量在编译期入ConstantValue属性，准备阶段赋真值；L3: public static int val = 123; 准备后val=0不是123！|准备阶段val=0还是123？final static和非final static区别？|FlowPulse全局配置常量的初始化时机|
|B1-25|**自定义ClassLoader实战**|★★☆|L1: 继承ClassLoader重写findClass()；L2: 加密.class文件的解密加载器实现；L3: 类的等价性判断——同一个.class被不同ClassLoader加载产生不同Class对象|如何实现类的热替换？defineClass的安全约束？|工作流规则引擎的动态类加载机制|
|B1-26|**JDK9模块化JPMS影响**|★☆☆|L1: module-info.java定义模块依赖/导出包；L2: 双层架构(Boot Layer + Platform/App Layer)；L3: 与类加载器的交互变化|JPMS对反射的限制？如何兼容旧代码？|了解即可，Spring Boot对JPMS的支持现状|