|序号|知识点|笔记写什么|important|
|---|---|---|---|
|3.1|调度线程模型|JobScheduleHelper 的 while(true) 轮询：每秒扫描未来5秒内要触发的任务 → 过期处理（错过触发时间的补偿策略）→ 触发调度；preRead 预读机制避免重复触发|核|
|3.2|任务触发方式|Cron 表达式触发（主流）/ 手动触发（立即执行一次）/ 父子任务触发（父任务完成后触发子任务）/ API 触发（外部系统调用）；每种方式的适用场景|🔥🔥🔥 **日常开发高频**|
|3.3|GLUE 模式(在线开发)|在线编辑代码并版本化管理（支持 Java / Shell / Python / PowerShell）；代码热加载（每次执行拉取最新版本）；与 Bean 模式的区别和适用场景|热|