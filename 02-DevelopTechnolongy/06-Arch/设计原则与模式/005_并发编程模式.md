|#|模式|核心要点|Java 实现|面试追问|FlowPulse 应用|
|---|---|---|---|---|---|
|**1.5.1**|**不可变对象 Immutable**|创建后状态不可变；天然线程安全；final所有字段 + 深拷贝 + 不暴露引用|String / Integer / LocalDateTime|"不可变对象一定线程安全吗？如果成员是引用类型呢？"|流程定义(`ProcessDefinition`)部署后不可变，每次新版本新建|
|**1.5.2**|**线程池 Thread Pool**|生产者-消费者变种；corePool/maxPool/queue/reject策略4要素|ThreadPoolExecutor 7参数详解|"线程池参数怎么调？CPU密集型IO密集型区别？"|**Scheduler执行器线程池** / RocketMQ消费者线程池|
|**1.5.3**|**生产者-消费者 Producer-Consumer**|解耦生产者和消费者速率；通过阻塞队列缓冲|ArrayBlockingQueue / LinkedBlockingQueue / Disruptor|"ArrayBlockingQueue和LinkedBlockingQueue区别？"|**RocketMQ异步消息**：WorkflowEngine(生产) → MQ → MessageService(消费)|
|**1.5.4**|**读写锁 Read-Write Lock**|读读不互斥，读写互斥，写写互斥|ReentrantReadWriteLock / StampedLock|"StampedLock比ReadWriteLock好在哪里？"|流程实例查询(高频读) vs 状态变更(低频写)|
|**1.5.5**|**Future/Promise**|异步计算结果 holder；回调地狱的解决方案|CompletableFuture (JDK8+) / FutureTask(RunnableFuture)|"CompletableFuture链式调用怎么编排？"|异步任务调度结果收集|
|**1.5.6**|**Guarded Suspension 保护性暂挂**|等待前提条件满足再执行；wait/notify的经典用法|Condition / CountDownLatch / CyclicBarrier|"CountDownLatch和CyclicBarrier区别？"|流程等待节点(TimerTask)：等待时间到才唤醒继续|
|**1.5.7**|**两阶段终止 Two-Phase Termination**|优雅停机：先停止接收新任务，再处理完已有任务|Spring @PreDestroy / ThreadPoolExecutor.shutdown()|"shutdown()和shutdownNow()的区别？"|Netty WebSocket服务优雅关闭 / Spring Boot Graceful Shutdown|