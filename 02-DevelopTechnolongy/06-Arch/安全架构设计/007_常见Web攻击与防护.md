|#|攻击类型|攻击原理|防护手段|面试追问|FlowPulse 防护|
|---|---|---|---|---|---|
|**6.7.1**|**SQL 注入**|恶意输入拼接到SQL语句中执行：`' OR '1'='1` → 绕过认证/窃取/删库|**MyBatis #{ } 参数化绑定**(根本解法) + 输入校验(正则白名单) + 最小权限DB账号|"ORDER BY动态排序怎么防注入？#{}不能用的情况怎么办？"|**全项目MyBatis强制#{ }** + SQL审计插件检测非法${}用法|
|**6.7.2**|**XSS 跨站脚本攻击**|反射型(恶意URL中的JS被浏览器执行) / 存储型(恶意内容存DB后展示给其他用户) / DOM型(JS直接操作DOM)|输出编码(HtmlUtils.htmlEscape) + CSP(Content-Security-Policy) HTTP头 + 输入过滤 + HttpOnly Cookie|"CSP策略怎么配置？unsafe-inline有什么风险？"|CSP头: `default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'`|
|**6.7.3**|**CSRF 跨站请求伪造**|用户已登录A站点 → 恶意B站点伪造请求到A站点 → 浏览器自动携带A的Cookie → A站点误认为是用户操作|**SameSite Cookie**(Lax/Strict) + **CSRF Token**(表单隐藏字段 + Header双重校验) + 检查Referer/Origin头|"SameSite=Lax能防所有CSRF吗？GET请求呢？"|POST接口强制校验 X-CSRF-Token Header + SameSite=Lax Cookie|
|**6.7.4**|**越权访问 (IDOR/BOLA)**|**水平越权**：用户A访问了用户B的数据(改id参数) → **垂直越权**：普通用户执行了管理员操作|每次查询都校验 `data.userId == currentUser.userId`；接口级 `@PreAuthorize` + 数据级DataPermission|"你们怎么防止水平越权？每个接口都手动加userId判断吗？"|MyBatis-Plus **DataPermissionInterceptor** 自动注入数据权限SQL条件|