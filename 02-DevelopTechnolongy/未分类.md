① 语言与框架

技术栈的地基，决定你能做什么

Java 核心（JVM / 集合 / 并发）必须

Spring Boot / Spring Cloud

MyBatis / JPA / Hibernate

Go 语言（高性能场景备用）

Python（脚本 / 数据处理）

② 数据库与存储

数据是业务的核心资产，存储设计决定系统上限

MySQL（索引 / 事务 / 锁）必须

Redis（数据结构 / 持久化 / 集群）必须

Elasticsearch（全文检索 / 聚合）

MongoDB（文档型场景）

分库分表（ShardingSphere）

对象存储（OSS / MinIO）

③ 消息中间件

异步解耦、削峰填谷的核心手段

Kafka（高吞吐 / 顺序消费 / 幂等）必须

RocketMQ（事务消息 / 延迟队列）

RabbitMQ（AMQP / 死信队列）

消息可靠性 / 重复消费处理

④ 分布式与微服务

大厂面试的核心考点，薪资锚点所在

CAP / BASE 理论必须

分布式事务（Seata / TCC / Saga）必须

服务注册发现（Nacos / Eureka）

RPC 框架（Dubbo / gRPC）

限流熔断（Sentinel / Hystrix）

分布式锁（Redis / Zookeeper）

分布式 ID（雪花算法 / Leaf）

⑤ 安全与接口设计

线上系统的护城河

JWT / OAuth2 / SSO

接口幂等性设计

SQL 注入 / XSS / CSRF 防御

接口签名 / 加解密

RESTful 最佳实践

⑥ 工程效能

让你写得快、稳、可维护

设计模式（6 大原则 + 常用 23 种）

单元测试 / Mock（JUnit / Mockito）

代码规范 / CR 能力

Git 工作流 / 版本管理

性能分析（Arthas / JProfiler）


| #   | 模块           | 知识点预估 | 定位              |
| --- | ------------ | ----- | --------------- |
| 01  | **RocketMQ** | ~45   | 你的主栈，最详细        |
| 02  | **Kafka**    | ~40   | 重点在架构设计和与RMQ的差异 |
| 03  | **RabbitMQ** | ~30   | 重点在可靠性机制和路由模型   |
| 04  | **MQ 对比与选型** | ~15   | 三者横向对比 + 选型决策框架 |


#  中间件方向 — 全景规划

## 先说清楚：什么是"中间件"

```
你的代码(业务逻辑)
    ↓ 调用
┌─────────────────────────────────────┐
│            中间件层                    │
│                                     │
│  "不直接产生业务价值，但没有它业务跑不起来" │
│  "你写的Service是砖，中间件是水泥和钢筋"   │
│                                     │
│  特征：通用性 / 解耦 / 基础设施性质      │
└─────────────────────────────────────┘
    ↓ 依赖
操作系统 / 网络 / 硬件
```

---

## 📋 全量清单（按功能域分类）

### 一、已完成梳理的（✅）

|中间件|归属|状态|
|---|---|---|
|**Redis**|缓存中间件|✅ 数据库与存储|
|**Elasticsearch**|搜索引擎|✅ 数据库与存储|
|**Nacos**|注册中心+配置中心|✅ Spring Cloud|
|**Sentinel**|熔断限流|✅ Spring Cloud|
|**Spring Cloud Gateway**|API网关|✅ Spring Cloud|
|**ShardingSphere**|分库分表中间件|✅ 数据库与存储|
|**HikariCP / Druid**|数据库连接池|✅ 数据库与存储|

### 二、已规划待开始（⏳）

|#|中间件|功能域|规划位置|说明|
|---|---|---|---|---|
|1|**RocketMQ**|消息队列|即将开始（MQ模块01）|你的主栈|
|2|**Kafka**|消息队列|即将开始（MQ模块02）|大数据生态标配|
|3|**RabbitMQ**|消息队列|即将开始（MQ模块03）|可靠性之王|
|4|**Dubbo**|RPC框架|架构方向-选型|阿里系，SCA已整合|

### 三、❗ 尚未规划但建议补充的（重点看这里！）

| #   | 中间件                      | 功能域               | 重要度        | 为什么需要                                   |
| --- | ------------------------ | ----------------- | ---------- | --------------------------------------- |
| ⭐1  | **XXL-JOB / PowerJob**   | 分布式任务调度           | 🔥🔥🔥🔥🔥 | **必学！** 定时任务从单机到分布式的刚需，阿里系项目几乎标配        |
| ⭐2  | **SkyWalking**           | 分布式链路追踪           | 🔥🔥🔥🔥   | 微服务调用链路可视化，排查跨服务问题的核心工具，面试常问            |
| ⭐3  | **Nginx**                | 反向代理/负载均衡/网关前站    | 🔥🔥🔥🔰   | Java后端必须懂原理和使用，不一定精通配置                  |
| ⭐4  | **Seata**                | 分布式事务协调器          | 🔥🔥🔥🔰   | 你在Spring Cloud提过但没深入，值得单独展开             |
| ⭐5  | **Prometheus + Grafana** | 监控告警              | 🔥🔥🔥🔰   | 云原生监控标配组合                               |
| 6   | **Zookeeper**            | 协调服务              | 🔥🔥🔰     | Kafka依赖它；理解分布式协调原理的好教材；虽然Nacos替代了它但面试还考 |
| 7   | **MinIO / OSS**          | 对象存储              | 🔥🔥🔰     | 文件上传/图片存储的标配方案（阿里云OSS / MinIO自建）        |
| 8   | **Canal**                | MySQL Binlog 增量订阅 | 🔥🔥🔰     | 数据同步神器（MySQL→ES/Cache），架构师必备工具          |

### 四、了解概念即可（🔵）

|中间件|说明|在哪提到就够了|
|---|---|---|
|**RocketMQ 5.0 (gRPC协议栈)**|RMQ新版本重大升级，了解变化|选型模块一笔带过|
|**Pulsar**|新一代云原生消息平台，存算分离|MQ选型对比中占1个点|
|**Kong / APISIX**|独立API网关（非Java生态）|网关选型对比中占1个点|
|**Consul / Eureka**|其他注册中心|与Nacos对比时提及|
|**Etcd**|K8s底层存储，分布式KV|了解即可|
|**Zipkin / Jaeger**|其他链路追踪方案|与SkyWalking对比时提及|

---

## 🎯 推荐的中间件知识体系结构

综合以上，我建议把中间件整理成 **6个独立模块**：

|#|模块|包含内容|知识点预估|必要性|
|---|---|---|---|---|
|**M1**|**消息队列三件套**|RocketMQ + Kafka + RabbitMQ + 选型对比|~130|🔴 必须|
|**M2**|**分布式任务调度**|XXL-JOB 为主 + PowerJob/Elastic-Job对比|~25|🔴 必须|
|**M3**|**分布式链路追踪**|SkyWalking 核心原理 + Zipkin/Jaeger对比|~20|🔴 必须|
|**M4**|**Seata 分布式事务深入**|AT/TCC/SAGA/XA 四种模式完整拆解|~30|🟡 强烈推荐|
|**M5**|**Nginx**|反向代理/负载均衡/缓存/限流原理与实践|~20|🟡 强烈推荐|
|**M6**|**其他常用中间件扫盲**|Canal/OSS-MinIO/Prometheus+Grafana/ZooKeeper 各一个模块|~35|🟡 按需学习|

**合计：~260 个知识点**

> 其中 M1~~M3 是**必须详细展开**的核心模块，M4~~M5 是强烈推荐的进阶模块，M6 可以每个技术只写"核心原理+使用场景+与其他方案的对比"三层笔记就够。

---

## 🔗 中间件之间的协作关系图

这张图能帮你理解为什么不能孤立地学：

```
                     ┌──────────┐
                     │   用户    │
                     └────┬─────┘
                          │
                   ┌──────▼──────┐
                   │   Nginx     │ ← M5: 反向代理/静态资源/限流
                   │  (网关前站)   │
                   └──────┬──────┘
                          │
               ┌──────────▼──────────┐
               │ Spring Cloud Gateway │ ← 已有: 认证/路由/熔断
               └──────┬─────┬────────┘
                      │     │
          ┌───────────▼─┐ ┌─▼────────────┐
          │  业务服务 A  │ │   业务服务 B   │
          └──┬──┬───┬───┘ └──┬───┬───────┘
             │  │   │         │   │
        ┌────▼┐│┌──▼──┐  ┌───▼──┐│┌────▼────┐
        │Nacos│││RMQ   │  │Nacos │││  RMQ     │ ← M1: 异步解耦
        │注册 │││消息  │  │注册  │││  消息    │
        └─────┘│└──┬──┘  └──────┘│└────┬────┘
             │  │   │           │  │   │
        ┌────▼┐│┌──▼──┐    ┌───▼──┐│┌────▼────┐
        │Nacos│││Kafka │    │Nacos │││  Kafka   │ ← M1: 大数据管道
        │配置 │││日志  │    │配置  │││  日志    │
        └─────┘│└──┬──┘    └──────┘│└────┬────┘
                │   │              │   │
        ┌───────▼───▼──────────────▼───▼──────┐
        │         SkyWalking                  │ ← M3: 全链路追踪
        │       (调用链路采集&展示)              │
        └──────────────┬───────────────────────┘
                       │
          ┌────────────┼────────────┐
          ▼            ▼            ▼
    ┌──────────┐ ┌──────────┐ ┌──────────┐
    │Prometheus│ │  ELK     │ │  XXL-JOB │ ← M2/M6: 监控/日志/调度
    │+ Grafana │ │ (日志)   │ │ (定时任务) │
    └──────────┘ └────┬─────┘ └──────────┘
                      │
                 ┌────▼────┐
                 │Canal → ES│ ← M6: 数据同步
                 └─────────┘

    ┌────────────────────────────────────┐
    │         底层数据层                    │
    │  MySQL ← Seata(M4) → 分库分表(已有)   │
    │  Redis(已有)  ES(已有)  OSS(M6)      │
    └────────────────────────────────────┘
```

---

## 💡 我的建议

|优先级|模块|理由|
|---|---|---|
|P0|**M1 消息队列**|你已经提了要搞，RocketMQ是你的主栈，必须最精|
|P0|**M2 任务调度**|XXL-JOB 几乎是阿里系项目标配，面试高频|
|P0|**M3 链路追踪**|微服务项目必问SkyWalking|
|P1|**M4 Seata深入**|分布式事务是高阶考点，你已有基础可以快速深入|
|P1|**M5 Nginx**|后端工程师的基本功，不深也必须会基础用法|
|P2|**M6 其他扫盲**|按需学习，每个写一份精简笔记|



| #   | 模块             | 核心问题             | 知识点预估 | 与后端的区别                           |
| --- | -------------- | ---------------- | ----- | -------------------------------- |
| 01  | **设计原则与模式**    | 写出好代码的底层法则       | ~35   | 后端学"怎么用装饰器"，架构学"什么时候该用/不该用"      |
| 02  | **分布式系统基础理论**  | 分布式环境下哪些事做不到     | ~25   | 后端知道 CAP 名字，架构能分析业务场景该选 CP 还是 AP |
| 03  | **微服务架构设计**    | 怎么拆、怎么治理、怎么演进    | ~45   | 后端会用 Nacos，架构知道为什么拆、拆到什么粒度       |
| 04  | **高可用与容灾架构**   | 系统如何不死、如何快速恢复    | ~30   | 后端知道主从复制，架构设计多活方案                |
| 05  | **高性能架构与容量规划** | QPS从1万到100万的升级路径 | ~30   | 后端会 SQL优化，架构做全链路容量规划             |
| 06  | **安全架构设计**     | 数据不泄露、权限不越界      | ~20   | 后端加 @PreAuthorize，架构设计认证授权体系     |
| 07  | **DDD 领域驱动设计** | 用业务语言驱动系统设计      | ~35   | 后端写 Service/Controller，架构画限界上下文图 |
| 08  | **技术选型与架构决策**  | 怎么做正确的技术决定并记录    | ~15   | 后端用框架，架构选框架                      |
```
架构方向新增（上面20项中精选核心）:
  ┌──────────────────────────────────────────┐
  │ 🔴 必须掌握（高频面试）:                  │
  │   • Nginx（反向代理/负载均衡/缓存）       │
  │   • JWT + OAuth2.0 + SSO（认证体系）      │
  │   • Spring Security（授权框架）           │
  │   • Caffeine/Guava（本地缓存）            │
  │   • Kafka（选型对比+使用场景）             │
  │   • gRPC/Dubbo（RPC选型）                │
  │                                          │
  │ 🟡 应该了解（中频面试）:                  │
  │   • Prometheus + Grafana（监控）          │
  │   • ELK完整链路（日志）                   │
  │   • RabbitMQ（选型对比）                  │
  │   • 分布式事务多种方案（非仅Seata）        │
  │                                          │
  │ 🔵 知道概念即可（加分项）:                │
  │   • Service Mesh / Istio                 │
  │   • ChaosBlade（混沌工程）                │
  │   • LVS/F5/SLB（负载均衡层级）           │
  └──────────────────────────────────────────┘
```





### P0 强烈建议新增（3 个）— 🔴 不补会有明显短板

#### N1 网络编程：Netty 深入 (~40 点)

|维度|分析|
|---|---|
|**为什么必须**|杭州大厂后端面试 **Top 5 必考**。RocketMQ/Elasticsearch/RPC/网关 全是 Netty 写的。不懂 Netty = 只会"用"中间件，不会"懂"中间件|
|**和现有知识的关系**|Java NIO 的直接延伸 → Reactor 模型贯穿所有异步框架 → 零拷贝/ByteBuf 对标 JVM 内存管理|
|**核心模块规划**|架构&Reactor模型(6) / ByteBuf&零拷贝(6) / 编解码&粘包拆包(6) / Pipeline&Handler链(7) / WebSocket实战(4) / 与Tomcat对比(4) / 性能调优(7)|
|**一句话定位**|从"会用 Nginx 做代理"升级到"理解所有中间件的通信底层"|

#### N2 安全认证：Spring Security + 认证授权体系 (~25 点)

|维度|分析|
|---|---|
|**为什么必须**|后端工程师**不能说"安全不是我的事"**。JWT/OAuth2 几乎每个项目都用，Spring Security 是事实标准|
|**和现有知识的关系**|S5 扫盲只提了一行 JWT/OAuth2，远远不够；与 Spring Cloud Gateway（鉴权过滤器）直接关联|
|**核心模块规划**|Spring Security架构(5) / 认证流程FilterChain(5) / JWT无状态认证(4) / OAuth2授权码流程(3) / 密码加密&安全实践(4) / SSO单点登录(4)|
|**一句话定位**|从"知道 JWT 三个部分叫什么"到"能独立设计一套认证授权方案"|

#### N3 RPC 服务调用：Dubbo 深入 (~30 点)

|维度|分析|
|---|---|
|**为什么必须（尤其阿里系）**|Dubbo 是阿里开源招牌产品，Dubbo 3.0 支持 Spring Cloud 生态融合。杭州大厂（阿里/蚂蚁/字节/网易）**Dubbo 问的概率极高**|
|**和现有知识的关系**|与 Feign/Ribbon 对标学习 → SPI 机制对标 Java SPI / Spring Factories → 服务治理概念复用 Nacos|
|**核心模块规划**|整体架构&SPI(5) / 远程调用&代理(5) / 集群容错&路由(6) / 序列化&通信协议(5) / Dubbo 3.0新特性(4) / 与Spring Cloud整合(5)|
|**一句话定位**|从"只会 Feign 调接口"到"能聊透 RPC 框架设计原理"|

> **P0 合计：~95 个知识点，高性价比**

---

### P1 建议考虑（3 个）— 🟡 有则加分明显，没有不致命但可能被问住

#### N4 设计模式深入 (~28 点)

|为什么需要|设计模式在 Spring Core 里已经覆盖了一些（工厂/代理/模板方法/策略），但**不够深、不够系统**。大厂面试喜欢问："手写一个线程池"、"手写一个简单 RPC"，本质在考模式应用能力|
|---|---|
|**和现有的区别**|之前的设计模式散落在 Spring 各模块里作为附带知识点，这里需要**系统性整理 GoF 23 + 并发设计模式 + 架构模式**，并且每个都要有 **Java 手写实现**|
|**核心内容**|创建型(单例/工厂/建造者)/结构型(适配器/代理/装饰器/组合)/行为型(策略/模板/观察者/责任链/状态) + 并发模式(Future/Promise/Actor/Balking) + 架构模式(CQRS/EventSourcing/CDC)|

#### N5 算法与数据结构（按题算）

|为什么需要|大厂笔试**硬门槛**。不管你项目多牛，算法不过关连面试机会都没有|
|---|---|
|**注意**|这个不太适合用"知识点清单"的方式组织——更适合 **LeetCode 刷题计划**（如 Hot 100 / 剑指 Offer）。如果要列知识点，应该列的是「题型分类 + 解题模板」而非单个知识点|
|**建议处理方式**|列一个 **刷题路线图**（优先级排序的题单 + 每类题的解题套路），而不是 ~N 个知识点的形式。要不要单独做这个？|

#### N6 Linux & 工具链 (~20 点)

|为什么需要|日常排查问题的基本功。"线上 CPU 飙高了你怎么查？"答不上来很减分|
|---|---|
|**核心内容**|Linux 常用命令(top/vmstat/jstat/jmap/arthas) / Shell 脚本基础 / Git 高级操作(rebase/cherry-pick) / Maven 多模块 & 依赖冲突排查 / CI/CD 流水线概念(Jenkins/GitLab CI)|

---

### P2 按需补充（3 个）— 🟢 了解即可，时间紧可跳过

#### N7 容器化 & 部署 (~20 点)

Dockerfile 编写 / K8s 核心概念（Pod/Service/Deployment/Ingress）/ Jenkins Pipeline 基础 → 后端非核心，但云原生趋势下**至少要能看懂 k8s.yaml 配置**

#### N8 性能测试方法论 (~15 点)

JMeter / wrk 使用 / 压测指标（TPS/QPS/RT/P99）/ 性能瓶颈分析方法论 → 有压测经验很好，没有也不扣分

#### N9 SQL 调优实战 (~18 点)

Explain 执行计划深度解读 / 典型慢查询优化案例集 / 分页优化 / 索引失效场景实战 → 注意：MySQL 理论已在数据库方向完成，这里是**纯实战案例向**

---



## 10.1 可观测性三支柱理论（4点）

|#|知识点|重要度|三层笔记建议|面试追问|FlowPulse 结合|
|---|---|---|---|---|---|
|A2-01|**Metrics(指标) / Traces(链路) / Logs(日志)**|★★★|L1: Metrics：聚合数值(CPU/内存/QPS/延迟)——可聚合、低成本、适合告警；L2: Traces：请求跨服务传播路径——定位瓶颈和依赖关系；L3. Logs：离散事件文本——问题根因分析的最后一道防线；三者关联(TraceID贯穿)构成完整观测体系|只有一个够吗？为什么需要三个？哪个最重要？|**FlowPulse可观测性三大支柱完整落地方案**|
|A2-02|**RED 方法 vs USE 方法**|★★☆|L1: RED(Rate/Error/Duration)：面向请求(每个端点的速率/错误率/延迟)；L2: USE(Utilization/Saturation Errors)：面向资源(CPU利用率/饱和度/错误计数)；L3: RED用于应用层监控，USE用于基础设施监控|什么时候用RED？什么时候用USE？|FlowPulse监控体系设计方法|
|A2-03|**SLI / SLO / SLA / Error Budget**|★★★|L1: SLI(服务水平指标，具体度量如"API响应时间P99<200ms")；L2: SLO(SLI的目标值，如"月度SLO=99.9%") → Error Budget = 1 - SLO (允许0.1%的失败预算)；L3: SLA(与用户签订的服务承诺协议，违反有赔偿) + Burn Rate Alert(Error Budget消耗过快触发)|SLO设99.9%还是99.99%？Error Budget怎么用？多目标SLO冲突怎么办？|**FlowPulse SLO定义：可用性99.9% / API P99<300ms / 错误率<0.1%**|
|A2-04|**四大信号类型**|★☆☆|L1: Latency(延迟)、Traffic(流量)、Errors(错误)、Saturation(饱和度)(Google SRE Book的黄金信号)；2. 每个信号的采集方式和告警策略；3. 从信号到行动的决策树|四个信号之间有什么关联？Saturation升高时先做什么？|FlowPulse基于四大信号的告警规则设计|

## 10.2 Prometheus 指标监控（8点）

|#|知识点|重要度|三层笔记建议|面试追问|FlowPulse 结合|
|---|---|---|---|---|---|
|A2-05|**Prometheus 数据模型**|★★★|L1: Metric Name(指标名) + Labels(标签键值对对) + Value(浮点数64bit) + Timestamp(ms)；L2: Counter(只增计数器，如http_requests_total) / Gauge(可上下波动，如current_connections) / Histogram(直方图分布，如request_duration_seconds_bucket) / Summary(分位数摘要)；L3: Histogram vs Summary：Histogram可在服务端聚合(推荐)，Summary客户端计算精度高但不可聚合|为什么推荐Histogram不用Summary？Counter能减少吗(重启归零怎么办)？|**FlowPulse核心指标：Counter(流程执行数) / Gauge(在线用户) / Histogram(API延迟)**|
|A2-06|**PromQL 查询语言**|★★★|L1: 即时向量(rate(http_requests_total[5m])) / 范围向量[时间] / 操作符(+ - * / % /^ > < == != and or unless)；L2: 内置函数(rate/increase/histogram_quantile/topk/bottomk/predict_linear/histogram_avg)；3. 聚合操作(sum/avg/min/max/by without) — by子句保留标签维度|rate()和increase()的区别？irate()和rate()的区别？histogram_quantile怎么算P99？|FlowPulse Grafana Dashboard的PromQL编写|
|A2-07|**Prometheus 采集架构(Pull模式)**|★★★|L1: Prometheus Server主动Pull拉取目标(/metrics HTTP接口)；2: Service Discovery(静态配置/K8s API/E Consul/文件SD自动发现目标)；3: Pushgateway(短生命周期Job推送指标的中间层，如批处理任务)|Pull vs Push各有什么优劣？Pushgateway的数据留存问题？|**FlowPulse: Spring Boot Actuator暴露/metrics → K8s SD自动发现 → Prometheus Pull**|
|A2-08|**Grafana Dashboard 设计**|★★☆|L1: Panel类型(Time Series/Stat/Table/Heatmap/Gauge/Logs) / 变量模板(Dashboard变量实现动态筛选)；L2. 告警Annotation(面板上展示告警事件标注) / 组织文件夹管理；3. Grafana作为统一观测平台(集成Prometheus/Loki/Jaeger数据源)|怎么做好的Dashboard？变量模板怎么配？|**FlowPulse Grafana统一监控大盘**|
|A2-09|**Prometheus 高可用 & 远程存储**|★★☆|L1: Thanos/Cortex/VictoriaMetrics 解决单点故障+长期存储；2:Thanos架构Sidecar(本地Prometheus附组件)→Query Layer(统一查询)→Object Storage(S3长期存储)；3. VictoriaMetrics：高性能替代方案(更少资源占用)|单台Prometheus能扛多大流量？数据保留多久合适？|FlowPulse中期方案：VictoriaMetrics替代原生Prometheus|
|A2-10|**Alertmanager 告警管理**|★★☆|L1. 告警路由(Route: based on labels匹配分发到不同接收者) / 分组(Group By聚合相似告警防风暴) / 抑制(Inhibition静默高级告警的低级告警) / 静默(Maintenance窗口)；2: 接收器(Webhook/钉钉/企业微信/邮件/Slack/PagerDuty)；3: 告警升级(Escalation未确认则逐级通知上级)|告警风暴怎么处理？怎么区分告警级别？|**FlowPulse Alertmanager：P0电话+钉钉 / P1-P2钉钉+邮件 / 分组5min**|
|A2-11|**Exporter 开发规范**|★☆☆|L1: 自定义Exporter(Go官方client库或JMX exporter)遵循/metrics格式；2: Exporter命名规范(xxx_exporter) + Up指标健康检查；3: 自定义Collector实现(Describe/Collect接口)|怎么写自定义Exporter？Up指标必须要有吗？|FlowPulse业务指标自定义Exporter|
|A2-12|**Spring Boot Actuator + Micrometer**|★★★|L1: spring-boot-starter-actuator暴露端点(/health/info/metrics/prometheus)；L2: Micrometer(计量门面)：统一指标API → 后端适配器(Prometheus/InfluxDatadog/JMF)；3: 自定义MeterRegistry注册指标(Timer.builder("api.latency").tag("endpoint").register(registry))|Micrometer和Spring Boot Metrics的关系？怎么自定义Tag？|**FlowPulse所有微服务集成Micrometer + Prometheus**|

## 10.3 OpenTelemetry 全链路追踪（7点）

|#|知识点|重要度|三层笔记建议|面试追问|FlowPulse 结合|
|---|---|---|---|---|---|
|A2-13|**分布式追踪基本模型**|★★★|L1: Trace(一次完整请求穿越所有服务的路径) / Span(单个操作的单元) / SpanContext(TraceId+SpanId+ParentSpanId+Flags+TraceState)；L2: Attributes(Span上的键值对标签) / Events(带时间戳的事件) / Status(OK/ERROR状态码)；L3: Baggage(跨边界传递的键值对，类似HTTP Header但全链路可见)|TraceId怎么生成的？Span的父子关系怎么构建？Baggage和Attribute区别？|**FlowPulse每次请求生成唯一TraceId贯穿全链路**|
|A2-14|**OpenTelemetry 架构(OTel)**|★★★|L1: OTel = 统一标准(取代OpenTracing + OpenCensus) —— API(SDK定义) + SDK(实现) + Collector(收集器)；L2: Collector处理管线：Receiver(接收数据) → Processor(批处理/过滤/聚合) → exporter(输出到Jaeger/Zipkin/Prometheus/Otel)；3. OTel Agent(自动注入Sidecar) vs SDK Library(代码集成)两种接入方式|OTel和SkyWalking选哪个？Agent注入性能损耗？|**FlowPulse采用OTel SDK方式集成(Java Agent自动埋点)**|
|A2-15|**上下文传播(Context Propagation)**|★★★|L1: Trace Context在进程间传递：HTTP Headers(w3c traceparent/tracestate)/RPC Metadata/MQ Message Properties；L2: OTel Java自动Instrumentation(支持Spring MVC/Dubbo/HttpClient/JDBC/Kafka等框架自动传播)；L3: MDC(Mapped Diagnostic Context)将TraceId注入日志(方便在Log中搜索TraceId)|异步线程池中Context会丢失怎么办？MQ消息如何传递Context？|**FlowPulse TraceId写入MDC + 日志格式包含trace_id字段**|
|A2-16|**采样策略(Sampling)**|★★☆|L1: Head-based Sampling(Trace入口决定是否采样，节省资源但可能漏掉慢请求)；L2: Tail-based Sampling(Trace结束后根据特征决定是否保存——需OTel Collector)；L3: 自定义采样规则(按Error/延迟阈值/关键Service必采)|采样率一般设多少？Tail-based采样的代价？|FlowPulse采样策略：默认10% + Error必采 + P99>1s必采|
|A2-17|**Span 最佳实践**|★★☆|L1: Span命名(低 cardinality，不用参数值如/getUser/123而用/getUser/{id})；2: 正确设置Span Kind(Client/Server/Producer/Consumer/Internal)；3. Error Span必须设置Status=Error + 记录Exception堆栈|Span名称用变量会有什么问题？DB Slow Query怎么记录？|FlowPulse Span命名规范|
|A2-18|**Jaeger 部署与使用**|★★☆|L1: Jaeger架构：Client SDK → Collector(收集) → Query(UI查询) + Storage(Elasticsearch/Kafka/Cassandra)；L2. UI功能：Search(按TraceId/Operation/Duration/Tags搜索) / Trace View(时间线视图) / Compare Traces(对比) / Architecture(服务依赖图)；3. 生产部署：Collector HA + ES Cluster + Ingress|Jaeger和Zipkin选哪个？ES存储容量规划？|**FlowPulse Jaeger UI：日常排查超时/错误请求的首选工具**|
|A2-19|**Trace 与 Metrics 关联**|★☆☆|L1: RedMetric(基于Trace数据派生指标：Span数/错误Span率/延迟直方图)——OTel Collector内置；L2. Exemplars(指标样本携带TraceID，Grafana点击数据点直接跳转到Jaeger查看对应Trace)；3. 关联价值：从异常指标→一键跳转Trace→定位根因|Exemplar怎么配置？什么情况下用RedMetric？|FlowPulse从Grafana告警→一键跳Jaeger|

## 10.4 日志系统（6点）

|#|知识点|重要度|三层笔记建议|面试追问|FlowPulse 结合|
|---|---|---|---|---|---|
|A2-20|**结构化日志(JSON格式)**|★★★|L1: 日志格式从纯文本转为JSON(便于解析和检索)：{"timestamp":"...","level":"INFO","logger":"com.xxx","message":"...","trace_id":"abc","user_id":123}；L2: Logback/Log4j2 JSON Layout配置(logstash-logback-encoder)；MDC(Mapped Diagnostic Context)注入上下文(traceId/userId/tenantId)；3. ELK Stack: Filebeat(采集) → Kafka(缓冲) → Logstash(解析) → Elasticsearch(存储索引) → Kibana(可视化)|MDC在异步场景下怎么保证正确传递？JSON日志比文本大多少？|**FlowPulse统一日志规范：JSON格式 + trace_id/tenant_id/span_id 字段**|
|A2-21|**Loki 轻量级日志系统**|★★☆|L1: 不像ES做全文索引而是用Label(标签)匹配类Prometheus的方式——大幅降低存储成本(比ES便宜10x)；L2: LogQL(Loki Query Language)：{app="flowpulse"}|= "error"|line_format "{{.message}}"；3. 采集端：Promtail(Filebeat替代品) / Fluent Bit / OTel Collector|
|A2-22|**日志分级与采样**|★☆☆|L1: ERROR/WARN全量采集 / INFO采样(按比例或按Trace采样决定是否记录) / DEBUG关闭(生产环境)；L2. 动态日志级别调整(Logback JMX / Spring Boot Actuator/loggers endpoint)；3. 敏感信息脱敏规则(手机号/身份证/密码/Token正则替换)|动态调日志级别会影响性能吗？脱敏怎么做最安全？|FlowPulse生产环境日志策略|
|A2-23|**日志聚合与流式处理**|★☆☆|L1: Kafka作为日志缓冲(削峰填谷 + 解耦采集和消费)；L2: 流式聚合：按时间窗口统计ERROR数量(5分钟内超过50个ERROR触发告警)；3. 日志归档策略(热数据30天Loki / 冷数据90天OSS/S3)|日志Kafka用什么Topic分区？归档怎么自动化？|FlowPulse日志生命周期管理|
|A2-24|**集中式日志的挑战**|★☆☆|L1: 日志量大(QPS×平均日志行数×每行大小 = 吞吐量GB/日)；L2. 日志顺序性保证(Kafka Partition有序 + 同一TraceId路由同一Partition)；3. 多行日志合并(Exception堆栈为一条event)|日志丢失怎么发现？多行堆栈怎么保持完整？|FlowPulse日志可靠性保障|
|A2-25|**日志告警**|★☆☆|L1: Loki/Grafana告警规则(ERROR关键词频率超阈值)；L2. 日志告警降噪(同一条错误5分钟只告警一次)；3. 和Metrics告警的配合(Metrics发现问题→Logs定位原因)|日志告警误报率高怎么办？怎么写好的告警规则？|FlowPulse日志告警规则集|

## 10.5 可观测性实战平台整合（9点）

|#|知识点|重要度|核心要点|FlowPulse 应用|
|---|---|---|---|---|
|A2-26|**统一观测平台架构图**|★★★|用户请求 → Gateway → Service A/B/C → DB/MQ/Redis ↓ ↓ ↓ ↓ OTel Agent → OTel Collector → Prometheus + Jaeger + Loki → Grafana统一UI|**FlowPulse可观测性总架构**|
|A2-27|**Spring Boot 集成 OTel Agent**|★★★|java -javaagent:opentelemetry-javaagent.jar -jar app.jar (零代码侵入) / otel.exporter=otlp / otel.service.name=flowpulse-workflow|所有微服务统一Agent注入|
|A2-28|**自定义业务指标**|★★☆|Counter(流程创建数) / Timer(节点执行耗时) / Gauge(运行中实例数) / 业务SLO仪表盘|FlowPulse业务可观测性|
|A2-29|**告警规则体系设计**|★★★|P0(立即电话): 服务宕机 / 数据库连接耗尽 / Full GC持续 / Error Rate>5%；P1(30min内): P99延迟超标 / 队列积压>1000；P2(每日): 磁盘使用>80% / 证书即将过期|**FlowPulse四级告警体系**|
|A2-30|**On-call 与 值班机制**|★☆☆|PagerDuty / 一米滴答值班轮换 / 告警升级策略 / Post-mortem复盘文化|团队运维规范|
|A2-31|**Runbook(操作手册)**|★★☆|每种告警对应的标准化排查步骤(如"数据库慢查询Runbook")；不断迭代更新|FlowPulse常见问题Runbook|
|A2-32|**Chaos Engineering(混沌工程)**|★☆☆|故障演练(随机Kill Pod / 延迟注入 / CPU限制)验证系统韧性；工具：Chaos Blade / Litmus Chaos|定期演练提升稳定性|
|A2-33|**容量规划与趋势预测**|★☆☆|基于历史Metrics预测未来资源需求(线性回归/季节性分解)；提前扩容避免瓶颈|FlowPulse季度容量评审|
|A2-34|**可观测性 maturity model(成熟度模型)**|★☆☆|L1: 基础(有日志) → L2: 有告警 → L3: 有指标+追踪 → L4: 自动化(自愈) → L5: AI驱动(智能诊断)；评估当前阶段并制定提升路线|FlowPulse可观测性成熟度评估|

深度思考

---

# 🟡 P1 重要 — A3: 事件驱动架构 EDA（~22 点）

## 11.1 EDA 核心概念（5点）

|#|知识点|重要度|三层笔记建议|面试追问|FlowPulse 结合|
|---|---|---|---|---|---|
|A3-01|**EDA vs 传统同步调用**|★★★|L1: 同步(A调B→B调C→C返回→B返回→A返回，强耦合链式依赖) vs 异步(发布事件 → 订阅者自主处理，松耦合)；L2: EDA优势(解耦/扩展性/最终一致性) vs 劣势(复杂性/数据一致性保证难/调试困难)；L3: 适用判断：业务流程是否需要实时强一致？不需要则考虑EDA|什么时候不该用EDA？事件丢失了怎么办？|**FlowPulse核心架构模式：工作流状态变更→发布事件→下游异步消费**|
|A3-02|**事件风暴(Event Storming)**|★★☆|L1: DDD建模方法——领域专家+开发团队用便签纸在墙上协作探索业务领域；L2. 识别领域事件(过去式命名如"审批已通过") → 命令(触发事件的用户操作命令) → 聚合/策略/限界上下文；3. 输出：事件列表/聚合关系图/上下文映射图|一次Event Storming要多长？怎么准备？|FlowPulse领域模型设计方法|
|A3-03|**事件类型分类**|★★☆|L1: Domain Event(领域事件：状态变更如FlowInstanceApproved)/ Integration Event(集成事件：跨上下文通知如UserRegistered)/ System Event(系统事件：定时任务触发)；2. 领域事件特征(不可变/带时间戳/业务含义明确)；3. 事件命名规范：{Aggregate}{PastVerb} 如 OrderCreated / PaymentCompleted / InstanceRejected|事件应该包含哪些字段？事件粒度怎么定？|**FlowPulse事件规范：Domain Event + 版本号 + TraceId + 时间戳**|
|A3-04|**Event Envelope(信封)设计**|★★☆|L1: 统一事件结构：eventId(唯一ID) / eventType(类型) / version(版本) / timestamp(发生时间) / traceId(链路ID) / source(来源服务) / dataContentType(data格式) / data(业务载荷)；2. CloudEvents规范(Knative CNCF标准)作为参考实现；3. 序列化选择(JSON通用/Avro/Protobuf高性能)|为什么要有version？CloudEvents和自定义Envelope选哪个？|FlowPulse事件Envelope遵循CloudEvents规范|
|A3-05|**事件驱动架构模式对比**|★☆☆|L1: Notification(仅通知发生了什么，消费者需自行查数据获取详情) vs Transfer Cargo(携带完整数据，消费者无需回查) vs Request-Reply(请求-回复模式)；2. 各自优劣势(Notification解耦彻底但增加查询/Transfer Cargo减少网络往返但数据冗余)；3. 选型依据：消费者需要多少信息？数据量多大？|Transfer Cargo数据变了怎么办？Request-Reply和RPC区别？|FlowPulse采用Transfer Cargo模式(携带关键业务数据)|

## 11.2 事件可靠性保证（6点）

|#|知识点|重要度|三层笔记建议|面试追问|FlowPulse 结合|
|---|---|---|---|---|---|
|A3-06|**Outbox 模式(发件箱)**|★★★|L1: 问题：本地DB事务成功但发送MQ失败→数据不一致；解决方案：本地事务中写入Outbox表(同库同事务)→后台轮询/MQ CDC(Capture Data Change)将Outbox消息投递到MQ；L2: 实现方式①轮询定时任务扫Outbox表(简单但有延迟)②Debezium监听Binlog实时投递(实时性好)；3. Outbox表设计(id/event_type/payload/status/created_time/attempts) + 幂等去重|轮询延迟怎么控制？Binlog方案复杂度？|**FlowPulse核心可靠性保障：Outbox表 + Debezium投递到RocketMQ**|
|A3-07|**事件幂等消费**|★★★|L1: At-Least-Once语义下消息可能重复→消费者必须幂等；L2. 幂等方案：①唯一约束(DB唯一索引) ②去重表(Redis Set记录processed_event_id) ③乐观锁(版本号CAS更新)；3. 最佳实践：先做幂等判断再执行业务逻辑(而非执行后catch重复异常)|去重Key的TTL设多久？分布式事务下的幂等？|**FlowPulse每个消费者第一件事就是check幂等Redis Key**|
|A3-08|**事件顺序保证**|★★☆|L1: 全局有序(单Partition单Consumer) / 分区有序(相同Business Key路由同一Partition)；2: RocketMQ MessageQueue按shard_key取模保证分区有序；Kafka Partition内有序(Offset递增)；3. 无序事件的应对(业务层面排序或容忍无序)|跨Partition的顺序怎么保证？必须全局有序吗？|**FlowPulse同一流程实例的事件按instance_id分片保序**|
|A3-09|**事件补偿与重试**|★★☆|L1: 消费失败重试(指数退避：1s→2s→4s→8s→最大3次)→进入死信队列(DLQ)；2: 补偿机制：定时扫描DLQ人工介入 或 自动重试(加大延迟间隔)；3: Saga补偿模式(正向执行+反向补偿)处理跨服务的长事务|重试次数怎么定？自动补尝会不会雪崩？|FlowPulse DLQ + 运营后台手动重试机制|
|A3-10|**事件 Schema 演进**|★★☆|L1: 事件结构必然演化(新增字段/改名/改类型) —— 向后兼容是关键；2: 策略：新增optional字段(兼容旧消费者) / 不得删除required字段 / 字段改名(新+旧标记@deprecated) / Avro/Protobuf Schema Registry管理版本；3. 消费者版本管理(多版本并存逐步迁移)|不小心删了字段怎么办？JSON和Avro的演进能力差异？|FlowPulse事件Schema向后兼容规范|
|A3-11|**死信队列(DLQ)治理**|★☆☆|L1. DLQ Topic独立存储消费失败的消息 + 元数据(原始Topic/失败原因/重试次数/时间戳)；2. DLQ Dashboard(运营后台查看和处理DLQ消息)；3. 根因分析(DLQ消息堆积=系统性问题需立即修复)|DLQ满了怎么办？要不要自动重入队？|FlowPulse DLQ监控告警|

## 11.3 事件驱动设计模式（5点）

|#|知识点|重要度|三层笔记建议|面试追问|FlowPulse 结合|
|---|---|---|---|---|---|
|A3-12|**Event Choreography(编排)**|★★★|L1: 去中心化——没有中央协调器，各服务通过事件自主协作；L2: 示例：订单创建→支付服务监听OrderCreated→扣款成功→发布PaymentCompleted→物流服务监听→发货；L3: 优势(松耦合/扩展性好) / 劣势(流程难以追踪/循环依赖风险)|流程太长怎么追踪？循环事件怎么避免？|**FlowPulse默认采用Choreography模式**|
|A3-13|**Event Orchestration(编排器)**|★★☆|L1: 中央协调器(Process Manager/Saga Orchestrator)负责编排整体流程；2: Camunda/Zeebe 作为编排引擎(可视化流程定义+状态持久化+错误处理)；3: 对比Choreography：集中管理易理解但引入单点协调耦合|什么时候该用Orchestrator？Camunda和自建的区别？|FlowPulse复杂多步骤审批可用Orchestrator|
|A3-14|**CQRS(Command Query Responsibility Segregation)**|★★☆|L1: 命令端(写模型，处理状态变更) + 查询端(读模型，优化读取性能)，两端数据最终通过事件同步；L2. 写模型：聚合根接收Command → 发布Domain Event → 更新写DB；读模型：订阅Event → 构建/更新读模型(物化视图) → 读DB查询；3. 适用场景(读写比例差异大/读模型复杂需多数据源Join/查询性能要求高)|CQRS增加了复杂度，值得吗？读写模型的同步延迟？|**FlowPulse报表统计采用CQRS：写端流程实例DB → Event → 读端ClickHouse/Elasticsearch**|
|A3-15|**Event Sourcing(事件溯源)**|★☆☆|L1: 不存当前状态，只存所有发生过的事件序列(State = Replay all Events)；2: Snapshot(定期快照减少回放开销) + 事件不可变(追加-only) / 事件存储专用DB；3. 适用场景(金融审计/合规要求高/需要完整历史追溯)|事件量太大怎么处理？回放性能怎么优化？|FlowPulse流程审计日志参考此思想|
|A3-16|**消息通道模式(Message Channel)**|★☆☆|L1: Point-to-Point Channel(一对一，Queue实现，负载均衡) / Publish-Subscribe Channel(一对多，Topic实现)；2. Dead Letter Channel(死信通道) / Guaranteed Delivery Channel(可靠通道) / Datatype Channel(数据类型通道)；3. Enterprise Integration Patterns(EIP)经典模式集|什么时候用P2P？什么时候用Pub/Sub？|FlowPulse MQ通道设计|

## 11.4 EDA 在 FlowPulse 中的落地（6点）

|#|知识点|重要度|核心要点|FlowPulse 应用场景|
|---|---|---|---|---|
|A3-17|**流程事件总线设计**|★★★|事件总线抽象层(可切换RocketMQ/Kafka/RabbitMQ) + 统一事件发布API / 订阅注解(@EventListener)|FlowPulseEventBus.publish(new InstanceApprovedEvent(instanceId, ...))|
|A3-18|**流程生命周期事件**|★★☆|CREATED → STARTED → NODE_COMPLETED → APPROVED → REJECTED → COMPLETED → CANCELLED 每个状态变更都发布事件|下游服务(通知/统计/审计)各自订阅关心的事件|
|A3-19|**事件版本化管理**|★☆☆|事件Schema注册中心(Avro Schema Registry) / 兼容性检查(向前/向后/全兼容) / 多版本消费者灰度升级|事件接口变更不影响已有消费者|
|A3-20|**事件轨迹追踪**|★★☆|每个事件携带TraceId + SpanId → Jaeger中看到完整的事件流转链路|排查"为什么这个流程卡住了"——从事件链路一目了然|
|A3-21|**事件驱动测试策略**|★☆☆|单元测试(Mock EventBus验证事件发布) / 集成测试(Embedded MQ测试端到端) /契约测试(消费者期望的事件格式)|FlowPulse事件相关测试覆盖|
|A3-22|**反模式：Event Carried RPC**|★☆☆|反模式：把事件当RPC用(发布事件后阻塞等待特定回复事件)——失去了EDA异步解耦的优势；正确做法：需要同步响应就用RPC/gRPC，需要异步才用事件|FlowPulse架构审查 Checklist|

---

# 🟡 P1 重要 — A4: API 架构设计进阶（~23 点）

## 12.1 RESTful API 设计深化（6点）

|#|知识点|重要度|三层笔记建议|面试追问|FlowPulse 结合|
|---|---|---|---|---|---|
|A4-01|**Richardson 成熟度模型**|★★☆|L0: POX(Plain Old XML，HTTP仅作隧道) / L1: Resources(使用URI标识资源) / L2: HTTP Verbs(GET/POST/PUT/DELETE正确使用) / L3: HATEOAS(超媒体控制，返回链接引导客户端发现API)；目标至少达到L2|HATEOAS在实际项目中用的多吗？为什么大部分项目停留在L2？|FlowPulse API达到L2级别(L3渐进引入)|
|A4-02|**RESTful URI 设计规范**|★★★|L1: 名词复数(/api/v1/flow-instances 不是 /getFlowInstances)；L2: 层级关系(/instances/{id}/nodes/{nodeId}) / 过滤排序(?status=RUNNING&sort=-createTime&page=1&size=20)；L3: HTTP动词语义：POST(创建)/GET(查询)/PUT(全量替换)/PATCH(部分更新)/DELETE(删除)|PUT和PATCH的区别？批量操作怎么设计URI？|**FlowPulse API URI规范：/api/v1/{resource}/{sub-resource}?filter=&sort=&page=&size=**|
|A4-03|**统一响应体与错误码体系**|★★★|L1: 统一响应：Result< T>(code/message/data/timestamp/requestId)；L2. 错误码分层：HTTP Status(传输层) + BizCode(业务层：100xx参数/200xx权限/300xx业务/500xx系统) + 子码(具体原因)；3. 错误响应体：{"code":"FLOW.003","message":"流程不存在","details":[...],"trace_id":"abc123","timestamp":"2026-06-10T14:30:00Z"}|怎么设计错误码体系？国际化(i18n)怎么做？|**FlowPulse统一响应体 + 错误码字典(50+标准错误码)**|
|A4-04|**API 版本管理策略**|★★☆|L1: URL版本(/api/v1/) / Header版本(Accept: application/vnd.flowpulse.v2+json) / Query Parameter(?v=2)；L2: 推荐：URL版本(直观/易缓存/易测试)；L3: 版本废弃策略(保留N-1版本 / Sunset Header提示废弃日期 / 废弃期≥6个月)|什么时候需要大版本升级？怎么通知API使用者？|**FlowPulse API版本：URL路径/v1/，当前维护v1+v2**|
|A4-05|**分页、过滤与排序标准化**|★★☆|L1: 游标分页(cursor-based, after={last_id} 适合深翻页) vs 偏移分页(offset-based, page=1&size=20 适合浅翻页)；L2: 过滤语法(类OData: ?name=eq:审批&status=in:APPROVED,REJECTED&createTime=gt:2026-01-01)；L3. 排序(sort=-createTime,+name 负号降序/正号升序)|游标分页能跳页吗？过滤条件SQL注入风险？|FlowPulse API支持两种分页模式|
|A4-06|**API 安全设计(HTTPS之外)**|★★☆|L1: 请求签名(AppSecret + HMAC-SHA256签名防篡改) / 时间戳防重放(5分钟窗口) / Nonce防重复；L2. Rate Limiting(Header返回X-RateLimit-Remaining) / API Key管理(申请/审核/轮换/回收)；3. OpenAPI Spec中的securitySchemes定义安全策略|签名算法怎么设计？API Key泄露了怎么办？|FlowPulse开放API的安全机制|

## 12.2 GraphQL 与 gRPC（6点）

|#|知识点|important度|三层笔记建议|面试追问|FlowPulse 结合|
|---|---|---|---|---|---|
|A4-07|**GraphQL 核心概念**|★★☆|L1: Schema(Type/Query/Mutation/Subscription) + Resolver(字段解析函数) + 强类型；L2. 客户端按需查询(只请求需要的字段，解决over-fetching/under-fetching)；3. 单一Endpoint(POST /graphql) + Introspection(自文档化)|GraphQL适合什么场景？N+1查询问题怎么解决(DataLoader)?|FlowPulse前端聚合API可选方案|
|A4-08|**GraphQL vs REST 优劣**|★★☆|L1: GraphQL优势(灵活查询/减少请求数/强类型Schema) vs 劣势(缓存复杂/授权细粒度/学习成本/性能风险(深层嵌套查询))；L2. 缓存问题(每个查询URL不同→HTTP缓存失效→需Apollo Client/持久化查询缓解)；3. 适用判断：前端需求多变且多终端(iOS/Android/Web)时考虑GraphQL|GraphQL会取代REST吗？什么时候不该用GraphQL？|FlowPulse暂不引入GraphQL(REST够用)|
|A4-09|**gRPC 核心原理**|★★☆|L1: Protocol Buffers二进制序列化(比JSON小3-5倍/解析快10-100倍) + HTTP/2多路复用 + IDL定义Service接口；L2. 四种Service Method：Unary(一元) / Server Streaming(服务器流) / Client Streaming(客户端流) / Bidirectional Streaming(双向流)；3. Code Generation(.proto文件生成各语言Stub代码)|gRPC不支持浏览器怎么办(gRPC-Web)？和REST JSON的性能差多少？|**FlowPulse微服务间内部通信评估gRPC替代Dubbo(保持协议灵活性暂用Dubbo)**|
|A4-10|**gRPC Streaming 类型详解**|★☆☆|L1. Server Streaming(一次请求→持续推送多条响应)：大文件下载/实时行情推送；L2: Client Streaming(持续上传→一次响应)：文件上传/数据上报；L3. Bidirectional(双向实时通信)：聊天/协同编辑|Stream有背压机制吗？连接断开怎么办？|FlowPulse大文件导出可用Server Streaming|
|A4-11|**Protocol Buffers 进阶**|★☆☆|L1. proto3语法(message/service/enum/map/repeated/oneof/reserved) / 向后兼容规则(不能修改已有tag编号)；L2. Well-Known Types(Timestamp/Duration/Any/Struct/Empty) / 自定义Options；3. 编码原理(Varint变长整数/Tag-Length-Value格式)|tag为什么不能用1-15之外的？Any类型怎么用？|了解即可|
|A4-12|**API Gateway 统一入口增强**|★★☆|L1. Gateway功能矩阵：路由转发/认证鉴权/限流熔断/协议转换(REST↔gRPC)/请求响应日志/缓存；L2. Kong(插件丰富)/Spring Cloud Gateway(Spring生态)/APISIX(高性能Go+Lua)；3. Gateway vs Service Mesh边界(Gateway管南北流量/Mesh管东西流量)|Gateway和Ingress的关系？Kong和APISIX怎么选？|**FlowPulse Spring Cloud Gateway + 自定义过滤器链**|

## 12.3 OpenAPI 规范与文档（4点）

|#|知识点|重要度|三层笔记建议|面试追问|FlowPulse 结合|
|---|---|---|---|---|---|
|A4-13|**OpenAPI 3.0 规范(Swagger)**|★★☆|L1: openapi.yaml/yaml定义：info(标题描述版本) / servers(环境地址) / paths(端点定义) / components(数据模型复用)；L2: Operation Object(summary/description/requestBody/responses/security/tags)；3. Code Generation(根据Spec生成Client SDK/Server Stub)|Swagger UI怎么部署？Mock Server怎么生成？|**FlowPulse每个API都有OpenAPI文档 + Swagger UI**|
|A4-14|**API 文档自动化**|★★☆|L1: Springdoc-openapi(自动从Controller注解生成OpenAPI文档) + Swagger Annotations(@Operation/@ApiResponses/@Schema)；L2. CI/CD集成(构建阶段自动校验API Spec变更 + 生成文档站点)；3. Postman Collection(从OpenAPI导入)用于联调和自动化测试|文档和代码怎么保持同步？怎么做多版本文档？|FlowPulse文档CI流水线|
|A4-15|**API 契约测试(Pact)**|★☆☆|L1. Pact(消费者驱动契约)：Consumer定义期望的交互(Contract) → Pact Broker共享 → Provider验证是否满足Contract；L2: 防止Provider修改API破坏Consumer(回归保护)；3. Pact Flow(CI/CD中自动化契约验证)|Pact和集成测试的区别？Contract First vs Code First？|微服务间集成质量保障|
|A4-16|**API 变更影响分析**|★☆☆|L1. Breaking Changes(删除字段/改变类型/必填改可选/删除端点)；2. Non-Breaking(新增字段/新增可选端点/添加新的Response Header)；3. 变更通知机制(Changelog/Webhook通知Consumer)|删除一个deprecated接口前要做什么？|FlowPulse API变更流程|

## 12.4 API 性能与缓存（7点）

|#|知识点|重要度|三层笔记建议|面试追问|FlowPulse 结合|
|---|---|---|---|---|---|
|A4-17|**API 响应时间优化策略**|★★☆|L1. 减少网络RTT(合并请求/Batch API) / 减少传输体积(Gzip/Brotli压缩/字段裁剪(field masking)) / 并行请求(前端并发调用多个API)；L2. 服务端优化(Connection Pool Keep-Alive / 异步非阻塞IO / 数据库查询优化)；3. 监控指标(P50/P95/P99延迟分解：DNS+TCP+TLS+Processing+Queueing)|Gzip压缩率一般多少？Brotli兼容性怎么样？|**FlowPulse API P99 < 300ms 目标**|
|A4-18|**API 缓存策略**|★★☆|L1. HTTP Cache-Control(public/private/max-age=no-cache/must-revalidate/s-maxage) + ETag(If-None-Match协商缓存) + Last-Modified；L2. CDN缓存(静态资源边缘加速) + API网关级缓存(热门配置数据) + 应用级缓存(本地+分布式)；L3. 缓存失效(Cache Invalidation): 主动失效(purge API) / TTL过期 / 标签失效(Tag-based)|CDN缓存和API网关缓存的配合？ETag的计算规则？|FlowPulse API缓存层级设计|
|A4-19|**API 网关限流深度设计**|★★☆|L1. 限流维度(IP/User/API-Key/Service-Level) / 限流算法(固定窗口/滑动窗口/令牌桶/漏桶/自适应限流)；L2. Sentinel热点限流(精确到某个API参数值) / 用户维度限流(每用户每秒N次)；3. 限流响应(429 Too Many Requests + Retry-After Header + X-RateLimit-Remaining)|分布式限流的计数存在哪？限流了用户该看到什么提示？|**FlowPulse Gateway限流：IP维度1000qps + User维度100qps + API维度分级**|
|A4-20|**API 幂等性设计**|★★☆|L1. Idempotency Key(客户端生成UUID放在Header: X-Idempotency-Key) + 服务端Redis/Set记录已处理的Key；L2. GET/HEAD/PUT/DELETE天然幂等 / POST/PATCH需要额外设计；3. 幂等窗口(Key的TTL设置，一般72h足够)|幂等Key由客户端还是服务端生成？网络超时导致重复提交怎么处理？|**FlowPulse POST/PUT操作强制要求Idempotency-Key**|
|A4-21|**API 压缩(Brotli/Gzip)**|★☆☆|L1. Accept-Encoding: br,gzip 协商压缩算法 / Brotli(比Gzip小15-25%但CPU消耗略高)；L2. Nginx配置(brotli on; brotli_comp_level 6; brotli_types text/plain application/json);3. 预压缩(构建时预压缩静态资源 + nginx try_files优先提供.br/.gz文件)|Brotli对所有内容类型都更好吗？动态内容能压吗？|FlowPulse Nginx Brotli配置|
|A4-22|**API 向后兼容设计原则**|★☆☆|L1. 只增不减(新增字段/新端点/新枚举值，不删除不改名不改类型)；L2. 默认值兼容(新增必填字段给合理default) / 宽进严出(输入宽松校验/输出严格符合Spec)；3. Sunset策略(提前6个月通知废弃 + 返回Deprecation Warning Header)|怎么发现不兼容的变更？API Governance工具？|FlowPulse API演进原则|
|A4-23|**Webhook 设计**|★☆☆|L1. Webhook = 事件驱动的回调(HTTP POST推送数据到订阅者URL)；L2. 安全(HMAC签名验证/白名单IP/HTTPS only) / 可靠性(重试指数退避最多5次) / 幂等(Webhook-Id去重)；3. 管理界面(注册/查看/重试/暂停Webhook)|Webhook和Polling选哪个？Webhook重试失败了怎么办？|**FlowPulse流程完成回调外部系统的机制**|

---

# 🟡 P1 重要 — A5: CI/CD 与 DevOps（~28 点）

## 13.1 CI/CD 流水线基础（7点）

|#|知识点|重要度|三层笔记建议|面试追问|FlowPulse 结合|
|---|---|---|---|---|---|
|A5-01|**CI/CD 概念与价值**|★★☆|L1: CI(Continuous Integration，持续集成——频繁合并代码+自动化构建测试) / CD(Continuous Deployment/Delivery，持续交付/部署——自动化部署到生产)；L2: 四个阶段：Source → Build → Test → Deploy → Monitor；L3: 核心价值：快速反馈(几分钟内知道代码有没有问题) + 降低发布风险(自动化减少人为失误)|CI和CD有什么本质区别？一天部署多次正常吗？|**FlowPulse GitLab CI/CD 完整流水线**|
|A5-02|**Git Flow vs GitHub Flow vs Trunk Based**|★★☆|L1: Git Flow(主开发develop/功能feature/release/hotfix多分支，适合严格版本发布)；L2: GitHub Flow(main分支始终可部署/PR合并到main/简化Git Flow)；L3. Trunk Based Development(主干开发+Feature Toggle特性开关控制发布，极致CI/CD)|团队选哪种？Feature Flag怎么管理？|FlowPulse采用GitHub Flow(简洁高效)|
|A5-03|**Pipeline 核心阶段设计**|★★★|L1: ①Code Quality(SonarQube静态扫描+Checkstyle+PMD) → ②Unit Test(Jacoco覆盖率>80%) → ③Build(Maven打包+Docker镜像构建+镜像安全扫描Trivy) → ④Integration Test(Testcontainers启动依赖服务测试) → ⑤Deploy to Dev(Dev环境部署冒烟) → ⑥Deploy to Prod(生产灰度发布)；L2. Pipeline as Code(.gitlab-ci.yml声明式定义) / 参数化(environment/branch/tag)；3. 失败快速Fail Fast(第一阶段失败立即停止后续阶段节省资源)|哪个阶段最耗时？怎么并行化？|FlowPulse Pipeline 6阶段定义|
|A5-04|**GitLab CI 配置详解**|★★★|L1. stages / job(only:except:variables:tags:image:script:before:after/services:artifacts/cache)；L2: 关键字(include引用模板/rules条件匹配/needs DAG依赖/parallel并行/retry重试/timeout超时/allow_failure允许失败继续)；3. 缓存(cache: key:files:[pom.xml] paths:[.m2/] 提速Maven依赖下载)|include模板怎么组织？artifacts过期清理？|**FlowPulse .gitlab-ci.yml 标准模板**|
|A5-05|**Docker 镜像构建优化**|★★★|L1: 多阶段构建(Builder Stage编译→Runtime Stage运行)减小镜像体积(JDK 400MB→JRE 80MB)；L2. Layer缓存最大化(不变的内容COPY在前/依赖安装单独一层)；L3. distroless镜像(无Shell无包管理器，最小攻击面) + 镜像签名(trust/Notary验证)|镜像体积怎么监控？构建速度慢怎么办？|**FlowPulse镜像:eclipse-temurin:17-jre-alpine (JRE ~80MB)**|
|A5-06|**容器镜像仓库**|★★☆|L1. Harbor(企业级：RBAC/漏洞扫描/镜像签名/复制同步/Helm Chart仓库)；L2. 镜像标签策略(commit-sha作为tag/不用latest/定时清理无用镜像)；3. 镜像安全扫描(Trivy在CI Pipeline中扫描CVE漏洞+阻断策略)|Harbor的高可用怎么搭？镜像垃圾回收策略？|**FlowPulse私有Harbor镜像仓库**|
|A5-07|**制品依赖管理**|★☆☆|L1. Maven私服(Nexus Repository: central代理+3rd-party+releases+snapshots)；2. 依赖安全检查(OWASP Dependency-Check / Snyk / Dependabot Alerts)；3. 制品版本管理(Semantic Versioning + CHANGELOG.md)|Nexus挂了怎么恢复？依赖冲突怎么排查？|FlowPulse Nexus制品管理|

## 13.2 测试金字塔与自动化（6点）

|#|知识点|重要度|三层笔记建议|面试追问|FlowPulse 结合|
|---|---|---|---|---|---|
|A5-08|**测试金字塔三层**|★★★|L1: Unit Test(底层大量，快/便宜，Mock外部依赖) → Integration Test(中层适中，Testcontainers真实组件) → E2E Test(顶层少量，慢/贵，真实环境模拟)；L2. 比例建议：70% Unit / 20% Integration / 10% E2E；3. 反模式(Inverted Pyramid冰激凌倒置：大量E2E慢且脆弱)|为什么E2E不能太多？Mock和Stub的区别？|**FlowPulse测试策略：JUnit5 + Testcontainers + Cypress E2E**|
|A5-09|**单元测试最佳实践**|★★☆|L1: JUnit5(@Test/@DisplayName/@Nested/@ParameterizedTest) + Mockito(mock/spy/verify) + AssertJ(流式断言)；L2. 测试命名规范(methodName_scenario_expectedResult 如approveInstance_whenAdmin_shouldApprove)；3. 覆盖率工具JaCoCO(report.xml) + CI门禁(minimum coverage 80% branch 60%)|私有方法怎么测？需要测试getter/setter吗？|FlowPulse JUnit5 + Mockito + JaocoCo|
|A5-10|**Testcontainers 集成测试**|★★☆|L1: Testcontainers(Java库，在Docker中运行真实的MySQL/Redis/RabbitMQ等依赖服务进行集成测试)；L2: @Container注解自动管理容器生命周期(测试启动创建/结束销毁) + 网络桥接(容器间互通)；3. 优势：测试环境与生产环境一致(消除"在我机器上能跑")|Testcontainers对CI性能有影响吗？怎么加速(容器复用)？|**FlowPulse集成测试全部用Testcontainers(MySQL8+Redis7+RocketMQ容器)**|
|A5-11|**API 自动化测试**|★★☆|L1: RestAssured(Java DSL: given().when().then()风格API测试) + WireMock( mock下游依赖服务)；L2.契约定向测试(Pact验证服务间接口契约) + Postman/Newman Collection Runner；3. 性能测试(JMeter/Gatling在CI中进行基础性能回归)|API Token怎么管理？测试环境数据怎么隔离？|FlowPulse API自动化测试套件|
|A5-12|**E2E 端到端测试**|★☆☆|L1. Cypress(现代E2E框架，实时重载/自动等待/截图录像/Debug能力强) / Playwright(微软开源，多浏览器支持)；L2. 测试数据隔离(每次测试前重置数据库/随机测试数据) + Headless CI模式；3. E2E不是越多越好(关注Critical Path核心业务流程即可)|Cypress和Playwright怎么选？E2E测试不稳定(Flaky)怎么办？|FlowPulse E2E覆盖核心流程(创建→审批→完成)|
|A5-13|**SonarQube 代码质量管理**|★☆☆|L1. SonarScanner在CI中扫描代码 → SonarQube Server分析(Bugs/Code Smells/Vulnerabilities/Duplication/Coverage)；2. Quality Gate(质量门禁：New Code必须达到A级否则阻断Pipeline)；3. 技术债管理(技术债估算/趋势图/修复优先级)|Quality Gate标准怎么定？误报怎么处理？|**FlowPulse SonarQube质量门禁：Bugs=0 / Vulnerabilities=0 / Coverage>80%**|

## 13.3 部署策略（6点）

|#|知识点|重要度|三层笔记建议|面试追问|FlowPulse 结合|
|---|---|---|---|---|---|
|A5-14|**滚动更新(Rolling Update)**|★★★|L1: 逐个替换Pod(停止旧版→启动新版→健康检查通过→继续下一个)；L2. K8s Deployment RollingUpdate(maxSurge 25% + maxUnavailable 25%)；L3. 优势(零停机/平滑过渡) / 风险(新旧版本同时在线可能兼容性问题)|滚动期间数据库Schema变了怎么办？怎么快速回滚？|**FlowPulse默认部署策略：Rolling Update**|
|A5-15|**蓝绿部署(Blue-Green)**|★★☆|L1: 两套完全相同的环境(Blue当前生产/Green下一版本) → Green部署验证完毕 → 切换流量(Instant Switch)；L2. 流量切换方式(DNS切换/LB权重调整/Ingress Route切换) / 瞬切(一秒内完成)；3. 优势(零停机+即时回滚只需切回去) / 成本(双倍资源)|Blue-Green适合什么场景？数据库迁移怎么做？|FlowPulse重大版本(如数据库Schema变更)使用蓝绿部署|
|A5-16|**金丝雀发布(Canary Release)**|★★★|L1: 先发布一小部分流量(1%/5%/10%)到新版本观察指标(错误率/延迟/资源) → 逐步增大流量 → 全量发布 or 回滚；L2. 实现方式：K8s Pod比例(两个RS不同replica数) / Istio VirtualService(按Header/Cookie分流) / Nginx Canary(by_weight) / Gateway路由规则；3. 金丝雀指标(金丝雀组vs基准组的Error Rate/Latency/P99对比)|金丝雀发现问题的概率怎么算？多久可以全量？|**FlowPulse灰度发布：Gateway按Header(X-Canary:true)分流5%流量到新版本**|
|A5-17|**Feature Flag(特性开关)**|★★☆|L1. 功能开关控制代码路径(不部署即可开启/关闭功能)；L2. 开关类型(Release Toggle发布开关/Experiment Toggle实验开关/Ops Toggle运维开关/Permission Toggle权限开关)；3. 管理平台(Unleash/LaunchDarkly/自建Feature Flag Service) + 灰度规则(按用户百分比/用户属性/白名单)|Feature Flag技术债务怎么管理？老开关怎么清理？|FlowPulse新功能上线先用Feature Flag灰度|
|A5-18|**暗发布(Dark Launch)**|★☆☆|L1. 新版本部署但不暴露流量(只收集Metrics/Logs验证行为正确)；L2. 验证通过后再逐步放量(转为金丝雀)；3. 用于高风险变更(数据库迁移/核心算法改动)|暗发布和金丝雀的区别？怎么验证暗发布的结果？|高风险变更前的验证手段|
|A5-19|**回滚策略**|★★☆|L1. K8s rollout undo(一键回滚到上一个Revision)；L2. 数据库回滚(Flyway/Liquibase版本化迁移，down命令回滚Schema——但很多情况需要forward-compatible迁移)；3. 配置回滚(Nacos配置中心版本管理/回滚到上一个配置版本)|回滚时用户正在写的数据怎么办？怎么做到无损回滚？|**FlowPulse回滚预案：App(kubectl undo) + DB(forward migration compatible) + Config(Nacos回滚)**|

## 13.4 DevOps 文化与实践（5点）

|#|知识点|重要度|三层笔记建议|面试追问|FlowPulse 结合|
|---|---|---|---|---|---|
|A5-20|**DevOps 核心文化 CALMS**|★☆☆|L1: Culture(文化：协作共享) / Automation(自动化一切) / Lean(精益：消除浪费) / Measurement(度量：数据驱动) / Sharing(分享知识)；2. DevOps不是工具而是文化和实践；3. 从瀑布→敏捷→DevOps→DevSecOps(安全左移)的演进|DevOps和敏捷有什么关系？DevSecOps具体做什么？|团队DevOps转型|
|A5-21|**GitOps 声明式运维**|★★☆|L1: Git作为单一事实来源(Single Source of Truth) —— 所有基础设施和应用配置都以YAML声明式存于Git；L2. Argo CD / Flux 持续同步(Git State → Cluster State 自动 reconcile)；3. Pull Model(ArgoCD拉取Git变更并应用) vs Push Model(推送到集群)|Argo CD和Flux怎么选？GitOps和传统CI/CD的区别？|**FlowPulse未来方向：Argo CD GitOps应用部署**|
|A5-22|**基础设施即代码(IaC)**|★★☆|L1. Terraform(多云 provisioning: AWS/阿里云/K8s资源) / Ansible(配置管理) / Kustomize(K8s overlay管理多环境差异)；L2. IaC原则(版本化/幂等/不可变性/模块化)；3. 状态管理(tfstate远程存储(S3/OSS)+加密锁定防并发)|Terraform drift检测(计划外变更)？多环境怎么管理变量？|FlowPulse K8s IaC: Terraform + Kustomize|
|A5-23|**变更管理与发布审批**|★☆☆|L1. 变更等级划分(P1标准/P2一般/P3紧急) + 不同等级审批流程(标准自动/一般TL审批/紧急值班经理+TL)；L2. Change Window(变更窗口：周二-周四 10:00-18:00禁止变更) + 发布清单(Pre-flight Checklist)；3. 发布后验证(Smoke Test + 关键指标监控30min)|紧急发布的审批流程怎么设计？热修复怎么处理？|FlowPulse发布管理制度|
|A5-24|**MTTR/MTBF/Change Failure Rate**|★☆☆|L1: MTTR(平均恢复时间，目标<1h故障) / MTBF(平均故障间隔) / CFR(变更失败率，精英团队<15%)；L2. DORA指标(Deployment Frequency/Lead Time for Change/Time to Restore/Change Failure Rate)；3. 持续改进(回顾会议Retrospective：What went well/What to improve/Action Items)|DORA指标怎么度量？怎么提升Deployment Frequency？|FlowPulse DevOps度量指标|

深度思考

---

# 🟣 P2 加分 — A6: 架构演进与重构策略（~18 点）

## 14.1 架构演进模式（6点）

|#|知识点|重要度|三层笔记建议|面试追问|FlowPulse 结合|
|---|---|---|---|---|---|
|A6-01|**绞杀者模式(Strangler Fig)**|★★★|L1: 在旧系统外围逐步构建新功能，每次替换一小块，像绞杀植物一样慢慢替代旧系统；L2: 实现步骤：①识别要替换的功能②在新系统中实现③API Gateway路由(旧功能→旧系统/新功能→新系统)④逐步扩大新系统范围⑤旧系统最终废弃；L3. 适用场景：单体→微服务迁移 / 技术栈替换(如PHP→Java) / 遗留系统改造|绞杀者模式需要多长时间？新旧系统数据怎么同步？|**FlowPulse如果未来重构核心引擎可使用此模式**|
|A6-02|**抽象分支(Anticorruption Layer, ACL)**|★★☆|L1: 在新系统和遗留系统之间建立防腐层，翻译/适配两个系统的领域模型差异；L2. ACL实现：Adapter Pattern + DTO转换 + 协议转换(Rest→SOAP/Dubbo→gRPC)；3. ACL的临时性(目标是最终消除ACL当所有功能迁移完毕)|ACL会不会成为性能瓶颈？什么时候该移除？|FlowPulse对接外部老旧系统的适配层|
|A6-03|**Branch by Abstraction**|★★☆|L1: 先创建一个抽象接口(让调用方依赖抽象而非具体实现) → 新旧实现都实现这个接口 → 切换实现无需改动调用方；L2: 配合Feature Flag动态切换实现；L3. Martin Fowler经典重构模式，降低大型重构的风险|和Strategy模式的区别？怎么保证新旧实现的兼容？|FlowPulse组件替换时的安全过渡方式|
|A6-04|**Sidecar 模式**|★★☆|L1: 核心应用旁边部署一个辅助容器(Sidecar)，处理横切关注点(日志/监控/配置/代理)；L2: K8s中Pod的多容器天然支持Sidecar(Log Sidecar/Filebeat + Main App)；L3. 与Service Mesh的关系(Mesh就是全局Sidecar)|Sidecar和Init Container的区别？资源怎么分配？|FlowPulse Pod中Log Collector作为Sidecar|
|A6-05|**前后端分离演进(BFF)**|★☆☆|L1: BFF(Backend for Front端)——为不同前端(iOS/Android/Web/H5)定制专属API层；L2. BFF聚合多个微服务的数据返回前端所需的格式；3. BFF的粒度控制(避免变成胖API层/BFF应保持轻量)|BFF和API Gateway的区别？BFF多了维护成本怎么办？|FlowPulse移动端/PC端可考虑BFF|
|A6-06|**模块化单体(Modulith)**|★☆☆|L1: 单体应用内部按业务域划分清晰模块(Module)，模块间通过定义好的接口通信(禁止循环依赖)；L2. Spring Modulith(官方支持：模块依赖检测/@Module注解)；3. 作为单体到微服务的中间态——先模块化再逐个拆分为服务|模块化单体的边界怎么划分？和DDD限界上下文的关系？|FlowPulse当前架构定位为模块化单体|

## 14.2 技术债务管理（5点）

|#|知识点|重要度|三层笔记建议|面试追问|FlowPulse 结合|
|---|---|---|---|---|---|
|A6-07|**技术债务分类与量化**|★★☆|L1: 无意债务(deadline赶工产生的代码坏味道) vs 有意债务(权衡后主动选择的技术捷径)；L2: 量化方法：SonarQube技术债天数 / 代码复杂度圈复杂度>10的数量 / 测试覆盖率缺口 / 安全CVE数量；3. 债务登记簿(Technical Debt Register：描述/影响/修复成本估算/优先级)|怎么说服PM分配时间还债？"永远没时间重构"怎么破？|FlowPulse技术债定期盘点|
|A6-08|**重构方法论(Fowler Refactoring)**|★★☆|L1: 重构前提(有测试覆盖才安全重构) / 重构节奏(小步持续而非大规模一次性重构)；L2. 经典重构手法：Extract Method / Extract Class / Move Method / Replace Conditional with Polymorphism / Introduce Parameter Object；3. IDE自动化重构(IntelliJ安全重构：Rename/Extract/Move/Safe Delete)|没测试能重构吗？重构和重写的区别？|FlowPulse Code Review中的重构建议|
|A6-09|**架构决策记录(ADR)**|★★★|L1: ADR(Architecture Decision Record)：每个重大技术决策都有书面记录(Context → Decision → Consequences → Status)；L2. ADR模板：标题/日期/状态(Proposed/Accepted/Deprecated/Superseded) / 背景/决策选项及权衡/后果/替代方案；3. ADR目录(docs/architecture/adr-001-mysql-vs-postgresql.md)|ADR和设计文档的区别？ADR需要评审吗？|**FlowPulse已有ADR体系，此处强化执行**|
|A6-10|**架构适应度函数(Architecture Fitness Function)**|★☆☆|L1: 用自动化测试验证架构属性是否满足要求(如"所有服务必须在100ms内响应"/"数据库不允许跨服务直连")；L2. Fitness Function在CI中运行(架构合规性门禁)；3. Neil Ford Building Evolutionary Architectures提出的概念|Fitness Function怎么写？CI中怎么集成？|FlowPulse架构守卫规则|
|A6-11|**演进式架构(Evolutionary Architecture)**|★☆☆|L1. 引导性变更(Guided Change)：架构设计时预埋扩展点，让未来变更容易进行；L2. 架构维度(可演化性/安全性/可靠性/性能/成本等) + Fitness Functions守护各维度不退化；3. 与Big Bang重写的对比(演进式风险低但慢/重写快但风险极高)|什么情况下必须Big Bang重写？演进的极限在哪？|FlowPulse架构演进路线图规划|

## 14.3 架构治理（7点简明表格）

|#|知识点|重要度|核心要点|FlowPulse 应用|
|---|---|---|---|---|
|A6-12|**架构评审委员会ARB**|★☆☆|重大架构变更需ARB评审通过(含TL+Architect+SRE代表)；定期会议(双周)|FlowPulse架构治理机制|
|A6-13|**服务治理规范**|★★☆|服务注册/发现规范 / 接口版本管理 / 依赖关系限制(避免深层嵌套调用) / SLA定义|微服务开发规范文档|
|A6-14|**API 变更审批流程**|★☆☆|Breaking Change需审批 / 向后兼容变更自助 / API Deprecation通知机制(提前30天)|API治理流程|
|A6-15|**混沌工程实践**|★☆☆|定期故障演练(Game Day)：随机Kill Pod / 网络延迟注入 / DNS故障 / 磁盘写满；验证恢复流程有效性|FlowPulse季度Chaos Game Day|
|A6-16|**容量规划流程**|★☆☆|季度容量评估(QPS增长趋势/存储增长/预算申请) / 大促前专项容量规划 / 弹性伸缩预案|FlowPulse容量管理体系|
|A6-17|**架构文档维护**|★☆☆|C4 Model(Level1~4: Context/Container/Component/Code) + ADR + 决策树 + as-is/to-be架构图|docs/architecture/ 目录结构|
|A6-18|**成本优化FinOps**|★☆☆|云资源成本监控(标签化/部门分摊) / 资源利用率分析(推荐降配实例) / Spot Instance使用 / 存储冷热分层|降低FlowPulse云基础设施成本|

---

# 🟣 P2 加分 — A7: 大数据架构基础（~22 点）

## 15.1 数据仓库分层（5点）

|#|知识点|重要度|三层笔记建议|面试追问|FlowPulse 结合|
|---|---|---|---|---|---|
|A7-01|**数仓分层模型(ODS-DWD-DWS-ADS)**|★★☆|L1: ODS(操作数据层，原始数据贴源复制) → DWD(明细数据层，清洗标准化) → DWS(汇总数据层，按主题汇总) → ADS(应用数据层，直接供报表/BI使用)；L2. 分层价值：解耦复用/空间换时间(中间层计算一次多处使用)/问题隔离(某层出错不影响其他层)；3. 缓慢变化维度(SCD)处理(Type 1覆盖/Type 2新增行加生效期/Type 3增加字段)|为什么需要四层？SCD Type 2怎么查最新数据？|FlowPulse数据分析平台的数仓设计参考|
|A7-02|**维度建模 vs 范式建模**|★☆☆|L1: 维度建模(Kimball，面向业务/星型模型/雪花模型事实表+维表) vs 范式建模(Inmon，面向数据集成/3NF范式)；L2. 主流选择：维度建模(更实用/查询性能好/易于业务理解)；3. 事实表类型(事务事实/周期快照/累积快照)|星型和雪花选哪个？宽表(大宽表反范式化)的利弊？|FlowPulse报表采用维度建模(星型)|
|A7-03|**ETL vs ELT**|★☆☆|L1: ETL(Extract-Transform-Load，先转换再加载到数仓) / ELT(Extract-Load-Transform，先加载到数仓再用SQL转换)；L2. ELT在大数据时代更主流(利用数仓计算能力/Hive/Spark做Transform)；3. 工具链(DataX/Airflow调度/Flink实时ETL)|ETL和ELT分别适合什么场景？Airflow怎么用？|FlowPulse离线数据用ELT(Hive SQL)|
|A7-04|**数据质量管理(DQ)**|★☆☆|L1. DQ维度(完整性/准确性/一致性/及时性/唯一性)；L2. DQC规则引擎(单字段检查/跨字段检查/跨源检查/波动性监控)；3. 异常数据处理(阻断报警/标记异常/自动修正)|DQ覆盖率目标多少？异常数据比例多少算健康？|FlowPulse数据质量监控|
|A7-05|**元数据管理**|★☆☆|L1. 元数据分类(技术元数据:表结构/字段类型/分区信息 vs 业务元数据:业务含义/负责人/安全等级 vs 操作元数据:访问日志/计算任务记录)；2. Apache Atlas(元数据管理平台) + 血缘关系(Lineage，字段级溯源)；3. 数据地图(Data Catalog，数据资产门户)|血缘分析怎么做的？为什么需要元数据管理？|了解即可|

## 15.2 Flink 实时计算（5点）

|#|知识点|important度|三层笔记建议|面试追问|FlowPulse 结合|
|---|---|---|---|---|---|
|A7-06|**Flink 核心架构**|★★☆|L1: JobManager(协调器) + TaskManager(工作进程/Slot槽位) / Stream(无限数据流) vs Batch(有限数据集)；L2. DataStream API(底层流式API) / Table API & SQL(声明式上层API) / State(键控状态Keyed State/Operator State)；3. Exactly-Once语义(Checkpoint + TwoPhaseCommitSink)|Flink和Spark Streaming区别？Checkpoint对性能的影响？|FlowPulse实时统计可选Flink|
|A7-07|**窗口(Window)机制**|★★☆|L1: Time Window(滚动Tumbling/滑动Sliding/会话Session) vs Count Window(按条数触发)；L2. Event Time vs Processing Time vs Ingestion Time(三种时间语义)；3. Watermark(水位线)处理乱序数据(允许迟到数据的容忍度)|Watermark怎么设置？迟到数据怎么处理(side output)？|流程执行实时统计窗口|
|A7-08|**状态管理与Checkpoint**|★☆☆|L1. Keyed State(ValueState/ListState/MapState/AggregatingState/ReducingState) / Operator State(广播状态Broadcast State)；L2. Checkpoint(增量快照，基于Chandy-Lamport算法) vs SavePoint(手动触发的全量快照用于版本升级)；3. State Backend(HashMapStateMem内存级 / RocksDBStateBackend磁盘级，超大规模状态)|RocksDB State Backend性能如何？Checkpoint间隔多久合适？|有状态的流计算场景|
|A7-09|**CEP 复杂事件处理**|★☆☆|L1. Pattern API定义事件序列模式(开始→条件匹配→量词(*+/{}?)→结束)；L2. 典型场景(用户行为路径分析/欺诈检测/告警关联分析)；3. 超时模式(within(time))处理未完成的事件序列|CEP和规则引擎的区别？性能瓶颈在哪？|FlowPulse异常检测(连续失败N次触发告警)|
|A7-10|**Flink SQL 实战**|★☆☆|L1. CREATE TABLE定义Source/Sink(Connector格式) / INSERT INTO SELECT实时SQL查询；L2. Temporal Table Join(维表Join) / Upsert(更新插入) / Windowed Aggregation(窗口聚合)；3. CDC Change Data Capture(连接MySQL Binlog实时同步数据变更)|Flink SQL和Hive SQL的区别？CDC延迟怎么控制？|FlowPulse准实时报表可用Flink SQL|

## 15.3 Elasticsearch（6点）

|#|知识点|重要度|三层笔记建议|面试追问|FlowPulse 结合|
|---|---|---|---|---|---|
|A7-11|**ES 基本原理**|★★☆|L1: 分布式搜索和分析引擎(Index→Shard(主+副)→Segment→倒排Index)；L2. 近实时(NRT)：写入→Refresh Interval(默认1s)→可搜索；L3: RESTful API(JSON交互) + Query DSL(Bool/Term/Match/Range/Aggregation)|ES和MySQL区别？什么时候用ES不用MySQL？|**FlowPulse全文搜索(流程模板/历史记录搜索)用Elasticsearch**|
|A7-12|**倒排索引原理**|★★☆|L1: 正向索引(Document ID → Content) vs 倒排索引(Term → Posting List[Document ID, TF, Position])；L2. 分词Analyzer(Character Filter → Tokenizer → Token Filter：IK中文分词)；3. Posting List压缩(FOR(Frame of Reference) + Roaring Bitmaps)|倒引索引占多大空间？IK分词怎么自定义词典？|FlowPulse搜索分词配置|
|A7-13|**ES 写入流程**|★☆☆|L1. Client → Coordinator Node(路由到Primary Shard) → Primary写入 + Translog(防丢失) + Replica同步 → Ack确认；L2. Refresh(1s后可见) / Flush(Translog→Segment持久化) / Merge(段合并，后台异步)；3. Write Ahead Logging(类似MySQL Redo Log保证崩溃恢复)|Translog什么时候刷盘？Merge对性能的影响？|流程数据写入ES流程|
|A7-14|**ES 查询优化**|★☆☆|L1. Filter Context(不计算Score，缓存友好) vs Query Context(计算相关性评分TF-IDF/BM25)；L2. Deep Paging问题(from+size超过10000性能骤降)→解决方案(Search After/Scroll API)；3. Index Template(索引模板，预先定义Settings/Mappings/aliases)|from+size深翻页为什么慢？Filter Cache怎么用？|FlowPulse搜索API性能优化|
|A7-15|**ES 集群与分片策略**|★☆☆|L1. 集群角色(Master Eligible/Data Only/Coordinating Only) / 脑裂(split-brain prevention: minimum_master_nodes)；2. 分片策略(单分片<5GB数据 / 每个分片≤50GB / Shard数=节点数×1-3倍)；3. Routing(自定义路由到特定Shard提高查询效率)|分片过多有什么问题？怎么判断分片大小是否合理？|FlowPulse ES集群规划(3节点 15 Shard)|
|A7-16|**ES vs MySQL vs ClickHouse 选型**|★☆☆|L1: ES(全文检索/复杂搜索/日志分析) vs MySQL(事务型OLTP/强一致性) vs ClickHouse(OLAP列存聚合分析极快)；L2. 选型决策树(需要事务?→MySQL / 需要全文检索?→ES / 需要海量数据聚合?→ClickHouse)；3. 可以组合使用(MySQL主库→Canal同步→ES搜索+ClickHouse分析)|ClickHouse为什么聚合这么快？三者能互相替代吗？|**FlowPulse: MySQL(事务)+ES(搜索)+ClickHouse(报表分析) 组合架构**|

## 15.4 ClickHouse OLAP（4点）

|#|知识点|重要度|核心要点|FlowPulse 应用|
|---|---|---|---|---|
|A7-17|**ClickHouse 核心优势**|★☆☆|列式存储(只读需要的列) + 向量化执行(SIMD指令并行处理) + LSM Tree-like MergeTree引擎(高效写入)|聚合查询比MySQL快100-1000x|
|A7-18|**表引擎 MergeTree Family**|★☆☆|MergeTree(基础引擎，按主键排序存储) / ReplacingMergeTree(去重) / SummingMergeTree(预聚合) / ReplicatedMergeTree(副本) / Distributed(分布式查询)|FlowPulse报表用ReplicatedMergeTree|
|A7-19|**ClickHouse 性能优化**|★☆☆|索引(稀疏索引/data_skip_unused_shards) / 物化视图(Materialized View预聚合) /Projection(查询加速投影) / 准实时的物化视图|报表秒级响应|
|A7-20|**数据接入方案**|★☆☆|MySQL直接SELECT导入 / Kafka Connector实时消费 / Canal CDCBinlog同步 / Spark Batch批量导入|FlowPulse从MySQL同步到CH做报表|

## 15.5 其他大数据概念（2点）

|#|知识点|重要度|核心要点|
|---|---|---|---|
|A7-21|**数据湖(Lake House)**|★☆☆|Delta Lake/Iceberg/Hudi(表格式ACID事务支持) + 对象存储(S3/OSS MinIO) + Schema Evolution + 时间旅行(Time Travel)|
|A7-22|**Kafka + Flink + ClickHouse 典型架构**|★☆☆|业务DB → Binlog/CDC → Kafka(缓冲) → Flink(实时ETL/窗口聚合) → ClickHouse(OLAP分析) + Grafana(可视化)|

---

# 🟣 P2 加分 — A8: AI 工程化集成（~18 点）

## 16.1 LLM API 接入（6点）

|#|知识点|重要度|三层笔记建议|面试追问|FlowPulse 结合|
|---|---|---|---|---|---|
|A8-01|**主流 LLM API 对比**|★★☆|L1: GPT-4/GPT-4o(OpenAI，最成熟生态) / 通义千问Qwen(阿里，中文优秀) / 文心一言ERNIE(百度，国内易用) / Claude(Anthropic，长文本强) / DeepSeek(国产性价比高)；L2. 选型维度：成本/token、延迟、上下文长度、中文能力、API稳定性、数据隐私(私有部署)；3. 多模型策略(简单任务用小模型省成本/复杂推理用大模型保效果)|Token计费怎么算？API超时怎么处理？|FlowPulse智能助手功能接入通义千问|
|A8-02|**Prompt Engineering 基础**|★★☆|L1. Prompt结构(System Role + User Message + Few-Shot Examples + Output Format约束)；L2. 高级技巧：CoT(Chain-of-Thought，引导模型逐步思考) / ReAct(Reasoning+Acting交替) / Self-Consistency(多次采样投票)；3. Prompt版本化管理(和代码一样Git管理Prompt变体A/B测试效果)|Prompt太长会怎样？Few-Shot几个例子最佳？|FlowPulse智能审批建议的Prompt设计|
|A8-03|**Token 成本控制**|★☆☆|L1. Token = 输入Token + 输出Token(输出通常更贵) / 上下文窗口限制(GPT-4:128K/Qwen:32K+)；L2. 优化策略：压缩上下文(只保留关键信息)/缓存常见回答(Semantic Cache)/模型路由(简单问题用小模型)；3. 成本预估(日均请求数 × 平均Token × 单价 = 月费用)|Semantic Cache怎么做？模型路由的准确率？|FlowPulse AI功能的成本预算|
|A8-04|**LLM 可靠性设计**|★☆☆|L1. 超时重试(指数退避) / 降级服务(LLM不可用时返回规则结果) / 异步队列(请求入队异步调用不阻塞用户)；L2. 输出安全过滤(敏感内容审查/JSON格式校验/Prompt Injection防护)；3. 监控指标(响应时间/Token消耗/错误率/用户满意度CSAT)|LLM产生幻觉怎么办？Prompt Injection怎么防？|FlowPulse AI模块的高可用设计|
|A8-05|**Function Calling / Tool Use**|★★☆|L1. LLM可以调用外部函数/API(OpenAI Function Calling/通义ToolChoice)；L2. 流程：User提问 → LLM判断需要调用哪个Function → 执行Function → 将结果反馈给LLM → LLM组织自然语言回复；3. 典型应用(查询数据库/调用搜索引擎/执行代码解释)|Function Calling和RAG的区别？工具调用的安全性？|**FlowPulse AI助手：查询流程状态/统计数据通过Function Calling**|
|A8-06|**RAG (Retrieval-Augmented Generation)**|★★★|L1. RAG架构：Query → Embedding(向量化) → Vector DB相似度检索(Top-K相关文档) → 将检索到的文档拼接到Prompt中 → LLM基于事实生成回答；L2. 解决LLM的局限(知识过时/私有数据不可见/幻觉问题)；3. RAG Pipeline优化：Chunk Strategy(文档切片策略) / Re-ranking(重排序提升精度) / Hybrid Search(向量+关键词混合搜索)|RAG和Fine-tuning怎么选？Embedding模型怎么选？Chunk大小怎么定？|**FlowPulse智能问答：基于RAG检索流程文档+操作手册回答用户问题**|

## 16.2 向量数据库（4点）

|#|知识点|重要度|核心要点|FlowPulse 应用|
|---|---|---|---|---|
|A8-07|**向量数据库选型**|★☆☆|Milvus(开源分布式/十亿级向量/多种索引HNSW/IVF_PQ) / Qdrant(轻量Rust编写/过滤能力强) / Weaviate(自带向量化模块) / pgvector(PostgreSQL插件，简单场景够用) / Elasticsearch 8.x(内置kNN向量搜索)|**FlowPulse选Milvus(云托管Zilliz Cloud)或pgvector(起步阶段)**|
|A8-08|**向量索引算法**|★☆☆|FLAT(暴力扫描精确但慢) / IVF(聚类倒排，速度精度平衡) / HNSW(层级导航小世界图，高召回高性能) / PQ(乘积量化压缩，省内存)|Milvus默认HNSW，百万级以下FLAT也够快|
|A8-09|**Embedding 模型**|★☆☆|OpenAI text-embedding-3(多语言通用) / BGE系列(智源中文优秀M3B-large) / M3E(中文向量化) / Cohere embed v3；维度(1536/OpenAI / 1024/BGE / 768/M3E)|FlowPulse用BGE-M3或text-embedding-3-small(成本低)|
|A8-10|**RAG 评估方法**|★☆☆|RAGAS框架(Precision/Recall/Faithfulness/Answer Relevances 四维度评估) / 人工评估(专家打分) / A/B Test(两个版本对比用户偏好)|RAG效果怎么衡量？上线前怎么验收？|

## 16.3 Agent 与工作流（4点）

|#|知识点|重要度|核心要点|FlowPulse 应用|
|---|---|---|---|---|
|A8-11|**AI Agent 概念**|★☆☆|Agent = LLM + Memory(短期对话记忆+长期知识库) + Tools(调用外部API的能力) + Planning(任务分解与执行计划)；自主Agent能根据目标自行决定调用哪些工具并迭代直到完成任务|FlowPulse未来：AI Agent自动编排工作流|
|A8-12|**LangChain / LangGraph**|★☆☆|LangChain(LLM应用开发框架：Chain链接多步骤/Prompt Template/Memory/Agent/Tools) / LangGraph(状态机驱动的Agent工作流，支持循环和条件分支)|FlowPulse AI功能的技术选型参考|
|A8-13|**Dify / MaxKB 开源平台**|★☆☆|Dify(LLMOps平台：可视化编排Prompt/Workflow/RAG知识库) / MaxKB(基于Dify的知识库问答系统)；快速搭建RAG应用而不用从零开发|FlowPulse可考虑用Dify构建智能运维助手|
|A8-14|**AI 编码助手 Copilot/Cursor**|★☆☆|GitHub Copilot(IDE内代码补全/生成/解释) / Cursor(更强AI能力的IDE，支持整个代码库理解)；开发者提效工具|日常开发效率工具|

## 16.4 AI 工程化落地注意事项（4点）

|#|知识点|重要度|核心要点|FlowPulse 应用|
|---|---|---|---|---|
|A8-15|**AI 功能灰度发布**|★☆☆|Feature Flag控制AI功能开关 / A/B Test对比AI建议vs无AI的用户转化率 / 渐进放量(1%→5%→20%→全量)|FlowPulse AI功能上线策略|
|A8-16|**数据 privacy 与合规**|★☆☆|用户数据脱敏后再传给LLM API / 不发送敏感信息(密码/Token/身份证) / 私有化部署(如有合规需求用Qwen-72B本地部署) / 日志不记录完整Prompt|FlowPulse数据安全红线|
|A8-17|**AI 能力评估体系**|★☆☆|准确率(Precision@1) / 有用性(Human Rating) / 延迟(P95响应时间) / 成本($/千次请求) / 覆盖率(多少问题能回答)|AI功能上线后的持续监控|
|A8-18|**MLOps 基础概念**|★☆☆|MLOps(机器学习运维)：模型训练→版本管理→部署→监控→再训练闭环；MLflow(实验追踪/模型注册) / BentoML(模型 serving)|了解即可，FlowPulse暂不需要完整MLOps|

深度思考

---

# 跨方向专项补充 — X1: 项目经验 STAR 模板库（~20 点）

## 17.1 STAR 方法论（4点）

|#|知识点|核心要点|
|---|---|---|
|X1-01|**STAR 结构**|**S**ituation(背景：什么项目/什么阶段/团队规模) → **T**ask(任务：你负责什么/面临什么挑战) → **A**ction(行动：你具体做了什么/技术决策/实现方案) → **R**esult(结果：量化数据/业务价值/技术收益)。面试官70%时间在深挖A和R|
|X1-02|**STAR 常见错误**|①S太长(超过1分钟还没到T) ②A太泛(用"我们"而不是"我") ③R没有数字("效果很好"→改为"P99延迟从800ms降到120ms") ④T不够有挑战性(任务太平淡无法体现能力)|
|X1-03|**每个模块准备2-3个STAR**|FlowPulse项目按模块准备：工作流引擎 / API网关与安全 / 分布式事务 / 可观测性 / 部署运维。每个模块一个完整STAR + 一个简短版本(3分钟版)|
|X1-04|**追问应对(SAY框架)**|当面试官深挖时用 **S**ummarize(先一句话总结) → **A**nalyze(拆解关键决策点和为什么这么做) → **Y**ield(引出量化结果)。保持结构化回答|

## 17.2 FlowPulse STAR 模板库（16个核心场景）

|#|场景|S背景|T挑战|A行动(你的关键技术决策)|R结果(量化)|追问预测|
|---|---|---|---|---|---|---|
|X1-05|**工作流引擎设计**|FlowPulse需支持复杂审批流程(会签/或签/条件分支/并行/子流程)|Activiti太重且难以定制，需要轻量可扩展的引擎|设计了基于DAG的工作流引擎：Node(策略模式封装节点类型)+Edge(条件路由)+ProcessInstance(状态机驱动)；使用Template Method定义执行生命周期；Chain of Responsibility处理审批链；状态变更通过EventBus解耦下游|引擎核心5000行代码，支持20+种节点类型；单个流程实例平均执行耗时<200ms；支持1000+并发流程运行|DAG怎么处理环路？状态机选什么模式？|
|X1-06|**分布式事务一致性**|流程审批通过后需同步更新：流程状态→发送通知→更新统计数据→记录审计日志，任一步失败导致不一致|多服务间强一致性难保证，传统2PC性能差且有阻塞风险|采用Seata AT模式 + TCC补偿混合方案：核心状态变更走AT(自动补偿)，通知类走TCC异步最终一致性；设计了Outbox表保证本地事务和消息原子性；幂等消费防止重复处理|数据一致性达到99.99%(月均不一致<5次)；事务引入的额外延迟<50ms；消息零丢失|AT模式的回滚机制？TCC空回滚/悬挂问题？|
|X1-07|**API网关限流熔断**|大促期间流量突增10倍，部分非核心接口拖慢整个系统|传统单机限流无法应对分布式流量，且缺乏细粒度控制|基于Spring Cloud Gateway自定义过滤器：Sentinel热点参数限流(精确到用户维度) + 令牌桶算法平滑突发流量 + 自适应熔断(慢调用比例>50%触发)；设计了多级限流(Nginx→Gateway→Service三级)|成功抵御QPS峰值50000(日常5000)；服务可用性从99.9%提升到99.99%；误杀率<0.01%|令牌桶参数怎么调的？冷启动怎么解决？|
|X1-08|**缓存体系优化**|流程模板列表接口QPS高但DB查询慢(关联多表)，响应时间P99达800ms|缓存命中率仅30%，存在严重的缓存穿透和雪崩风险|设计了三级缓存架构：L1 Caffeine本地缓存(TTL=30s，字典数据) + L2 Redis Cluster分布式缓存(TTL=5min，业务数据) + L3 布隆过滤器防穿透；实现了Cache-Aside模式+延迟双删保证一致性；Redis Key按租户隔离前缀|接口P99从800ms降到15ms(提升53倍)；DB QPS下降90%；缓存命中率从30%提升到95%|延迟双删的时间间隔？Caffeine容量怎么定？|
|X1-09|**数据库分表优化**|单张flow_instance_log表超2000万行，查询性能急剧恶化，备份耗时超4小时|大表导致全表扫描、锁竞争严重、DML操作变慢|按tenant_id(32个Hash槽)水平分片 + 按create_time按月分区(Range Partition)；MyBatis-Plus分片插件透明路由；跨片查询使用全局表(小表冗余) + 广播表；历史数据归档到ClickHouse做分析|复杂查询从5s降到200ms；备份时间从4h降到20min；写入TPS提升3倍|分片键怎么选的？跨片JOIN怎么处理？|
|X1-10|**全链路追踪落地**|微服务调用链路长(6-8个服务)，问题定位困难，一次线上排查平均耗时2h|各服务日志孤立，无法串联一个请求的完整路径|集成OpenTelemetry Java Agent(零侵入)统一埋点；设计TraceId生成规则(请求入口生成→Header传递→MDC注入日志→MQ消息携带→DB字段存储)；Jaeger部署+Grafana联动(点击指标直接跳转Trace)；采样策略：默认10%+Error必采+P99>1s必采|问题定位时间从2h缩短到10min(效率提升12x)；全链路覆盖率100%；Trace数据存储成本控制在预算内|TraceId在异步线程池怎么传递？采样率怎么定的？|
|X1-11|**JVM GC调优**|上线后Full GC频繁(每30min一次)，每次STW长达8秒，用户感知明显卡顿|堆内存设置不合理(年轻代太小导致频繁YGC+对象过早晋升老年代)|分析GC日志发现：①Young区过小(512M)②大对象未优化(流程定义JSON>2MB直接进Old)③System.gc()被依赖库调用；调整：Xmx8g -Xmn3g G1GC MaxGCPauseMillis=200 + PretenureSizeThreshold=1M + ExplicitGCInvokesConcurrent|Full GC从30min/次降到每天<1次(降低99%)；STW从8s降到<200ms(P99)；吞吐量提升40%|怎么判断是Young区太小还是Old区太小？G1 IHOP怎么调的？|
|X1-12|**消息队列可靠性**|支付回调消息丢失导致订单状态不正确，每月约3笔资金对账异常|RocketMQ默认配置下消息可能丢失(异步刷盘+异步复制)|生产环境改为SYNC_FLUSH(刷盘) + SYNC_MASTER(同步复制)；实现了Producer同步发送+ACK=-1重试3次；Consumer手动ACK + 幂等消费(Redis去重表)；事务消息保证本地操作和消息发送原子性；DLQ监控告警(积压>100触发P1)|消息丢失率从~0.03%降到0(6个月零丢失)；端到端可靠性的额外延迟增加<20ms|同步刷盘对吞吐的影响怎么评估？事务消息回查频率？|
|X1-13|**容器化与K8s部署**|手动部署一台服务器要2h，环境不一致导致的"在我机器上能跑"问题频发|部署流程手工化、无标准化、无回滚能力|Dockerfile标准化(多阶段构建镜像80MB vs 原始400MB)；Helm Charts管理所有K8s资源(12个微服务+中间件)；GitLab CI流水线自动化(代码提交→测试→构建→部署Dev→冒烟→ Prod灰度)；K8s HPA根据CPU>70%自动扩容|部署时间从2h缩短到10min(自动化)；环境问题减少95%(容器标准化)；大促期间自动扩容响应<2min|Helm Values怎么管理多环境？HPA扩容冷却时间？|
|X1-14|**认证授权体系**|原系统硬编码权限，新租户接入需改代码重新发布，平均交付周期2周|RBAC权限模型僵化，无法支持多租户SaaS化|设计了动态RBAC三级权限(菜单权限+按钮权限+数据权限) + ABAC属性策略(租户级数据隔离)；JWT双Token机制(Access Token 30min + Refresh Token 7天)；Spring Security过滤器链定制(8个Filter)；Nacos推送权限变更实时生效(无需重启)|新租户接入从2周缩短到30min(配置化)；权限粒度从页面级细化到API级；Token刷新成功率99.99%|Refresh Token轮换策略？权限变更的推送延迟？|
|X1-15|**定时任务调度优化**|定时任务散落在各服务的@Scheduled中，无法集中管控和故障转移|任务分布不均衡(某节点负载过高)、单点故障(节点挂掉任务丢失)、无法监控|统一迁移到XXL-Job分布式任务调度中心：任务分片(大数据量任务并行处理)、故障转移(节点挂掉自动重试)、路由策略(第一个/最后一个/轮询/一致哈希)；任务执行器Spring Boot独立部署；Dashboard可视化监控(成功/失败/运行时长/趋势)|任务按时执行率从95%提升到99.99%+；故障恢复时间从人工介入30min→自动<1min；任务数量从分散到集中管控(50+任务)|XXL-Job分片策略怎么选？Glue模式vs Bean模式？|
|X1-16|**搜索功能实现**|用户反馈流程搜索太慢(模糊搜+条件过滤+排序，SQL Like '%xxx%' 全表扫)|MySQL做全文检索性能差，复杂查询3-5s才能返回|引入Elasticsearch：流程定义/实例/日志建ES索引(IK中文分词+自定义同义词词典)；Canal监听MySQL Binlog实时同步到ES(延迟<3s)；Search After替代from+size深翻页；ES Cluster 3主3从(15 Shard)|搜索响应时间从3-5s降到<200ms；支持全文检索/高亮/智能推荐；搜索QPS支撑1000+(MySQL只能50)|ES和MySQL数据一致性怎么保？索引怎么设计的？|
|X1-17|**安全加固**|安全扫描发现多个高危漏洞(XSS/CSRF/SQL注入风险)|快速开发期安全意识不足，输入校验不严格|XSS防护(前端转义+后端HttpOnly Cookie + CSP Header) / SQL注入(MyBatis-Plus Parameterized Query强制规范 + SQL防火墙) / CSRF(Token双重提交验证) / 接口签名(HMAC-SHA256防篡改+Timestamp防重放) / 敏感数据脱敏(手机号身份证号正则替换)|安全漏洞数从High 23个→0个(一季度内清零)；通过等保二级测评；安全扫描自动化集成CI Pipeline|CSP策略怎么配的？签名密钥怎么轮换？|
|X1-18|**日志体系搭建**|日志散落在各服务器文件中，排查问题需逐台SSH查看，格式不统一|无集中式日志，故障排查效率极低，日志保留期限不合规(审计要求180天)|架构ELK：Filebeat(DaemonSet采集) → Kafka(缓冲削峰) → Logstash(解析JSON+Grok) → ES(存储) → Kibana(可视化工)；日志格式统一JSON(trace_id/tenant_id/span_id/level/logger/message/stack_trace)；Loki作为轻量备选(开发环境)；日志分级(ERROR全量/INFO采样/DEBUG关闭)|故障排查效率提升10x(集中搜索vs SSH翻日志)；日志存储成本优化(压缩+热温冷分层)；合规满足180天留存要求|Kafka Buffer多大？日志量怎么预估？|
|X1-19|**监控告警体系**|曾出现DB连接池耗尽导致服务不可用，但运维2h后才收到用户投诉|缺乏有效监控，被动响应而非主动发现|Prometheus + Grafana 四层监控(基础设施/K8s/应用层/业务层)：Node Exporter(cAdvisor) + Blackbox Exporter(拨测) + JMX Exporter(JVM) + Micrometer(Spring Boot Metrics) + 自定义业务Exporter；Alertmanager四等级告警(P0电话+钉钉/P1-P2钉钉+邮件/P3企业微信日报)；Runbook标准操作手册(每种告警对应排查步骤)|平均故障发现时间(MTTD)从2h缩短到<3min；误报率从30%降到<5%；告警风暴次数降为0(分组+抑制)|告警阈值怎么定的？Runbook怎么维护？|
|X1-20|**代码质量与工程效能**|Code Review流于形式，Bug逃逸到生产环境占比15%，技术债务持续累积|缺乏自动化质量门禁，Code Review无标准Checklist|SonarQube集成CI(Quality Gate：Blocker=0 Critical=0 Coverage>80%否则阻断部署)；GitLab MR Template强制填写What/Why/Test Plan；Code Review Checklist(安全性/性能/可测试性/命名规范)；每周五技术债务1h专项治理(轮流认领)|Bug逃逸率从15%降到<2%；代码覆盖率从45%提升到82%；技术债天数从120天降到35天|Quality Gate太严影响发版怎么办？Code Review怎么避免形式主义？|

---

# 跨方向专项补充 — X2: System Design 面试题库速查（~18 点）

## 18.1 O(1) 设计题（5点）

|#|题目|核心设计要点|时间复杂度|FlowPulse 关联|
|---|---|---|---|---|
|X2-01|**LRU Cache**|HashMap + 双向LinkedList(O(1)查找+O(1)移动) / get()访问移到头部 / put()删除尾部+插入头部|O(1)|Redis缓存淘汰策略理解加深|
|X2-02|**LFU Cache**|HashMap + MinHeap(频率最小堆) + Counter计数器 / 或用HashMap + DoublyLinkedList + Counter(类似LRU但按频率排序)|O(log n)堆/O(1)双向链|缓存淘汰策略对比|
|X2-03|**Rate Limiter**|令牌桶(固定速率放token/取token) / 漏桶(固定速率出水) / 滑动窗口(时间窗口内计数) / 固定窗口(时间片重置)|O(1)|Sentinel限流底层原理|
|X2-04|**Consistent Hashing**|Hash Ring环(0~2^32-1) + 虚拟节点(解决数据倾斜) + 顺时针查找节点|O(n)节点数/O(log n)跳表优化|一致性哈希在Redis Cluster/Dubbo中的应用|
|X2-05|**URL Shortener**|Base62编码(ID→短码) / Snowflake ID / MD5截断(前6位+冲突处理) / Bloom Filter预判|O(1)|FlowPulse外部分享链接功能参考|

## 18.2 高并发设计题（5点）

|#|题目|核心设计要点|FlowPulse 关联|
|---|---|---|---|
|X2-06|**秒杀系统**|CDN静态化 + Nginx限流 + Redis预扣库存 + MQ异步下单 + DB唯一约束兜底|已在B7详细展开|
|X2-07|**微博Feed**|Write(推)模式 + Read(拉)模式混合 / Redis ZSet inbox / BigKey拆分|已在B7详细展开|
|X2-08|**聊天室(IM)**|WebSocket长连接 + Server心跳 + 消息ACK + 离线消息存储(Redis List) + 消息ID有序|FlowPulse站内消息通知|
|X2-09|**实时排行榜**|Redis ZSet(score=分数, member=用户ID) + 定时刷新 + TopK(ZREVRANGE 0 N-1)|FlowPulse流程执行效率排行|
|X2-10|**附近的人(LBS)**|GeoHash编码(经纬度→字符串前缀) / Redis GEORADIUS(Geo命令) / 六边形网格(S2)|了解即可|

## 18.3 分布式系统设计题（4点）

|#|题目|核心设计要点|FlowPulse 关联|
|---|---|---|---|
|X2-11|**分布式唯一ID**|Snowflake(41bit时间戳+10bit机器ID+12bit序列) / 号段模式(Leaf Segment) / UUID(不用！无序) / 数据库步长(auto_inc step=N)|FlowPulse已采用Snowflake|
|X2-12|**分布式Session**|Spring Session + Redis(SESSION存Redis) / JWT无状态Token(Cookie存Token) / Sticky Session(同一用户路由同一节点)|FlowPulse采用JWT方案|
|X2-13|**分布式锁**|Redis SET NX EX(RedLock多实例) / ZooKeeper临时顺序节点 / DB唯一索引(乐观锁)|FlowPulse使用Redisson分布式锁|
|X2-14|**分布式事务**|2PC/XA(强一致低性能) / TCC(Try-Confirm-Cancel) / Saga(长事务编排) / 本地消息表(Outbox) / Seata(AT/TCC/Saga)|FlowPulse采用Seata AT + TCC|

## 18.4 数据库设计题（4点）

|#|题目|核心设计要点|FlowPulse 关联|
|---|---|---|---|
|X2-15|**短链接设计**|ID生成(Base62) + 302/301重定向 + Redis缓存 + Click统计(BigTable/ClickHouse)|已在B7详细展开|
|X2-16|**分库分表**|垂直拆(按业务域) / 水平拆(Hash/Range/Geo) / 中间件(ShardingSphere) / 全局ID / 跨片查询(广播表/BindingTable)|FlowPulse按租户分表|
|X2-17|**海量数据分页**|游标分页(last_id > last_seen_id) / Covering Index覆盖索引 / ES搜索代替深翻页|FlowPulse历史记录翻页优化|
|X2-18|**数据库读写分离**|主库写/从库读(Master-Slave复制) / 中间件代理(ShardingSphere-Proxy/Mycat) / 延迟解决方案(强制主库读/Cache标记/半同步)|FlowPulse读写分离架构|

---

# 跨方向专项补充 — X3: 简历优化与亮点包装（~15 点）

## 19.1 简历黄金法则

|#|法则|说明|FlowPulse 应用示例|
|---|---|---|---|
|X3-01|**数字说话**|每个成就必须有量化数据。不要说"提升了性能"，要说"P99延迟从800ms降到120ms(提升6.7倍)"|所有STAR中的Result都带数字|
|X3-02|**动词开头**|每条经历以强动作动词开头：设计/实现/优化/重构/主导/推动/搭建/迁移|"主导设计了分布式工作流引擎..."|
|X3-03|**STAR隐式嵌入**|简历每行 ≈ STAR的精简版。面试时再展开完整STAR|简历上写一行，面试能讲5分钟|
|X3-04|**技术栈标注熟练度**|熟练(★)、熟悉(☆)、了解(○)三级标注。不要写"精通"(除非你真的写了该语言的编译器)|Java ★★★ / Spring Cloud ★★☆ / K8s ★★☆|
|X3-05|**项目描述公式**|**项目名(角色) + 技术栈 + 一句话概括做了什么 + 核心难点 + 量化成果**|"FlowPulse(核心架构师) · Spring Cloud Alibaba · 设计并实现分布式工作流引擎，解决复杂审批流程编排难题 · QPS承载5000+ · 响应时间P99<200ms"|

## 19.2 简历各模块优化要点（10点）

|#|模块|优化要点|示例|
|---|---|---|---|
|X3-06|**个人信息**|姓名/手机/邮箱/GitHub(如有好项目)/博客(如有技术文章)/学历。**不要**写年龄/性别/照片|GitHub: github.com/zhang-flowpulse (FlowPulse开源项目)|
|X3-07|**专业技能**|分类排列：语言/框架/中间件/数据库/工具/云原生。每项标注熟练度+一句话亮点|"Spring Cloud Alibaba(★)：主导Nacos/Gateway/Sentinel/Seata全家集成的生产级落地"|
|X3-08|**工作经历**|倒序(最近的在前)。每段包含：公司/职位/时间 + **3-5个Bullet Points**(每个都是迷你STAR)|"· 设计三级缓存架构(Caffeine+Redis+BloomFilter)，将核心接口P99从800ms降至15ms(提升53倍)"|
|X3-09|**项目经验(最重要)**|选**最有代表性的3-4个项目**。每个项目：名称/角色/技术栈 + 3-5个Bullet + 链接(GitHub地址/在线Demo)|FlowPulse项目占最大篇幅(因为这是你最好的证明)|
|X3-10|**开源贡献**|有就写！PR数量/Star数/Fork数。即使小的Bug Fix也体现社区参与|提交PR到Spring Cloud Alibaba/Nacos社区...|
|X3-11|**奖项/证书**|只写含金量高的(技术竞赛/专利/软著)。ACP/CKA等认证可以写|软著：FlowPulse V1.0 (申请中) / CKA认证(备考中)|
|X3-12|**自我评价**|**可选**。如果写，要具体而非空洞。如："3年后端开发经验，专注于微服务架构和高并发系统设计，目标成为架构师"|不要写"吃苦耐劳/学习能力强"这类套话|
|X3-13|**常见减分项**|①错别字(致命！) ②联系方式错误 ③时间线有空缺(解释空窗期) ④技术栈罗列无重点(不要列HTML/CSS如果你面后端)⑤项目描述像招聘JD(不要写"负责xxx的开发"，要说"怎么做的+结果如何")|简历检查清单：请别人帮你Review一遍|
|X3-14|**针对不同公司调整**|阿里系(强调中间件深度/高并发/源码) / 字节(强调基础算法/系统设计/快速学习能力) / 网易(强调工程质量/稳定性/工程文化) / 滴滴(强调出行领域业务理解+高可用)|准备3个版本的简历(不同公司突出不同重点)|
|X3-15|**简历投递渠道**|内推(最高效，直接给HR) > Boss直聘(沟通方便) > 招聘网站(智联/猎聘) > 校招官网|杭州大厂优先找内推(校友/前同事/技术社群)|

---

# 跨方向专项补充 — X4: 开源框架源码阅读清单（~18 点）

## 20.1 Spring 源码阅读路径（5点）

|#|模块|核心类/方法|阅读顺序建议|重要度|
|---|---|---|---|---|
|X4-01|**IOC容器启动**|`AbstractApplicationContext.refresh()` → `obtainFreshBeanFactory()` → `invokeBeanFactoryPostProcessors()` → `registerBeanPostProcessors()` → `finishBeanFactoryInitialization()`|先看整体refresh()流程(15个步骤)，再深入每个步骤|★★★|
|X4-02|**Bean创建流程**|`AbstractAutowireCapableBeanFactory.createBean()` → `doCreateBean()` → `createBeanInstance()` → `populateBean()` → `initializeBean()`|重点看属性填充(Autowired注解)和初始化(BeanPostProcessor)|★★★|
|X4-03|**AOP代理创建**|`AbstractAutoProxyCreator.wrapIfNecessary()` → `createProxy()` → `JdkDynamicAopProxy.getProxy()` / `CglibAopProxy.getProxy()`|对比JDK和CGLIB两种代理的创建过程|★★☆|
|X4-04|**事务管理**|`TransactionInterceptor.invoke()` → `createTransactionIfNecessary()` (获取事务) → `commitTransactionAfterReturning()` (提交) → `completeTransactionAfterThrowing()` (回滚)|看@Transactional注解是怎么被AOP拦截处理的|★★★|
|X4-05|**循环依赖**|`DefaultSingletonBeanRegistry.getSingleton()` (三级缓存) → `getSingleton(beanName, true)` (允许早期引用) → `addSingletonFactory()` (三级缓存放入工厂) → `getEarlyBeanReference()` (解决AOP代理)|必须能手画循环依赖解决的完整流程图|★★★|

## 20.2 MyBatis-Plus 源码（3点）

|#|核心类/方法|阅读重点|重要度|
|---|---|---|---|
|X4-06|**Mapper代理创建**|`SqlSession.getMapper()` → `MapperProxyFactory.newInstance()` → `MapperProxy.invoke()` → `MappedStatement` 执行|Mapper接口怎么变成可执行对象的？|
|X4-07|**SQL拼接(Wrapper)**|`AbstractWrapper` → `QueryWrapper/LambdaQueryWrapper` → `segmentMerge()` → SQL片段组装|Wrapper条件怎么变成WHERE子句的？|
|X4-08|**分页插件**|`PaginationInnerInterceptor.beforeQuery()` → `getPage()` → `addLimit()` 追加LIMIT SQL|MyBatis-Plus分页怎么做到物理分页的？|

## 20.3 Dubbo 源码（3点）

|#|核心类/方法|阅读重点|important度|
|---|---|---|---|
|X4-09|**服务暴露**|`ServiceBean.export()` → `Protocol.export()` → `openServer()` (Netty Server启动) + `register()` (注册到Nacos)|服务暴露的完整链路(从@Bean到网络端口监听)|
|X4-10|**服务引用(消费者)**|`ReferenceBean.getObject()` → `Protocol.refer()` → `Invoker` 创建 → 动态代理(ProxyFactory)|消费者怎么拿到远程服务代理对象的？|
|X4-11|**Filter链(集群容错)**|`ClusterInvoker.invoke()` → `LoadBalance.select()` (负载均衡) → `filterList` (Mock/Failover/Failsafe/ Forking)|Dubbo的十种集群容错策略的实现差异|

## 20.4 Sentinel 源码（2点）

|#|核心类/方法|阅读重点|重要度|
|---|---|---|---|
|X4-12|**限流核心**|`EntryUtil.entryWithResourceHandler()` → `ContextUtil.enter()` → `SlotChain` ProcessorSlot链 (NodeSelectorSlot → ClusterBuilderSlot → StatisticSlot → AuthoritySlot → SystemSlot → FlowSlot → DegradeSlot)|理解ProcessorSlot责任链的设计模式|
|X4-13|**滑动窗口统计**|`LeapArray` → `OccupiableBucket` (时间窗口桶) → `WindowWrap` (窗口包装) → `ArrayMetric` (QPS计算)|Sentinel怎么实现精确到毫秒的滑动窗口统计？|

## 20.5 Nacos 源码（2点）

|#|核心类/方法|阅读重点|重要度|
|---|---|---|---|
|X4-14|**服务注册**|`NamingClientProxy.registerInstance()` → HTTP POST `/nacos/v1/ns/instance` → 服务端接收(`InstanceController.register()`) → 存入ConcurrentHashMap|Nacos AP架构的服务注册原理|
|X4-15|**配置中心长轮询**|`LongPollingRunnable.run()` → 客户端发起30s长轮询请求 → 服务端hold住请求 → 配置变更时立即返回|Nacos配置推送的Long Polling实现原理(比RocketMQ Push更省资源)|

## 20.6 RocketMQ 源码（2点）

|#|核心类/方法|阅读重点|重要度|
|---|---|---|---|
|X4-16|**消息发送**|`DefaultMQProducerImpl.sendKernelImpl()` → 选择MessageQueue → 组装SendMessageRequestHeader → Netty Client发送 → Broker接收(`SendMessageProcessor`) → 写CommitLog|Producer到Broker的消息发送全链路|
|X4-17|**消息消费**|`MQClientInstance.pullMessage()` → PullAPIWrapper.processPullResult() → PullMessageService处理 → ConsumeMessageConcurrentlyService/OrderlyService消费|PushConsumer的Rebalance+Pull+消费回调全过程|

## 20.7 源码阅读方法论（1点）

| # | 方法论 | 说明 | | X4-18 | **源码阅读四步法** | **Step1**: 带着问题阅读(不要从头读到尾，而是"XXX功能怎么实现的？"定向切入) → **Step2**: Debug跑起来(IntelliJ Remote Debug打断点，看运行时变量和调用栈) → **Step3**: 画图总结(每读完一个模块画调用时序图/类图) → **Step4**: 输出笔记(写成三层笔记存入知识库，否则一周就忘) |

---

# 跨方向专项补充 — X5: 杭州大厂针对性准备（~19 点）

## 21.1 目标公司画像（6点）

|#|公司|技术特点|面试风格|高频考点|薪资范围(P6/P7)|FlowPulse 匹配度|
|---|---|---|---|---|---|---|
|X5-01|**阿里巴巴(淘宝/天猫/蚂蚁/阿里云)**|自研中间件极强(Dubbo/RocketMQ/Nacos/Sentinel/Seata全部出自阿里)|八股文+系统设计+价值观考察|JVM/并发/中间件源码/分布式事务/高可用设计/双11场景题|P6 25-35w / P7 35-50w|FlowPulse完全匹配(技术栈就是阿里系)|
|X5-02|**字节跳动(抖音/TikTok/飞书)**|基础至上(Go为主但也招Java)/算法要求高/系统设计权重极大|算法题必考(LC Medium/Hard) + 系统设计(开放题无标准答案看思路)|数据结构/算法手撕/系统设计/Open Source项目讨论|2-1 30-45w / 3-1 45-65w|需补强算法能力|
|X5-03|**网易(网易云音乐/网易互娱/有道)**|工程文化浓厚/重视代码质量和稳定性/相对WLB|八股文扎实+项目深挖+工程实践|Spring全家桶/JUC/MySQL优化/Redis实战/JVM调优|P6 22-32w / P7 32-45w|FlowPulse高度匹配|
|X5-04|**滴滴出行**|出行业务场景复杂(派单/计费/地图/安全)/高并发实时性要求|业务场景+技术深度并重|高并发系统设计/地理空间(LBS)/实时计算/消息队列/分布式调度|D6 25-35w / D7 35-50w|FlowPulse工作流引擎可类比派单系统|
|X5-05|**蚂蚁集团(支付宝/蚂蚁财富)**|金融级别要求(一致性/安全性/合规性极高)/SOFAStack生态|极其严格的八股文+金融场景题+安全合规|分布式事务(Seata源码级)/安全架构/SOAFAStack组件/资金链路设计|P6 28-38w / P7 42-60w|FlowPulse需加强安全和金融级一致性表述|
|X5-06|**其他杭州选项**|**华为杭研所**(通信背景/稳定但加班多) / **海康威视**(安防/AIoT/硬件结合) / **有赞**(电商SaaS/PHP转Go) / **涂鸦智能**(IoT平台/全球化)|视公司而定|—|—|根据个人偏好选择|

## 21.2 各公司差异化准备（5点）

|#|维度|阿里系|字节|网易|滴滴|
|---|---|---|---|---|---|
|X5-07|**必须深入的中间件**|Dubbo/RocketMQ/Nacos/Sentinel(源码级)|不强制(但要有理解)|Spring全家桶+MyBatis|MQ+分布式调度|
|X5-08|**算法要求**|LeetCode Hot100(中等难度)|LeetCode Hot100+Hard高频(至少能做Medium)|LeetCode Hot100(简单-medium够用)|LeetCode Hot100(中等)|
|X5-09|**系统设计风格**|画出架构图+Trade-off分析(为什么选A不选B)|Open-ended(给出需求→自由发挥→追问极限场景)|工程落地导向(怎么做测试/怎么运维/怎么监控)|业务场景驱动(结合出行/地图/计费)|
|X5-10|**项目深挖风格**|"你这个方案的缺点是什么？""如果QPS翻10倍怎么办？"|"这个项目最大的技术挑战？""如果是你来 redesign？"|"代码质量怎么保证？""怎么做的Code Review？"|"怎么保证一致性？""怎么做的降级？"|
|X5-11|**加分项**|开源贡献(给Apache项目提PR)/技术博客/CKA认证|GitHub Star项目/竞赛 ACM-ICPC/专利|技术分享/内部培训讲师/开源项目|出行领域业务理解/实时系统经验|

## 21.3 面试流程与策略（4点）

|#|阶段|内容|策略|
|---|---|---|---|
|X5-12|**一面(技术基础 45-60min)**|Java基础/JVM/并发编程/数据库/Redis/Spring|八股文要扎实但不能背书，要能说出"我在项目中遇到过这个问题，当时是这样解决的..."|
|X5-13|**二面(项目深挖 45-60min)**|挑一个最复杂的项目深挖到底|用STAR框架主动引导面试官。"让我介绍下FlowPulse项目，这是我们团队做的..."|
|X5-14|**三面(系统设计/架构 45-60min)**|给一个开放性问题(如"设计一个分布式任务调度平台")|用Design Methodology：Clarify Requirements → Estimate → High-Level Design → Deep Dive → Trade-off → Limitations → Follow-up|
|X5-15|**四面/HR面(价值观+薪酬 30min)**|为什么离职/职业规划/期望薪资/团队协作|薪资谈判技巧：先了解对方薪资结构(Base+股票+补贴) → 给出区间而非底价 → 强调你能带来的价值|

## 21.4 薪资谈判策略（4点）

|#|要点|说明|
|---|---|---|
|X5-16|**杭州Java后端薪资基准(2026)**|P6/中级: 22-30w · P7/高级: 30-45w · P8/专家: 45-65w · P9/资深: 65-100w+. 你的目标30-40w = P7级别|
|X5-17|**薪资组成**|总包 = 月薪×12~16 + 年终奖(2-6个月) + 股票/期权(4年归属) + 补贴(餐补/交通/租房) + 公积金(12%全额)。问清楚**总包**不只是月薪|
|X5-18|**谈判时机**|拿到Offer后再谈(不要在面试过程中谈薪) → 先让对方给出数字 → 反问"是否有空间"+"基于我的经验市场价在X-Y之间" → 最终目标是区间上限或略高于期望|
|X5-19|**多Offer策略**|尽量同时拿到2-3个Offer → 用高Offer去谈另一个的薪资(注意诚信，不要恶意抬价) → 综合考量不只看钱(成长空间/团队氛围/WLB/业务前景)|