|序号|知识点|笆记写什么|important|
|---|---|---|---|
|4.1|数据上报流程|Agent 采集 Span → 内存缓冲区（Buffer）→ 定时批量发送到 OAP（gRPC 协议）→ OAP Receiver 接收 → **Merger 聚合**（多维度指标计算）→ 存储到 ES/H6/MySQL；**采样策略**：全部采集 vs 基于采样率（减少数据量）|核|
|4.2|存储方案对比|**Elasticsearch**（默认推荐，查询灵活，适合 Trace 日志检索）/ **H6/TiDB**（适合大规模 Metric 指标，写入性能高）/ **MySQL**（小规模可用，不适合生产）；各方案的适用规模和取舍|热|
|4.3|指标聚合(Metric Aggregation)|OAP 将原始 Trace 数据聚合成预计算的指标：Service Apdex / Service Response Time Percentile(P50/P90/P99) / Service Throughput(QPS) / Success Rate；聚合窗口（分钟级/小时级/天级）；为什么需要聚合（避免实时计算的性能开销）|热|