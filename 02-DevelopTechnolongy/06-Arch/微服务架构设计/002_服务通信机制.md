| #         | 知识点                    | 核心内容                                                                               | 对比维度                                   | 面试追问                                                   | FlowPulse 选择 & 理由                                                   |
| --------- | ---------------------- | ---------------------------------------------------------------------------------- | -------------------------------------- | ------------------------------------------------------ | ------------------------------------------------------------------- |
| **3.2.1** | **同步 RPC: Dubbo**      | Apache Dubbo 3.x；基于Netty的长连接；TCP二进制协议(Dubbo协议/Hessian2/Kryo序列化)；支持10种负载均衡 + 9种容错模式 | 吞吐量极高、强类型约束、延迟低(亚毫秒级)、跨语言差             | "Dubbo的SPI扩展机制和Java SPI区别？自适应扩展原理？"                    | **★ 内部高频通信首选！** workflow ↔ scheduler / auth ↔ workflow 等内部调用全用Dubbo |
| **3.2.2** | **同步 HTTP: OpenFeign** | 声明式HTTP客户端；基于动态代理生成实现类；集成Ribbon负载均衡/Sentinel熔断；Spring Cloud生态标准组件                  | 跨语言友好、开发便捷、调试容易(RestClient直接测)、性能低于RPC | "Feign的拦截器链是怎么工作的？怎么加自定义Header？"                       | **★ Gateway→后端服务** 以及**外部开放接口** 用Feign(RESTful风格，方便对接前端和第三方)        |
| **3.2.3** | **异步消息: RocketMQ**     | 发布-订阅模式；解耦生产者和消费者；削峰填谷；可靠投递(同步刷盘/异步刷盘/主从同步)；事务消息                                   | 最终一致、削峰、缓冲、异步处理；但延迟较高(ms级)且顺序不易保证      | "RocketMQ如何保证消息不丢失？不重复？有序？"                            | **★ workflow→message / scheduler→monitor / 所有跨服务事件通知** 全走MQ         |
| **3.2.4** | **gRPC (对比了解)**        | Google开源；HTTP/2 + Protocol Buffers；流式(Streaming)双向通信；强类型IDL定义接口                    | 性能接近Dubbo、跨语言最强、IDL代码生成、HTTP/2多路复用     | "gRPC和Dubbo的核心区别？什么场景选gRPC？"                           | 备选方案；如果未来有Go/Rust服务加入可考虑gRPC桥接                                      |
| **3.2.5** | **通信方式选型决策树**          | 同步 vs 异步？→ 需要实时响应→同步；可接受延迟→异步。同步内选RPC还是HTTP？→ 内部高频→RPC(Dubbo)；对外/跨语言→HTTP(Feign)   | "你们项目为什么有些用Dubbo有些用Feign？"             | **FlowPulse四通信矩阵** ↓                                   |                                                                     |
| #         | 知识点                    | 核心内容                                                                               | 面试追问                                   | FlowPulse 选择 & 理由                                      |                                                                     |
| **3.2.6** | **接口版本管理**             | URL版(`/v1/user`) / Header版(`Api-Version: v1`) / 参数版(`?version=v1`)；兼容性策略(向后兼容优先)   | "接口做了不兼容变更怎么办？多版本共存怎么维护？"              | 统一 `/api/v1/` 前缀；新增字段向后兼容；废弃接口标记deprecated保留2个大版本      |                                                                     |
| **3.2.7** | **服务调用链路传递**           | TraceId在Dubbo(Attachment) / Feign(Header) / MQ(Properties)中的透传机制；MDC线程变量           | "TraceId在线程池中会丢失吗？怎么解决？"               | SkyWalking Agent自动注入TraceId；手动场景用MDC + TaskDecorator传递 |                                                                     |