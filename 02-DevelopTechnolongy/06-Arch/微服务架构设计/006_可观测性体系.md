|#|知识点|三支柱|核心工具链|面试追问|FlowPulse 实施|
|---|---|---|---|---|---|
|**3.6.1**|**Metrics 指标监控**|**RED方法**: Rate(请求速率) / Errors(错误率) / Duration(耗时)；四大黄金信号：Saturation / Latency / Traffic / Errors|Prometheus(采集+存储) + Grafana(可视化) + AlertManager(告警)|"Prometheus的Pull模型和Pushgateway分别用于什么场景？"|JVM指标(HTTP线程池/GC/堆内存) + 自定义业务指标(流程启动QPS/任务完成率) + HTTP接口指标|
|**3.6.2**|**Traces 链路追踪**|Span(一次调用) → Trace(一次完整请求经过的所有Span)；父子关系构建调用树；采样率策略|**SkyWalking 9.x**(Agent探针零侵入) / Zipkin / Jaeger|"SkyWalking Agent插桩原理？Byte Buddy字节码增强？"|**全链路追踪**：浏览器 → Gateway → Workflow → DB/MQ/Redis，完整Trace|
|**3.6.3**|**Logs 日志管理**|结构化JSON日志(ELK Stack) 或 轻量级(Loki + Promtail)；日志分级(ERROR/WARN/INFO/DEBUG)；日志上下文(TraceId/UserId)|Loki(存储) + Promtail(采集) + Grafana(查询) 或 ELK(Elasticsearch + Logstash + Kibana)|"ELK和Loki各自的优缺点？日志量大了怎么优化？"|Logback JSON格式输出 + MDC注入TraceId + UserId + Loki收集 + Grafana Explore查询|
|**3.6.4**|**三支柱联动分析**|Metrics报警 → 查Traces定位哪个Trace慢 → 查Logs看具体报错原因；从宏观到微观的排查路径|"线上告警了，你一般怎么一步步排查？"|排查SOP：Grafana告警→SkyWalking链路→Loki日志→定位根因→修复→回归||
|**3.6.5**|**告警体系设计**|告警规则(阈值/趋势/组合条件) → 分级(P0电话/P1企微/P2邮件) → 抑制(同告警不重复发) → 静默(维护窗口) → 升级(无人处理自动升级)|"怎么避免告警风暴？告警疲劳怎么解决？"|P0: 流程引擎挂掉(电话+企微)；P1: 任务积压超阈值(企微)；P2: 磁盘使用>80%(邮件)||
|**3.6.6**|**监控大盘设计原则**|分层级：L1全局概览(总QPS/ErrorRate/SLA) → L2服务维度(各服务健康度) → L3接口维度(Top10慢接口) → L4基础设施(DB/Redis/MQ/Nacos)|"一个好的监控大盘应该包含哪些关键指标？"|FlowPulse监控大屏：8个服务状态卡片 + 流程引擎实时吞吐 + 任务队列深度 + 系统资源利用率||