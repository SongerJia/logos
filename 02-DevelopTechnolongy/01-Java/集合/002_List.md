ArrayList 底层数组结构

ArrayList 扩容机制（1.5倍）热

ArrayList 的 add / remove 时间复杂度

LinkedList 双向链表结构

ArrayList vs LinkedList 选型

subList 的坑（视图而非副本）

Arrays.asList 返回的不可变 List

Collections.unmodifiableList

List.of / List.copyOf（Java 9+）

RandomAccess 标记接口意义