|序号|知识点|笔记写什么|重要度|
|---|---|---|---|
|3.1|消息不丢失保障方案对比|Kafka：acks=all + min.isr=2 + 副本数≥3（ strongest）/ RMQ：SYNC_FLUSH + 主从同步 + 事务消息（strong）/ RMQ：Publisher Confirm + 持久化 + 手动ACK（ strongest 单条）|🔥🔥🔥🔥 **必考**|
|3.2|分布式事务支持对比|RMQ：原生事务消息（半消息+回查，最完善）/ Kafka：0.11+ 支持事务（EOS，跨 Partition 原子写）/ RMQ：无原生事务支持（需业务侧补偿）|🔥🔥🔥🔥|
|3.3|消息重复处理对比|三者都不保证 Exactly-Once（需 Consumer 幂等）；Kafka 有 EOS 语义（Producer 幂等+事务）/ RMQ 事务消息也有去重机制 / RMQ 无内置去重|🔥🔥🔥|
|3.4|消费语义对比|Kafka：At-Least-Once（默认）/ At-Most-Once / Exactly-Once（EOS）/ RMQ：At-Least-Once（默认，无 At-Most）/ RMQ：At-Least-Once（默认）|🔥🔥|



|#|知识点|重要度|三层笔记建议|面试追问|FlowPulse 结合|
|---|---|---|---|---|---|
|B5-14|**生产端可靠性(发送重试 + 确认机制)**|★★★|L1: 同步发送(sync)/异步发送(async)/单向发送(oneway)三种方式可靠性递减；2. Broker ACK机制：0(不等确认) / 1(Leader收到即确认) / -1(所有ISR副本确认)；3. 重试策略：retryTimesWhenSendFailed + 重试时切换下一个Broker|ACK=-1对性能的影响？同步发送的超时设置？|**FlowPulse重要消息：同步发送 + ACK=-1 + retry=3**|
|B5-15|**存储端可靠性(同步刷盘 + 多副本)**|★★☆|L1: RocketMQ: SYNC_FLUSH + SYNC_MASTER / Kafka: min.insync.replicas + acks=all；2. 多机房部署(跨机架/跨可用区)；3. 数据校验(CRC32校验文件完整性)|单机三副本够不够？跨机房复制的延迟？|FlowPulse三机房部署策略|
|B5-16|**消费端可靠性(手动Ack + 重试队列)**|★★★|L1: 自动提交(可能重复消费) vs 手动提交(业务成功后才ack)；2. 消费失败处理：重试(有限次) → 死信队列(DLQ) → 人工介入；3. 并发消费(consumeThreadMin/Max) + 顺序消费(MessageQueueQueue锁)|什么情况下用顺序消费？死信队列的处理流程？|**FlowPulse消费模式：手动Ack + 3次重试 → DLQ + 告警通知**|
|B5-17|**事务消息(半消息机制)**|★★★|L1: 发送半消息(Half Message) → 执行本地事务 → Commit/Rollback半消息 → 消费者可见；2. 回查机制(checkLocalTransaction)：Broker未收到Commit/Rollback则回调Producer查询事务状态；3. 事务消息的局限性：不支持延时消息/不支持批量消息|本地事务执行完但没来得及Commit怎么办？回查频率怎么设？|**FlowPulse分布式事务核心：订单创建→发事务消息→扣减库存**|
|B5-18|**延迟消息实现原理**|★★☆|L1: RocketMQ: 延迟等级(1s/5s/10s/30s/1m/2m/.../2h共18级) + 定时任务轮询(deliverDelayedMessageTimerService)；2. RocketMQ 5.0 TimerWheel(时间轮)支持任意精度延迟；3. Kafka: 无原生延迟消息(需外部定时任务+重发)|为什么RocketMQ只有18个延迟等级？时间轮怎么实现O(1)调度？|**FlowPulse支付超时取消/审批超时提醒用延迟消息**|
|B5-19|**消息积压处理**|★★☆|L1: 积压原因：消费速度 < 生产速度(Consumer故障/DB慢/GC频繁)；2. 应急方案：①扩Consumer(但不能超Partition数)②新建Topic做转发Consumer③降级丢弃非关键消息；3. 预防：Consumer监控(lag告警) + 动态扩容|Consumer数能大于Partition数吗？积压了怎么快速恢复？|**FlowPulse消息积压监控：lag > 1000 触发一级告警，lag > 10000 触发紧急扩容**|