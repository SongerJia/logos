对象创建过程（分配内存 → 初始化零值 → 设置对象头 → init 方法）

热对象内存分配方式（指针碰撞 vs 空闲列表）

核TLAB 本地线程分配缓冲

对象头 Mark Word 内容（哈希/分代年龄/锁状态位）

对象访问定位（句柄池 vs 直接指针 HotSpot 选择）

String.intern() 原理与 JDK6/7/8 差异

数组对象的内存布局


|#|知识点|重要度|三层笔记建议|面试追问|FlowPulse 结合|
|---|---|---|---|---|---|
|B1-32|**JMM 抽象结构(主内存/工作内存)**|★★★|L1: 主内存(共享)=所有变量存储区域，工作内存(线程私有线程缓存)；L2: 8种原子操作(lock/unlock/read/load/use/assign/store/write)；L3: JMM与硬件内存模型的映射关系(LLC/L1/L2 Cache)|JMM和JVM内存结构的区别？工作内存就是物理寄存器吗？|多线程并行处理流程任务的可见性问题根源|
|B1-33|**happens-before 原则**|★★★|L1: 8条HB规则(程序序/管程锁/Volatile传递/start/join/Interrupt/finalizer/default)；L2: Volatile规则(V写→V读HB)/Monitor规则(unlock→lock HB)；L3: 时间顺序≠HB关系，HB是偏序关系(partial order)|两条线程没有HB关系会发生什么？volatile变量能保证有序性吗？|分布式事务Seata AT模式下锁的可见性保证|
|B1-34|**as-if-serial 语义**|★★☆|L1: 单线程内重排序不影响最终结果；L2: 编译器重排序(指令级并行)+处理器重排列(StoreBuffer/LoadBuffer)；L3: 重排序类型表(Data Control/Processor)|重排序在单线程一定安全吗？什么情况下单线程也会出问题？|编译器对FlowPulse业务代码的重排序影响|
|B1-35|**Volatile 实现原理**|★★★|L1: 保证可见性(Memory Barrier) + 禁止重排序(LoadStore/StoreStore屏障)；L2: x86架构下仅StoreLoad屏障有实际开销(CPU已强一致)；L3: JSR-133增强语义后的Volatile(long/double原子读写)|volatile能保证原子性吗(i++)？x86下volatile真的没开销？|流程状态标志位的Volatile声明|
|B1-36|**Final 字段语义**|★☆☆|L1: final字段在构造函数完成后对其他线程可见；L2: 安全发布(Safe Publication)：final引用的对象在构造完后可见；L3: JMM对final的特殊保障(final field barrier)|final一定能保证可见性吗？构造函数this逸出呢？|不可变流程定义对象(Immutable Template)的安全性|
|B1-37|**Double-Checked Locking 问题**|★★★|L1: DCL为何需要volatile(instance=new Singleton()非原子：分配→构造→赋值可能重排)；L2: JDK5之前DCL失效原因(JMM规范缺陷)；L3: 基于Holder的懒加载替代方案|不加volatile的DCL在什么条件下出错？Holder模式为什么线程安全？|FlowPulse单例组件(如Nacos客户端)的DCL实现|
|B1-38|**Long/Double 原子性特殊规则**|★☆☆|L1: JVM规范允许long/double分为两次32位操作(非原子)；L2: volatile long/double保证原子读写(JSR-133)；L3: 实际64位JVM大多原子化了，但规范仍允许非原子|64位JVM上long还需要volatile吗？AtomicLong的实现原理？|流程ID(Snowflake long)在高并发下的安全性|