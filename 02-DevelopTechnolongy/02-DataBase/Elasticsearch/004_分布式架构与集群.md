|序号|知识点|笔记写什么|important|
|---|---|---|---|
|4.1|ES 集群角色分工|Master（集群管理元数据）/ DataNode（存储数据+执行CRUD）/ Coordinating（请求路由转发）/ Ingest（预处理管道）；角色分离 vs 混合部署|🔥🔥🔥 **必懂基础**|
|4.2|分片(Shard)机制|Primary Shard（主分片）/ Replica Shard（副本分片）；分片数确定后不可改的原因；副本数可动态调整；一个 index = N primary × (1 + replicas) 个 shard|🔥🔥🔥 **绝对核心**|
|4.3|文档路由算法|`shard_num = hash(_routing) % num_primary_shards`；自定义 routing 解决数据倾斜；routing 变更的影响|核|
|4.4|集群健康状态|Green（所有主副分片正常）/ Yellow（主分片全有，副本缺失）/ Red（主分片丢失）；各状态产生原因和恢复步骤|热|
|4.5|脑裂(Split Brain)问题|产生条件（网络分区导致多个 master 候选人各自当选）；预防参数（`minimum_master_nodes` / `cluster.initial_master_nodes`）；ES 7+ 的改进|核|