|#|知识点|核心内容|部署建议|面试追问|FlowPulse 实施|
|---|---|---|---|---|---|
|**6.6.1**|**网络分层隔离**|DMZ区(Nginx+Gateway) → 应用区(各微服务) → 数据区(DB/Cache/MQ)；防火墙规则：只开放必要端口(80/443/22)；VPC子网隔离|K8s NetworkPolicy(命名空间间隔离) + SecurityGroup(云厂商安全组)|"VPC和安全组的区别？容器网络隔离怎么做？"|生产环境：VPC划分DMZ/App/Data三区 + SecurityGroup最小化开放端口|
|**6.6.2**|**WAF Web应用防火墙**|防御OWASP Top 10攻击(SQL注入/XSS/命令注入/路径遍历等)；规则引擎匹配请求特征；ModSecurity / 云WAF(阿里云WAF/Cloudflare)|云WAF(免运维) vs 自建ModSecurity(灵活但需维护)|"WAF能替代代码层面的安全措施吗？"|初期Nginx+WAF规则集(限流/IP黑名单/User-Agent过滤) → 上线后接入阿里云WAF|
|**6.6.3**|**DDoS 防护**|流量清洗：区分正常流量和攻击流量(包特征/IP信誉/行为分析)；CDN分散流量 + 高防IP + 流量限流(同4.4)|三层防护：DNS层(Cloudflare/阿里云DNS) → 边缘层(CDN/WAF) → 应用层(Sentinel)|"CC攻击和传统DDoS区别？应用层怎么防护CC攻击？"|Sentinel限流(单IP阈值) + Nginx连接数限制(conn_limit) + CDN加速(分散源站压力)|