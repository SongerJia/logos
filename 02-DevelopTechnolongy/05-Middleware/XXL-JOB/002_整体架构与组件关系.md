|序号|知识点|笔记写什么|重要度|
|---|---|---|---|
|2.1|XXL-JOB 两角色架构|**Admin 调度中心**（Web管理后台 + 调度引擎）/ **Executor 执行器**（内嵌在业务服务中的 Agent）；通信协议：HTTP + JSON（不是 RPC！）；画出部署拓扑图|🔥🔥🔥 **基础中的基础**|
|2.2|DB 表结构设计（理解设计意图的关键）|xxl_job_group（执行器注册组）/ xxl_job_info（任务配置）/ xxl_job_log（运行日志）/ xxl_job_logglue（GLUE代码版本）/ xxl_job_registry（在线节点注册表）/ xxl_job_lock（分布式锁）；**为什么用 DB 而不是 ZK 做协调**|🔥🔥🔥|
|2.3|调度完整时序流程|Admin 触发调度（Cron到达或手动）→ 查询路由策略选择 Executor → HTTP 调用 Executor → Executor 接收请求丢入线程池执行 → 回调 Admin 报告结果 → Admin 记录日志；**画时序图**|🔥🔥🔥|
|2.4|为什么选择 DB 协调而非 ZK/Eureka|设计哲学：**简单优先**——DB 已经是系统基础设施，不需要额外引入 ZK；通过 DB 行锁实现分布式锁和故障转移；缺点：DB 成为单点（可通过主从缓解）；与 Elastic-Job(ZK) 的取舍|🔥🔥🔥|