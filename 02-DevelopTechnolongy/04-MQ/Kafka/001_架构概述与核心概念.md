|序号|知识点|笔记写什么|重要度|
|---|---|---|---|
|1.1|Kafka 整体架构与角色|Producer / Consumer / Broker / ZooKeeper(旧) → KRaft(新，去ZK化)；Topic / Partition / Consumer Group 四大核心概念图|🔥🔥 **入门基础**|
|1.2|为什么 Kafka 吞吐量碾压一切|顺序写磁盘（比随机写快600倍）/ 零拷贝 sendfile() / 批量压缩(batch+compress) / 消费者拉模式；与RMQ/RabbitMQ的架构差异对比|🔥🔥🔥 **必考灵魂问题**|
|1.3|ZooKeeper vs KRaft（去ZK化）|Kafka 2.8 引入 KRaft 模式；3.x 默认 KRaft；Controller Quorum 基于 Raft；去 ZK 的好处（部署简化/启动快/元数据一致性更强）|热|
|1.4|Kafka 的典型使用场景|日志收集 / 流式处理 / 事件驱动架构 / 消息队列（降级使用）；不适合的场景（低延迟单条消息 / 复杂路由）|热|



|#|知识点|重要度|三层笔记建议|面试追问|FlowPulse 结合|
|---|---|---|---|---|---|
|B5-08|**Partition & 副本机制**|★★★|L1: Topic→Partition(有序队列)→Leader+Follower ISR(In-Sync Replicas)副本集；2: Leader读写/Follower只从Leader拉取同步；3: ISR动态管理(min.insync.replicas保证写入可靠性)|ISR和AR(Assigned Replicas)的区别？Unclean Leader Election风险？|**FlowPulse日志收集Topic：12 partition × 3副本**|
|B5-09|**Kafka 高效写入原理**|★★★|L1: 顺序写磁盘(比随机写快6个数量级) + Page Cache(操作系统缓存减少应用层缓存) + 零拷贝(sendfile)；2: 批量发送(batch.size + linger.ms聚合) + 压缩(compression.type)；3: 为什么Kafka吞吐量远超其他MQ？(充分利用OS能力而非自己管理)|Page Cache和应用程序Buffer的区别？linger.ms和batch.size怎么配？|FlowPulse海量日志采集用Kafka|
|B5-10|**消费者组 Rebalance 机制**|★★★|L1: 消费者组内Partition分配策略(Range/ RoundRobin/ Sticky/ CooperativeSticky)；2: Rebalance触发：组成员变化/Topic Partition数变化/订阅变化；3: Rebalance过程：Stop-the-World(暂停消费) → Reassign Partitions → Resume|Rebalance有什么危害？怎么减少Rebalance频率？CooperativeSticky改进了什么？|**FlowPulse消费者组配置：session.timeout.ms=30s + heartbeat.interval.ms=10s**|
|B5-11|**消息语义(Exactly-Once)**|★★☆|L1: At-Most-Once(auto.commit.offset=true)/At-Least-Once(manual commit)/Exactly-Once(事务API + 幂等生产者)；2: Kafka 0.11+的事务API：beginTransaction/sendOffsets/commitTransaction/abortTransaction；3: 幂等生产者(idempotent producer, PID + 序号)防止重复写入|EoS在Kafka中怎么实现的？事务API的性能开销？|关键业务消息的Exactly-Once保证|
|B5-12|**Watermark / Offset 管理**|★★☆|L1: High Watermark(HW)——ISR中所有副本都已同步的位置(消费者只能看到HW之前的消息)；2. Log End Offset(LEO)——Leader写入的最后位置；3. HW和LEO的滞后关系(LEO≥HW) + Follower的LEO追赶|HW和LSO(Log Stable Offset)区别？消费者Offset存储在哪？|消费进度的监控与积压告警|
|B5-13|**Kafka vs RocketMQ 选型维度**|★★☆|L1: 吞吐量(Kafka>RocketMQ)、延迟(RocketMQ毫秒级/Kafka毫秒级)、可靠性(都高)、功能(RocketMQ事务/延迟消息/死信队列更丰富)；2. 技术栈匹配(Java生态选RocketMQ)、运维复杂度(Kafka ZooKeeper依赖)、社区活跃度；3. FlowPulse选型结论：**业务消息RocketMQ + 日志采集Kafka**|如果只能选一个怎么选？各自的劣势是什么？|已在模块8技术选型中决策，此处深化理解|