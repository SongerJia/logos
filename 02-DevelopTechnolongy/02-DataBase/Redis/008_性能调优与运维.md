|知识点|说明|
|---|---|
|**慢查询日志**|`slowlog-log-slower-than`（阈值，默认 10ms）/ `slowlog-max-len`（最多保存多少条慢查询记录）。分析：`SLOWLOG GET [count]`。注意：慢查询只记录命令执行时间，**不包括网络传输和排队时间**|
|**Pipeline 管道**|热 客户端一次性发送多条命令，减少 RTT（Round Trip Time）。**Pipeline 不是事务**（不保证原子性），只是网络优化手段。与 Lua 脚本的区别：Pipeline 是批量发送，Lua 是服务端原子执行。**适合场景**：批量写入 / 批量读取，性能提升 5~10 倍|
|**Redis 6.0 多线程 IO**|Redis 6.0 之前是单线程（网络 IO + 命令执行 都在主线程）。6.0 引入多线程 IO（只负责网络读写，命令执行仍是单线程）。**为什么命令执行不改成多线程？** 因为 Redis 的核心瓶颈在网络 IO，不是 CPU 计算；且单线程避免了锁竞争，实现简单|
|**maxmemory 与生产环境配置**|推荐配置速查：`maxmemory 物理内存的 60%~70%` / `maxmemory-policy allkeys-lru` / `appendonly yes` / `appendfsync everysec` / `aof-use-rdb-preamble yes` / `timeout 300`（客户端空闲超时）/ `tcp-keepalive 60`。**分 4G/8G/16G/32G 内存给四套推荐配置**|
|**Redis 预热与熔断**|系统重启后缓存为空，大量请求打 DB → 预热（启动脚本批量加载热点数据）。缓存雪崩 → 熔断（Sentinel 配置 `@SentinelResource` 降级方法返回默认值）。**这两项是生产环境必须做的**|




| #     | 知识点                   | 重要度 | 三层笔记建议                                                                                                                                                              | 面试追问                                            | FlowPulse 结合                            |
| ----- | --------------------- | --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------- | --------------------------------------- |
| B4-24 | **Pipeline 与 Lua 脚本** | ★★★ | L1: Pipeline打包多个命令一次性发送减少RTT(Round Trip Time)；2: Lua脚本(EVAL/EVALSHA)保证原子性(单线程执行，类似事务但更强)；3: Pipeline vs 事务(MULTI/EXEC) vs Lua的选择：Pipeline无原子性/事务有原子性但有些命令不支持/Lua可编程 | Pipeline命令数有限制吗？Lua脚本执行超时怎么办？                   | **FlowPulse批量操作用Pipeline / 分布式锁用Lua脚本** |
| B4-25 | **Redis 事务与Watch**    | ★★☆ | L1: MULTI/EXEC/DISCARD/WATCH 四命令；WATCH实现乐观锁(监视key在EXEC前未被修改)；2: Redis事务不支持回滚(命令语法错误整个事务拒绝/运行时错误跳过该命令继续执行)；3: 与关系型数据库事务的本质区别                                         | 为什么Redis不支持回滚？Watch的CAS原理？                      | 流程库存扣减的乐观锁(Watch + MULTI/EXEC)          |
| B4-26 | **发布订阅 Pub/Sub**      | ★☆☆ | L1: SUBSCRIBE/PUBLISH/UNSUBSCRIBE/PUNSUB 命令；2: 模式订阅(PSUBSCRIBE flow.* 通配符)；3: 局限性：消息不持久化(离线收不到)/无堆积/消息确认机制——不适合可靠消息队列                                               | Pub/Sub和Message Queue(Kafka/RocketMQ)区别？什么时候用它？ | **FlowPulse轻量事件通知(流程状态变更通知前端)**         |
| B4-27 | **Stream 类型(5.0+)**   | ★★☆ | L1: Redis的日志数据结构(XADD/XREAD/XGROUP/XACK)；2: Consumer Group消费组概念(类似于Kafka Consumer Group)；3: XREADGROUP BLOCK 阻塞读取 + Pending Entries List(PEL未确认消息)                  | Stream和List做MQ的区别？Stream的消息可靠性？                 | **FlowPulse轻量消息队列替代(不需要RocketMQ的场景)**   |
| B4-28 | **Redis 6/7 新特性**     | ★☆☆ | L1: Redis 6: IO多线程(网络IO非命令执行)/ACL权限控制/Resp3协议/Module API扩展；2: Redis 7: Functions(函数引擎)/Version-time expiry(带版本过期)/Multi-part AOF；3: IO多线程并非真正多线程命令执行——命令执行仍是单线程     | IO多线程能提升多少性能？ACL怎么配？                            | 未来升级评估                                  |