  
application.yml / application.properties 多环境配置

热@ConfigurationProperties 类型安全绑定

热@Value vs @ConfigurationProperties 区别

热Profile 多环境切换（dev / test / prod / local）

配置文件加载优先级（命令行 > 环境变量 > application-{profile} > application.yml）

松散绑定（Relaxed Binding）：user-name / userName / user_name 统一映射

配置加密（Jasypt / Spring Cloud Config 加密）