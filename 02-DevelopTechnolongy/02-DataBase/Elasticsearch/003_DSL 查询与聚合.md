|序号|知识点|笔记写什么|important|
|---|---|---|---|
|3.1|Query DSL vs Filter DSL 区别|Query（计算 _score，影响排序）/ Filter（不评分，有缓存）；bool 查询中 must/filter/should/must_not 各自行为；性能差异原因|🔥🔥🔥 **高频必问**|
|3.2|全文查询类|match / match_phrase / multi_match / query_string 的区别和使用场景；match 内部实际执行了什么（分析→查倒排表）|核|
|3.3|精确查询类|term / terms / range / exists / prefix / wildcard / ids；term 和 match 在 text 字段上的致命区别（keyword 问题）|🔥🔥 **经典坑题**|
|3.4|复合查询（bool）|must / should / must_not / filter 四子句组合；minimum_should_match 含义；bool 嵌套场景|核|
|3.5|聚合框架(Aggregation) 四大分类|Metric（avg/max/stats）/ Bucket（terms/date_range/histogram）/ Pipeline（derivative/moving_avg）/ Matrix（矩阵）；嵌套聚合写法|热|
|3.6|深分页问题与解决方案|from+size（深度 >10000 时性能崩盘）/ search_after（游标式）/ scroll（快照式，已标记 deprecated）；三种方案适用场景|🔥🔥🔥 **面试高频**|
|3.7|排序与高亮|sort 对 _score 的影响；多字段排序；highlight 配置（pre_tags/post_tags/fragment_size/number_of_fragments）|热|