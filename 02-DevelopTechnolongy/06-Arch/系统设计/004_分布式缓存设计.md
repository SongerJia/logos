|#|知识点|重要度|三层笔记建议|面试追问|FlowPulse 结合|
|---|---|---|---|---|---|
|B7-22|**缓存三大问题深度解法**|★★★|L1: 缓存穿透(查不存在的key) → 布隆过滤器拦截 + 空值缓存(TTL短)；L2: 缓存击穿(热点key过期) → 互斥锁重建 / 永不过期(异步续期)；L3: 缓存雪崩(大量key同时过期) → TTL随机打散 / 多级缓存 / 高可用集群|布隆过滤器误判率怎么算？互斥锁对吞吐的影响？|**FlowPulse缓存防御体系已在此处深化**|
|B7-23|**缓存一致性策略**|★★★|L1: Cache Aside Pattern(读：先cache再db / 写：先db再delete cache)；L2. Write Through(缓存与DB同步写，由缓存组件保证) / Write Behind(异步批量写回)；3. 延迟双删(删缓存→更DB→Sleep→再删缓存)解决并发不一致|先删缓存还是先更DB？为什么一般不建议Write Through？|FlowPulse缓存一致性策略选择|
|B7-24|**Hot Key 发现与处理**|★★☆|L1. 客户端上报(统计key访问频率定期上报) / 服务端统计(Proxy层统计)；2. Hot Key处理：本地缓存(Guava/Caffeine) + 多级缓存 + key拆分(加随机后缀分散到多节点)；3. Hot Key自动发现：Netflix Archaius / JD HotKey|本地缓存和多级缓存的区别？Key拆分后怎么聚合？|FlowPulse热点数据自动发现|
|B7-25|**Big Key 治理**|★★☆|L1. Big Key危害(单次操作阻塞/集群数据倾斜/网络拥塞/主从同步慢)；2. 发现：redis-cli --bigkeys -r 100000 / SCAN渐进扫描；3. 治理：压缩(snappy/gzip) / 拆分(hash tag分片) / Lazy删除(UNLINK)|UNLINK和DEL的区别？怎么预防Big Key产生？|FlowPulse Big Key预防规范|
|B7-26|**分布式缓存 vs 本地缓存**|★★☆|L1: 分布式缓存(Redis/Memcached)：多节点共享/持久化/集群模式；本地缓存(Guava Cache/Caffeine)：零网络开销/容量有限/单节点；2. 二级缓存架构：L1本地(Caffeine, TTL短) → L2分布式(Redis, TTL长)；3. 缓存穿透问题(本地缓存命中但可能过期)|什么时候只需要本地缓存？二级缓存的失效策略？|**FlowPulse L1 Caffeine(字典数据/租户配置) + L2 Redis(业务数据)**|
|B7-27|**缓存预热与灰度发布**|★☆☆|L1. 缓存预热(启动时/定时任务加载热点数据)；2. 灰度发布时缓存问题(新旧版本缓存格式不一致/版本标识)；3. 缓存版本化(cache v2 prefix) + 灰度期间双写|灰度期间怎么保证缓存兼容？缓存怎么回滚？|FlowPulse部署时缓存预热|