| #         | 知识点               | 核心机制                                                                                             | 适用场景                                                | 面试追问                                                   | FlowPulse 应用                                                                                              |
| --------- | ----------------- | ------------------------------------------------------------------------------------------------ | --------------------------------------------------- | ------------------------------------------------------ | --------------------------------------------------------------------------------------------------------- |
| **5.5.1** | **MQ 异步解耦**       | 同步串行调用(总耗时=A+B+C) → 异步并行(MQ解耦，主流程只需A)；削峰：流量高峰时消息暂存队列，消费者按自身速率消费                                  | 非实时要求的通知/日志/统计分析/跨服务事件通知                            | "MQ如何保证消息顺序消费？如何保证消息不丢失？"                              | **工作流事件全部异步**：流程启动→发MQ→MessageService消费→发送站内信+邮件+短信                                                       |
| **5.5.2** | **RocketMQ 削峰填谷** | Producer快速写入Broker(毫秒级) → Consumer按配置的consumeThreadNum拉取消费；消息堆积在Broker磁盘上而非压垮下游                  | 消费者线程数控制 / 消息重试 / 死信队列(DLQ) / 消息堆积监控(queueSize告警)   | "消息堆积了怎么办？有哪些应对措施？"                                    | Scheduler触发大批量任务执行 → MQ缓冲 → workflow-engine按能力消费，避免瞬时打满                                                   |
| **5.5.3** | **线程池异步编排**       | CompletableFuture链式编排：`thenApply / thenAccept / thenCompose / allOf / anyOf`; 将多个独立IO操作并行执行减少总耗时 | 适用：需要聚合多个服务结果的场景(如首页信息聚合)；注意：异常传播和超时控制              | "CompletableFuture的thenApply和thenCompose的区别？什么时候该用哪个？" | 流程启动时的并行初始化：加载定义 + 创建实例 + 初始化变量 + 发送事件 = 4个CompletableFuture.allOf                                        |
| **5.5.4** | **事件驱动架构 EDA**    | 领域事件(Domain Event)作为服务间通信的核心载体；Event Store(可选)存储事件支持重放；最终一致性模型                                   | 事件版本管理 / 幂等消费 / 事件溯源(Event Sourcing) / CQRS(命令查询分离) | "事件驱动和传统RPC调用的本质区别？什么场景适合事件驱动？"                        | **WorkflowEventBus**: ProcessStartedEvent / TaskCreatedEvent / TaskCompletedEvent / ProcessCompletedEvent |