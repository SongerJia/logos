|知识点|笔记时重点写什么|
|---|---|
|**RDB 快照持久化**|热 `SAVE`（阻塞主线程）/ `BGSAVE`（fork 子进程，Copy-On-Write 机制）。触发方式：save 配置（如 `save 900 1` 表示 900 秒内至少 1 次写就触发）/ 手动执行 / 主从全量同步时。优点：文件紧凑、恢复快；缺点：最后一次快照后的数据会丢失|
|**AOF 追加日志持久化**|热 记录每一条写命令（协议格式，可读可重放）。三种刷盘策略：`always`（每条命令 fsync，最慢最安全）/ `everysec`（每秒 fsync，推荐，最多丢 1 秒数据）/ `no`（依赖 OS 刷盘，最快最不安全）。AOF 重写（`BGREWRITEAOF`）：合并冗余命令，减小文件体积|
|**RDB + AOF 混合持久化**|核 Redis 4.0+ 默认开启（`aof-use-rdb-preamble yes`）。AOF 重写时先写一份 RDB 快照，再追加重写期间的增量 AOF 命令。**重启时先加载 RDB 再重放 AOF 增量**，兼顾速度和安全性。**这是生产环境推荐配置**|
|**AOF 重写流程（源码级）**|核 fork 子进程 → 子进程遍历内存写新的 RDB（混合模式下）或 AOF → 重写期间的新命令写入 `aof_rewrite_buf` 缓冲区 → 子进程完成，父进程将缓冲区内容追加到新 AOF → 原子替换旧 AOF 文件。**理解 fork + COW 是理解 Redis 持久化的关键**|
|**持久化恢复优先级**|同时开启 RDB 和 AOF 时，**优先加载 AOF**（数据更完整）。AOF 文件损坏时可用 `redis-check-aof` 修复工具。RDB 文件损坏可用 `redis-check-rdb` 检查|



|#|知识点|重要度|三层笔记建议|面试追问|FlowPulse 结合|
|---|---|---|---|---|---|
|B4-08|**RDB 快照持久化**|★★★|L1: fork()子进程Copy-On-Write写时复制，子进程将内存数据dump到.rdb文件；L2: SAVE(阻塞主线程，已废弃) vs BGSAVE(后台异步fork子进程)；L3: 触发方式：手动SAVE/BGSAVE / 自动(save N秒 M次改动) / FLUSHALL/RDB载入|RDB恢复速度快的原理？fork瞬间会阻塞吗？COW对内存的影响？|**FlowPulse Redis定时RDB备份策略：save 900 1 / save 300 10 / save 60 10000**|
|B4-09|**AOF (Append Only File) 日志持久化**|★★★|L1: 记录每一条写命令(追加到AOF文件)；L2: AOF重写(rewrite)：fork子进程遍历内存生成新的精简AOF(多条命令合并)；L3: 同步策略：always(每次写sync，最安全最慢)/everysec(每秒fsync，推荐)/no(交OS)|AOF文件过大怎么处理？rewrite过程中新命令怎么记录？AOF加载恢复过程？|**FlowPulse关键数据用AOF+everysec策略**|
|B4-10|**RDB vs AOF 选型对比**|★★★|L1: RDB：文件小/恢复快/可能丢数据(最后一次快照后) / AOF：数据安全性高/文件大/恢复慢；L2: 混合持久化(Redis 4.0+)：RDB做基础+AOF增量(重写时先写RDB格式头部+后续AOF格式)；L3: 生产环境推荐：混合持久化 + aof-use-rdb-preamble yes|混合持久化的AOF文件格式？什么时候该选哪种？|**FlowPulse采用混合持久化(persistence-aof-use-rdb-preamble yes)**|
|B4-11|**fork() 性能与 Copy-on-Write**|★★☆|L1: Linux fork()创建子进程时复制父进程页表(非内存)，实际内存页COW机制；L2: fork耗时与内存大小正相关(大内存Redis fork可能阻塞数百ms)；L3: info memory中的used_memory_rss humans判断COW开销|大内存Redis(>20GB)fork风险？如何减小fork影响？|监控Redis fork耗时指标|
|B4-12|**AOF 重写细节与异常恢复**|★☆☆|L1: BGREWRITEAOF后台重写过程：fork→子进程遍历内存→写临时文件→rename原子替换；L2: 重写期间的新写命令记录在AOF rewrite buffer→重写结束后append到新AOF；L3: 异常中断恢复：redis-check-aof --fix修复|AOF重写期间宕机怎么恢复？rewrite buffer满了吗怎么办？|了解即可|