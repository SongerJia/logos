@RequestMapping 注解详解（value / method / params / headers / consumes / produces）

热@GetMapping / @PostMapping / @PutMapping / @DeleteMapping / @PatchMapping

热@PathVariable / @RequestParam / @RequestBody / @RequestHeader / @CookieValue 区别

热参数绑定原理（WebDataBinder / 数据转换 / 格式化）

@ModelAttribute 的两种用法（方法参数 / 方法级别）

自定义类型转换器 Converter

自定义格式化器 Formatter

@Valid 参数校验（JSR-303 / Bean Validation）

核HttpMessageConverter 消息转换器体系（JSON 序列化/反序列化）


|#|知识点|重要度|三层笔记建议|面试追问|FlowPulse 结合|
|---|---|---|---|---|---|
|B6-18|**DispatcherServlet 核心流程**|★★☆|L1: HandlerMapping(找到处理器) → HandlerAdapter(执行处理器) → 视图渲染 → 响应返回；2. doDispatch()完整逻辑(拦截器→处理器→视图→afterCompletion)；3. 九大组件初始化(onRefresh策略模式)|DispatcherServlet是单例的吗？多线程安全问题？|FlowPulse API网关层的请求分发|
|B6-19|**参数解析(ArgumentResolvers)**|★★☆|L1: HandlerMethodArgumentResolver策略接口(30+内置实现)；2: @PathVariable/@RequestBody/@RequestParam/@RequestHeader 各自的解析器；3: 自定义ArgumentResolver(如当前用户信息自动注入)|@RequestBody和@RequestParam的区别？自定义参数解析器的步骤？|FlowPulse WfUserContext自动注入(从Header/JWT解析)|
|B6-20|**返回值处理器(ReturnValueHandlers)**|★☆☆|L1: HandlerMethodReturnValueHandler处理Controller方法返回值；2: ResponseEntity/ResponseBody/String各对应不同的Handler；3: 统一响应体包装(@RestControllerAdvice + ResponseBodyAdvice)|怎么统一包装返回格式？怎么处理异常统一响应？|FlowPulse Result统一响应封装|
|B6-21|**拦截器 vs 过滤器 vs Aspect**|★★☆|L1: Filter(Servlet规范，请求到达DispatcherServlet前) → Interceptor(Handler前后，可访问Handler) → Aspect(方法级别细粒度)；2. 执行顺序：Filter→DispatcherServlet→Interceptor preHandle→Controller→Interceptor postHandle→Aspect afterReturning→Interceptor afterCompletion→Filter；3: 适用场景：Filter(CORS/编码/GZIP) → Interceptor(登录鉴权/权限) → Aspect(日志/监控/事务)|Filter能获取到Spring Bean吗？Interceptor能获取请求参数吗？|**FlowPulse三层切面体系：Filter(认证) → Interceptor(权限) → Aspect(日志+监控)**|
|B6-22|**异步请求处理**|★☆☆|L1: Callable< T>/DeferredResult< T>/ListenableFuture(旧版)/CompletableFuture(推荐)；2: Servlet 3.0 AsyncContext + Spring MVC异步处理机制；3: 超时处理 + 回调|异步处理的线程模型？和@Async有什么区别？|工作流长时间运行任务的异步响应|