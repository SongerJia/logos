### 为什么这个模块重要

**Seata 上线后的问题往往集中在性能和异常处理上。** 这部分内容直接决定你在生产环境的排障能力，也是体现"不只是会用"的关键区分点。

### 知识点表

|序号|知识点|重要度|笔记三层建议|跨模块关联|
|---|---|---|---|---|
|8.1|**性能瓶颈分析与优化手段**|🔴必背|① 瓶颈一：undo log写入（IO开销）→ 异步刷盘 ② 瓶颈二：全局锁竞争（串行化）→ 减小事务范围 ③ 瓶颈三：额外RPC（TC通信）→ TC就近部署 ④ 面试：Seata对QPS的影响大概多少？怎么优化？|→ MySQL性能优化(M10)|
|8.2|**脏写/悬挂/空回滚 深度剖析**|🔴必背|① 三种异常的完整触发条件复现 ② Seata的内置防御机制 ③ 面试：详细讲一下AT模式的三大异常场景|→ M2.5 AT异常防御 / M3.2 TCC异常|
|8.3|**超时 & 重试机制**|🟡应掌握|① 全局事务超时配置(timeout) ② 分支事务注册失败重试 ③ 死锁检测 ④ 面试：全局事务超时了会发生什么？|→ Feign超时重试(M8框架)|
|8.4|**日志排查 & 问题定位方法**|🟡应掌握|① Server端日志：branch_register/global_* ② Client端日志：RM/TM角色日志 ③ 常见错误码解读 ④ 面试：Seata事务失败了你怎么排查？|→ SkyWalking链路追踪(M21)|
|8.5|**AT & TCC 混合使用**|🟡了解|① 同一全局事务中混用AT和TCC ② 注意事项：提交/回滚顺序 ③ 面试：可以在同一个全局事务中混合使用AT和TCC吗？|→ M2 AT / M3 TCC|
|8.6|**Seata vs 其他分布式事务方案终选**|🟡应掌握|① Seata(AT) vs RocketMQ事务消息 vs 本地消息表 ② 各自适用场景 ③ 面试：为什么选Seata而不是RocketMQ事务消息？|→ RocketMQ事务消息(M16) / MQ对比选型(M19)|
