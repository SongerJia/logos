|知识点|笔记时重点写什么|
|---|---|
|**主从复制原理（异步模式）**|热 Master 的 binlog dump 线程 → Slave 的 IO 线程请求 binlog 并写入 relay log → Slave 的 SQL 线程重放 relay log 到本地数据库。**三个线程协作模型**要能手画出来。延迟来源：网络传输 + IO 写入 + SQL 重放|
|**binlog 三种格式对比**|STATEMENT / ROW / MIXED（已在 05 模块详细展开，这里侧重于**对复制的 影响**：ROW 格式下从库完全忠实还原，但大数据量 DML 时 binlog 会暴增；STATEMENT 在有不确定函数时可能导致主从不一致）|
|**GTID（全局事务标识符）**|热 UUID:sequence_number 格式，唯一标识每一个事务。优势：故障切换不再需要计算 binlog 位置（`CHANGE MASTER TO MASTER_AUTO_POSITION=1`）。GTID 复制模式下 Slave 只执行 Master 已提交的事务，跳过已执行的。**MySQL 5.6+ 推荐 GTID 模式**|
|**主从延迟问题与解决方案**|热 **工程中最常见的 MySQL 问题**。延迟原因：Slave 单线程 SQL 重放是大瓶颈（MySQL 5.6 前）。解决方案：①并行复制（MTS，基于 LOGICAL_CLOCK / WRITESET 方案）②半同步复制（semi-sync，Master 等待至少一个 Slave 确认 ACK 后才提交）③业务规避（主库写后读主库）④监控告警（Seconds_Behind_Master 阈值报警）|
|**半同步复制**|核 after_sync（Master 先写 binlog + sync 到磁盘，等 Slave ACK 后才提交事务）vs after_commit（Master 先提交事务再等 Slave ACK）。前者更安全（保证主从一致），后者性能略好。rpl_semi_sync_master_timeout 超时降级为异步|
|**一主多从 / 级联复制**|一主多从适合读密集场景（从库分担读压力）。级联复制（Master → Relay A → Slave B/C/D）减轻 Master 的 binlog dump 压力。**但级联增加了延迟链路**，需要权衡|



| #     | 知识点                                | 重要度 | 三层笔记建议                                                                                                                                                                                            | 面试追问                                            | FlowPulse 结合                         |
| ----- | ---------------------------------- | --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------- | ------------------------------------ |
| B3-22 | **Binlog 三种格式对比**                  | ★★★ | L1: STATEMENT(基于SQL语句，节省空间但不安全如NOW()/UUID()) / ROW(基于行数据变化，最可靠但体积大) / MIXED(混合模式)；L2: ROW格式的binlog_row_image参数(FULL/MINIMAL/NOBLOB控制记录内容)；L3: ROW格式下mysqlbinlog --base64-output=decode-rows -v 解码 | STATEMENT格式什么场景有问题？ROW格式binlog太大了怎么办？           | **FlowPulse生产环境用ROW格式(数据安全优先)**      |
| B3-23 | **MySQL 主从复制原理**                   | ★★★ | L1: Master写binlog → Slave I/O线程拉取relay log → Slave SQL线程重放；L2: 异步复制(默认)/半同步(插件rpl_semi_sync_master确认至少一个Slave收到)/GTID复制(GTID代替binlog_pos)；L3: 复制延迟原因及解决方案(并行复制MTS/读写分离延迟补偿)                       | 主从延迟怎么监控？GTID的优势？半同步的性能损耗？                      | **FlowPulse读写分离架构 — 从库延迟补偿策略**       |
| B3-24 | **两阶段提交(2PC)**                     | ★★★ | L1: 事务提交时同时写Redo Log(prepare状态)和Binlog(commit状态)——保证两者一致；L2: 崩溃恢复：Redo prepare+Binlog存在→提交 / Redo prepare+Binlog不存在→回滚；L3: 这里的2PC不是分布式事务2PC而是MySQL内部的提交协议                                         | 为何需要两阶段提交？prepare状态的事务怎么处理的？                    | FlowPulse数据一致性底层保障机制                 |
| B3-25 | **GTID (Global Transaction ID)**   | ★★☆ | L1: GTID = source_id:transaction_id 全局唯一事务标识；L2: 优势：故障切换无需找binlog_pos(Executed_Gtid_Set自动同步)；gtid_mode=ON_PERMISSIVE平滑迁移                                                                          | GTID的限制(不能CREATE TABLE AS SELECT等)？传统复制迁移到GTID？ | FlowPulse未来主从切换便利性                   |
| B3-26 | **并行复制(MTS Multi-Threaded Slave)** | ★☆☆ | L1: LOGICAL_CLOCK(基于commit_order并行) / DATABASE(按数据库并行，需分库)；L2: slave_parallel_workers设置并行Worker数(建议4-8)；slave_parallel_type=LOGICAL_CLOCK；L3: MTS瓶颈在Coordinator分发效率                               | MTS一定能加快复制吗？什么情况下MTs效果不明显？                      | 多租户FlowPulse按tenant_id分库适合DATABASE模式 |