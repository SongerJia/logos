|序号|知识点|笔记写什么|important|
|---|---|---|---|
|7.1|索引设计优化|避免过多 field / 控制 nested 层深 / 使用 keyword 做 term 聚合而非 text / `_source` 存储 vs `store` / `doc_values` vs `fielddata`|🔥🔥🔥 **实战重灾区**|
|7.2|查询性能优化|Filter 缓存利用 / 避免 script query / routing 减少分片扫描 / preference=_local / profile API 分析慢查询|核|
|7.3|JVM & OS 层面调优|Heap 大小（不超过31G避免指针压缩失效）/ GC选择(G1GC)/ mmapfs / file descriptor / swap off / bootstrap.memory_lock|热|
|7.4|索引生命周期管理(ILM)|Hot（频繁读写）→ Warm（只读）→ Cold（低频访问）→ Delete（删除）；rollover 策略；ILM 与 Rollover Alias 配合使用|核|
|7.5|常见故障处理|Cluster Block（磁盘水位线85%/90%）、Mapping 冲突、OOM（fielddata / aggregation）、Recovery 慢|热|
|7.6|监控指标|`_cat/indices` / `_nodes/stats` / `/_cluster/health`；关键指标：搜索QPS、写入TPS、JVM GC时间、段内存、队列积压量|核|