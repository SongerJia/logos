| #         | 知识点                   | 核心内容                                                                                                                                   | 面试追问                                              | FlowPulse 部署方案                                                                                          |
| --------- | --------------------- | -------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------- | ------------------------------------------------------------------------------------------------------- |
| **3.8.1** | **Docker容器化**         | Dockerfile最佳实践(多阶段构建减小镜像)；镜像仓库(Harbor)；资源限制(memory/CPU)；健康检查(HEALTHCHECK)                                                              | "Docker镜像分层原理？怎么优化镜像大小？"                          | 每个服务一个Dockerfile；JDK17-jre-alpine基础镜像(≈150MB)                                                           |
| **3.8.2** | **Docker Compose 编排** | 开发/测试环境一键启动；MySQL/Redis/Nacos/RocketMQ/Seata/MinIO 7个基础容器 + N个应用容器的依赖编排                                                                | "docker-compose.yml中depends_on的含义？健康检查依赖怎么表达？"    | `docker-compose up -d` 一键启动全套开发环境(见技术方案文档第6章)                                                           |
| **3.8.3** | **Kubernetes 编排(进阶)** | Pod → ReplicaSet → Deployment(滚动升级)；Service(ClusterIP/NodePort/LoadBalancer)；ConfigMap/Secret(配置分离)；PV/PVC(持久化)                        | "Deployment的RollingUpdate策略怎么配置？Ready探针和存活探针的区别？" | 生产环境：每个服务Deployment(3副本) + ConfigMap(配置) + HPA(水平伸缩)                                                    |
| **3.8.4** | **三种发布策略对比**          | **滚动发布**(Rolling Update)：逐个替换Pod，零停机但有版本共存期；**蓝绿部署**(Blue-Green)：两套环境瞬间切换，资源翻倍但回滚快；**金丝雀发布**(Canary)：先放1%流量验证，逐步放量                     | "金丝雀发布怎么做到只让部分用户访问新版本？基于Header还是Weight？"          | 开发期：Rolling Update即可；首次上线：金丝雀(先内部测试)；稳定后：蓝绿(快速回滚能力)                                                     |
| **3.8.5** | **CI/CD 流水线**         | CI(Continuous Integration)：代码提交→编译→单元测试→构建镜像→推送仓库；CD(Continuous Delivery)：拉取镜像→部署到测试→自动化测试→生产发布                                        | "你们的CI/CD用了什么工具？Jenkins还是GitLab CI？"              | Git Push → GitHub Actions(或Jenkins) → Maven Build → Docker Build → Push Harbor → kubectl apply          |
| **3.8.6** | **优雅停机与健康检查**         | Spring Boot Actuator(`/actuator/shutdown` + `/actuator/health`)；preStop Hook(等待处理完现有请求)；K8s readinessProbe(就绪探针) + livenessProbe(存活探素) | "优雅停机的完整过程是怎样的？正在执行的请求怎么处理？"                      | `server.shutdown: graceful` + `spring.lifecycle.timeout-per-shutdown-phase: 30s` + K8s preStop sleep 5s |