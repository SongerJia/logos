|知识点|笔记时重点写什么|
|---|---|
|**8 种淘汰策略**|热 **必背清单**：`noeviction`（不淘汰，写满报错，默认）/ `allkeys-lru`（全局 LRU）/ `volatile-lru`（设有过期时间的 key 中 LRU）/ `allkeys-random` / `volatile-random` / `volatile-ttl`（剩余 TTL 最短的先淘汰）/ `allkeys-lfu`（4.0+，全局 LFU）/ `volatile-lfu`。**LRU vs LFU 的区别：LRU 看最近访问时间，LFU 看访问频率（更适合突发热点）**|
|**LRU 与 LFU 实现原理**|核 Redis 的 LRU 是**近似 LRU**（采样 `maxmemory-samples` 个 key 淘汰最久未访问的，默认采样 5 个，不是严格 LRU）。LFU 用 8bit 计数器（高 4bit 是访问频率对数，低 4bit 是衰减时间），**理解 LFU 的衰减机制（随时间降低频率）才能理解为什么它能更好处理突发热点**|
|**键过期删除策略**|热 **三种策略**：定时删除（设定时器，CPU 压力大，不推荐）/ 惰性删除（访问时检查是否过期，对内存不友好）/ **定期删除（默认每 100ms 抽查一批， Redis 实际用的方案）**。Redis 实际是**惰性删除 + 定期删除**组合使用|
|**内存碎片整理**|`activedefrag yes` 开启主动碎片整理（4.0+）。碎片率 = `used_memory_rss / used_memory`，> 1.5 就需要关注。整理原理：将占用连续内存的小键值对重新排列，释放碎片空间。**这是一个后台线程，不阻塞主线程**|
|**bigkey 大键问题**|热 定义：String > 10KB / Hash/List/Set/ZSet > 5000 个元素。**危害**：网络阻塞（DEL 大 key 阻塞主线程）、内存不均（Cluster 模式下某些 slot 数据过多）。**排查**：`redis-cli --bigkeys` / `MEMORY USAGE key`。**解决**：拆分（大 Hash 拆成多个小 Hash）/ 异步删除（`UNLINK` 命令，4.0+，后台线程删除）|
|**hotkey 热键问题**|热 定义：某个 key 访问频率极高（如热点新闻、秒杀商品）。**危害**：该 key 所在节点 CPU 飙高，成为瓶颈。**解决方案**：①本地缓存（JVM 级 Caffeine / Guava Cache）②备份（key 加上后缀 `_copy1` / `_copy2` 分散到不同节点）③读写分离（读从节点，但注意数据延迟）|




|#|知识点|重要度|三层笔记建议|面试追问|FlowPulse 结合|
|---|---|---|---|---|---|
|B4-19|**8种淘汰策略详解**|★★★|L1: noeviction(默认不淘汰返回错误) / allkeys-lru(所有key LRU) / volatile-lru(设置了TTL的key中LRU) / allkeys-lfu(4.0+, LFU) / volatile-lfu / allkeys-random / volatile-random / volatile-ttl(最快过期)；L2: LRU近似算法(随机采样N个key淘汰最旧的)而非真正LRU(节省内存)；3: LFU(Least Frequently Used) — freq counter(8bit,衰减因子counter_decay_time)|近似LRU和精确LRU差距多大？LFU的频率计数器怎么工作的？|**FlowPulse: allkeys-lru(流程缓存) + volatile-ttl(临时token)**|
|B4-20|**近似LRU算法实现**|★★☆|L1: 每个Object的lru字段记录上次访问时钟(24bit，精度秒级)；L2: maxmemory-samples采样数(默认5)——淘汰时随机抽N个key比较lru字段；3: 3.0+淘汰池(eviction pool，候选池大小16)——多次采样取最优淘汰|maxmemory-samples调大效果更好但CPU消耗增加？最佳值是多少？|调优Redis缓存命中率|
|B4-21|**LFU 算法细节**|★★☆|L1: freq counter(0-255)记录访问次数 + decay time(分钟级衰减)防止冷启动key一直驻留；2: 新key初始freq=5(最大值255的约2%)给新key机会；3: object-lfu-log-factor调整计数器增长速度(越大越难增长)|LFU vs LRU适用场景？冷启动问题怎么解决的？|热点数据缓存用LFU更合理|
|B4-22|**内存碎片率(mem_fragmentation_ratio)**|★★☆|L1: used_memory_rss / used_memory = 碎片率(>1.5说明碎片严重，<1说明用了SWAP)；L2: 碎片原因：Redis分配器(jemalloc/tcmalloc)的内存池管理策略；3: 清理方法：重启Redis(不推荐) / CONFIG set activedefrag yes(4.0+在线碎片整理)|activedefrag对性能影响？jemalloc和glibc malloc区别？|监控告警：mem_fragmentation_ratio > 1.5|
|B4-23|**BigKey 与 HotKey 问题**|★★★|L1: BigKey(>10KB)：导致阻塞(单个key操作是单线程)、网络拥塞、 uneven内存分布；2: HotKey：大量请求集中到一个key→击穿缓存打到DB；3: 治理：BigKey扫描(redis-cli --bigkeys)拆分/HotKey本地缓存+读写分离|BigKey的判断标准？怎么优雅删除BigKey(UNLINK异步)？|**FlowPulse防BigKey：流程定义JSON>1KB则压缩/流程实例列表分页存**|