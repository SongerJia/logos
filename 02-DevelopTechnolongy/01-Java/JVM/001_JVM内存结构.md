运行时数据区总览（线程共享/隔离）

热程序计数器 PC Register

热虚拟机栈 JVM Stack（栈帧结构）

热本地方法栈 Native Method Stack

核堆 Heap（年轻代/老年代/元空间）

热方法区/元空间 Metaspace

热栈帧：局部变量表 / 操作数栈 / 动态链接 / 返回地址

直接内存 Direct Memory

字符串常量池 StringTable 位置变迁（JDK7/8 变化）

运行时常量池 Runtime Constant Pool

对象在堆中的内存布局（对象头/实例数据/对齐填充）

|#|知识点|重要度|三层笔记建议|面试追问|FlowPulse 结合|
|---|---|---|---|---|---|
|B1-01|**程序计数器(PC Register)**|★★☆|L1: 线程私有，记录当前执行字节码行号；L2: 唯一不会OOM的区域，Native方法时值为undefined；L3: 为何需要？多线程上下文切换后恢复执行位置|Native方法PC值是多少？线程切换时PC怎么保存的？|FlowPulse工作流引擎多线程执行时每个线程独立PC|
|B1-02|**虚拟机栈(VM Stack)**|★★★|L1: 栈帧=局部变量表+操作数栈+动态链接+返回地址；L2: 局部变量表Slot复用、long/double占2个Slot；L3: StackOverflowError vs OOM触发条件差异(Xss参数)|Xss设1M能递归多深？局部变量表Slot复用怎么影响GC？|工作流节点递归调用深度限制设计|
|B1-03|**本地方法栈(Native Stack)**|★★☆|L1: 为Native方法服务(JVM调用的C/C++方法)；L2: HotSpot将VM Stack与Native Stack合二为一；L3: JNI调用链路中的栈帧变化|HotSpot为何合并两个栈？JNI性能开销在哪？|FlowPulse若集成Python脚本引擎涉及Native调用|
|B1-04|**Java堆(Heap)**|★★★|L1: 对象实例分配区域，GC主战场；L2: 分代思想(新生代/老年代)的理由——弱分代假说；L3: TLAB(Thread Local Allocation Buffer)分配机制与争用优化|为何分代？TLAB大小如何确定？对象分配流程完整路径|流程定义对象在Heap中的生命周期分析|
|B1-05|**方法区(Method Area)**|★★★|L1: 存储类信息/常量池/静态变量/JIT编译代码；L2: JDK7(永久代)→JDK8(元空间Metaspace)变迁原因；L3: Metaspace容量受什么限制？-XX:MetaspaceSize/-XX:MaxMetaspaceSize|为什么要移除永久代？字符串常量池JDK7/8/11的变化？|FlowPulse Nacos配置类加载到方法区的过程|
|B1-06|**运行时常量池(Runtime Constant Pool)**|★★☆|L1: 类文件常量池的运行时表示，包含符号引用；L2: 动态性——String.intern()运行期加入；L3: StringTable底层结构(JDK6数组→JDK7链表→JDK8+红黑树)|intern()在不同JDK版本行为差异？-XX:StringTableSize调优？|流程模板中大量固定字符串的intern优化|
|B1-07|**直接内存(Direct Memory)**|★★★|L1: 堆外内存，NIO ByteBuffer.allocateDirect()使用；L2: 避免Java堆→Native堆拷贝(零拷贝)，GC不管理但受MaxDirectMemorySize限制；L3: Unsafe.allocateMemory()实现原理 + Cleaner释放机制|Direct Memory会OOM吗？Netty的ByteBuf池化如何管理？|Netty通信模块直接内存使用场景|
|B1-08|**对象内存布局(Mark Word + Klass Pointer)**|★★★|L1: 对象头(Mark Word 8B + 类型指针 4/8B) + 实例数据 + 对齐填充；L2: Mark Word状态转换(无锁→偏向锁→轻量级锁→重量级锁)位图；L3: Compressed Oops(-XX:+UseCompressedOops)压缩指针原理|锁升级过程中Mark Word各阶段存储内容？开启指针压缩节省多少空间？|工作流实例对象内存占用估算|