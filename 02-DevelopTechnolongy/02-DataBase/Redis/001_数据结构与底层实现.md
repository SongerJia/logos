|知识点|笔记时重点写什么|
|---|---|
|**5 种基本类型 + 3 种高级类型**|热 基本：String / Hash / List / Set / ZSet。高级：Bitmap（位图，签到/活跃用户）、HyperLogLog（基数估算，UV 统计）、GeoSpatial（地理位置，附近的人）。**每个类型给一个典型业务场景**|
|**String 三种编码**|核 int（整数，8 字节有符号）/ embstr（短字符串 ≤44 字节，embedded，连续内存）/ raw（长字符串，SDS 动态字符串）。**44 字节是分界点（Redis 5.0+），面试常问为什么是 44**|
|**List 底层：QuickList**|Redis 3.2+ 使用 QuickList（ ziplist + 双向链表 的混合结构）。之前是 ziplist（小数据）+ linkedlist（大数据），现在统一为 QuickList。理解 ziplist 的紧凑存储是理解 Redis 内存优化的关键|
|**Hash 两种编码**|热 ziplist（小 Hash，field 数 ≤ `hash-max-ziplist-entries` 默认 512，value 长度 ≤ `hash-max-ziplist-value` 默认 64 字节）/ hashtable（超大时转成字典）。**这就是为什么 Hash 适合存对象（相比 String 省内存）**|
|**Set 两种编码**|intset（纯整数且元素少时，有序紧凑数组，查找 O(log n) 二分）/ hashtable（其它情况）。intset 的升级机制：int16 → int32 → int64，一旦升级不降级。**给一个元素添加导致编码转换的例子**|
|**ZSet 两种编码（最高频）**|热 ** skiing + dict**（元素 ≤ `zset-max-ziplist-entries` 默认 128，value 长度 ≤ `zset-max-ziplist-value` 默认 64）→ **skiplist + dict**（超阈值时）。**跳表的结构必须能手画**：多层有序链表，每一层是下一层的"快速通道"，查询 O(log n)。为什么用跳表不用平衡树？（范围查询方便、实现简单、内存友好）|
|**BitMap 与 HyperLogLog 原理**|BitMap：本质是 String（最大 512MB = 2^32 bit），`SETBIT` / `GETBIT` / `BITCOUNT`。HyperLogLog：基数估算算法，标准误差 0.81%，固定用 12KB 内存估算 2^64 个不同元素。**两者都是"用精度换空间"的典型**|
|**键与值的内存结构**|每个 Redis 键是一个 redisObject（16 字节：type 4bit + encoding 4bit + LRU 24bit + refcount 4B + ptr 8B）。理解这个结构才能理解为什么 `TYPE key` 能返回类型、为什么 `OBJECT ENCODING key` 能看到底层编码|

> **笔记技巧**：01 模块建议准备**两张核心图**：
> 
> 1. **ZSet 跳表结构图**（画出 3~4 层，标注 forward 指针和 span 跨度）
> 2. **编码转换决策树**（每个类型的两种编码，转换阈值是多少）
> 
> 这两张图能覆盖 Redis 底层面试题的 70%。


|#|知识点|重要度|三层笔记建议|面试追问|FlowPulse 结合|
|---|---|---|---|---|---|
|B4-01|**SDS (Simple Dynamic String)**|★★★|L1: Redis自定义字符串结构(len/free/buf[]/flags)——O(1)获取长度 + 二进制安全 + 杜绝缓冲区溢出；L2: SDS相比C字符串优势(常数复杂度len/自动扩容惰性释放/兼容部分C函数)；L3: embstr(<=44字节内联) vs raw(>44字节独立分配)两种编码|embstr和raw编码的边界为什么是44？SDS扩容策略(<1M翻倍/>1M+1M)？|**FlowPulse流程定义JSON缓存以SDS存储**|
|B4-02|**Linked List (双端链表)**|★★☆|L1: prev/next/value组成节点 + list(head/tail/len/dup/free)管理结构——O(1)头尾插入删除；L2: 特点：无环形无哨兵(不同于Linux内核list_head)；L3: 用于List数据类型底层 + Publish/Subscribe + 客户端输出缓冲|Redis链表和Java LinkedList区别？什么时候会用到底层链表？|发布订阅频道消息队列|
|B4-03|**Dictionary (Hash Table)**|★★★|L1: dict(dictht[2]两个哈希表 + rehashidx渐进式rehash标志)；L2: 哈希冲突链地址法(Singly Linked List) + SICM(渐进式rehash)逐步迁移；L3: 负载因子=used/size：>1触发扩容(×2)，<0.1触发缩容(rehash shrink)|rehash期间读写怎么处理？负载因子为什么阈值是1？|Hash结构的用户Session缓存|
|B4-04|**Skip List (跳跃表)**|★★★|L1: 有序集合(ZSET)底层实现之一——多层索引链表，平均O(logn)查找；L2: 插入时随机决定层数(概率1/4晋升一层)；L3: 相比红黑树：实现更简单/范围查询更快/同等时间复杂度但常量更优|为什么ZSET用SkipList而不用红黑树？查找过程画出来？|**FlowPulse延时任务排行榜(ZSET score=执行时间戳)**|
|B4-05|**IntSet (整数集合)**|★★☆|L1: int16/int32/int64 三种编码自动升级(不降级)；L2: 整数集合用于SET元素全为整数且数量较少时(intset_encoding)；L3: 升级规则：新元素超出当前编码则整体升级(重新分配内存+移动元素)|为什么IntSet不支持降级？升级的时间复杂度？|流程状态枚举缓存(小规模整数SET)|
|B4-06|**ZipList (压缩列表)**|★★★|L1: 连续内存块(zlbytes/zltail/zllen/entry.../zlend)——节省内存；L2: entry格式(prevlensize/encoding/content)，连锁更新问题(前置entry长度变化导致后续全部移动)；L3: 用于Hash/List/ZSET的小数据量场景(元素少且内容短)|连锁更新的最坏时间复杂度？ziplist配置 thresholds？|**FlowPulse短流程定义缓存(Hash小对象用ziplist)**|
|B4-07|**QuickList (Redis 3.2+)**|★★☆|L1: ZipList + LinkedList的混合体——每个节点是一个ZipList；L2: 解决纯ZipList元素过多时连锁更新严重的问题(分段)；L3: list-max-ziplist-size(-2)和list-compress-depth(0)参数|QuickList和ZipList的关系？为什么取代了LinkedList作为List底层的唯一实现？|流程消息列表(List类型)底层结构|