常用 JVM 启动参数（-Xms/-Xmx/-Xmn/-Xss/-XX 等）

热OOM 各场景排查（堆 OOM / Metaspace / 栈溢出 / 直接内存）

热jps / jstack / jmap / jstat / jinfo 命令详解

热VisualVM / MAT 内存分析工具使用

热CPU 飙高排查思路（jstack 找热点线程）

内存泄漏定位步骤（Dump → Histogram → Dominator Tree → GC Root）

频繁 Full GC 排查与优化方向

Arthas 在线诊断工具

常见 JVM 调优案例（大对象 / 缓存膨胀 / 类加载爆炸）

生产环境 JVM 参数配置最佳实践


|#|知识点|重要度|三层笔记建议|面试追问|FlowPulse 结合|
|---|---|---|---|---|---|
|B1-39|**OOM 分类与排查思路**|★★★|L1: Java heap space/Metaspace/Direct memory/GC overhead limit/Unable to create thread；L2: 各类OOM的根因特征(Heap dump分析/Metaspace泄漏/连接未释放)；L3: -XX:+HeapDumpOnOutOfMemoryError + MAT分析Histogram/Dominator Tree|OOM了先看哪几个指标？MAT的Shallow vs Retained Heap区别？|**FlowPulse高可用：OOM预防 + 自动Dump保留现场**|
|B1-40|**MAT(Memory Analyzer Tool)使用**|★★☆|L1: Histogram(按类分组) + Dominator Tree(对象支配树) + Leak Suspects(疑似泄漏报告)；L2: OQL(Object Query Language)查询特定对象；L3: Thread Overview分析线程持有的对象引用|怎么快速定位哪个对象撑爆了内存？ThreadOverview怎么找持有大对象的线程？|分析流程实例堆积导致的内存增长|
|B1-41|**常见内存泄漏模式**|★★★|L1: Static集合不断添加/未关闭的资源(Connection/Stream)/监听器未移除/ThreadLocal未remove；L2: 每种模式的检测方法和修复策略；L3: ThreadLocal泄漏的完整链路(Thread→ThreadLocalMap→Entry(key弱引用,value强引用)→value无法GC)|ThreadLocal为什么必须remove？Static Map做缓存有什么风险？|FlowPulse工作流上下文(WfContext)的ThreadLocal正确使用|
|B1-42|**JVM 调优方法论**|★★★|L1: 调优目的(响应延迟/吞吐量)；L2: 调优步骤：明确目标→测量基准→参数调整→持续监控；L3: "不要过早优化"原则 + 80/20法则(20%热点占80%资源)|调优第一步做什么？怎么衡量调优效果？|FlowPulse上线前JVM参数规划(Xms/Xmx/GC选择)|
|B1-43|**堆内存参数精解**|★★★|L1: -Xms/-Xmx(-XX:InitialHeapSize/-XX:MaxHeapSize)建议相等避免动态扩容；L2: -Xmn(NewGen比例) / -XX:SurvivorRatio(Eden:S0:S1)；L3: -XX:PretenureSizeThreshold(大对象直接进老年代)|NewGen设多大合适？SurvivorRatio调大会怎样？|流程实例对象大小评估 → 合理设置PretenureSizeThreshold|
|B1-44|**G1 调优关键参数**|★★★|L1: -XX:MaxGCPauseMillis(目标停顿200ms) / -XX:G1HeapRegionSize(2MB-32MB)；L2: -XX:InitiatingHeapOccupancyPercent(触发并发标记的堆占用比，默认45%)；L3: G1调优三板斧：停顿时间→Region大小→IHOP调整|G1 Full GC触发了说明什么？IHOP调大调小各有何影响？|**FlowPulse生产环境G1参数模板：-XX:MaxGCPauseMillis=200 -XX:G1HeapRegionSize=16m**|
|B1-45|**年轻代GC调优**|★★☆|L1: YGC频率过高→加大Young区或提高TenuringThreshold；L2: YGC后 Survivor利用率低→减小Survivor或降低TenuringThreshold；L3: 动态年龄判定(Promotion failed时年龄>Threshold的对象直接进入老年代)|YGC多久一次算正常？对象过早晋升老年代的危害？|流程临时对象的YGC频率监控|
|B1-46|**Full GC 触发原因与规避**|★★★|L1: 五大触发原因：System.gc()/老年代空间不足/CMS Concurrent Mode Failure/G1 Evacuation Failure/分配大对象；L2: 各自的解决方案；L3: -XX:+ExplicitGCInvokesConcurrent 让System.gc()走CMS并发周期|生产环境能禁止System.gc()吗？CMS Concurrent Mode Failure怎么解决？|**FlowPulse绝对要避免Full GC — 设置监控告警**|
|B1-47|**CPU Profile 与火焰图**|★★☆|L1: async-profiler采样生成火焰图(Flame Graph)；L2: 火焰图解读(on-CPU vs off-CPU)；L3: perf/JFR(Java Flight Recorder)替代方案|火焰图的宽度和颜色代表什么？off-CPU火焰图怎么分析I/O等待？|定位FlowPulse工作流执行引擎的CPU热点函数|
|B1-48|**Arthas 在线诊断**|★★★|L1: dashboard/watch/trace/thread/sc命令速查；L2: 反编译jad + 内存监控memory + 方法耗时profiler；L3: Arthas Tunnel远程诊断多实例|Arthas会影响线上性能吗？trace太深有什么副作用？|**FlowPulse线上故障排查首选工具 — trace工作流执行链路**|
|B1-49|**JVM 调优案例：一次Full GC排查**|★★★|L1: 背景(接口超时报警)→现象(Full GC频繁)→分析(MAT发现大对象)→根因(SQL返回过多数据未分页)→解决(加分页+SQL优化)；L2: 完整排查时间线和决策依据；L3: 如何建立防止回归的监控|如果MAT找不到明显的大对象怎么办？调优后如何验证有效？|用FlowPulse模拟一个完整的调优案例(流程查询接口OOM)|
|B1-50|**容器环境的JVM注意事项**|★★☆|L1: Docker容器中JVM看不到容器内存限制(默认看宿主机)；L2: JDK8u131+需加-XX:+UnlockExperimentalVMOptions -XX:+UseContainerSupport；L3: Kubernetes中Resource limits与JVM参数联动最佳实践|不加ContainerSupport会怎样？K8s request/limit和Xms/Xmx怎么配？|**FlowPulse K8s部署时JVM参数自动注入方案**|