  
IOC 与 DI 概念与区别

热BeanFactory vs ApplicationContext

热ApplicationContext 实现类对比（ClassPath/Annotation/FileSystem/Web）

核容器设计思想：工厂模式 / 单例模式 / 模板方法

BeanDefinition 概念与角色

HierarchicalFactory / Environment 抽象



|#|知识点|重要度|三层笔记建议|面试追问|FlowPulse 结合|
|---|---|---|---|---|---|
|B6-01|**IOC 容器层次结构**|★★★|L1: BeanFactory(基础容器，懒加载) vs ApplicationContext(高级容器，预实例化)；L2: ApplicationContext实现类：AnnotationConfigServletWebServerApplicationContext(Spring Boot默认)；L3: HierarchicalBeanFactory父子容器(Spring MVC的Root WebApplicationContext + ServletContext)|为什么要父子容器？BeanFactory和ApplicationContext的区别？|FlowPulse Spring Boot自动装配的Container|
|B6-02|**Bean 生命周期完整流程**|★★★|L1: 实例化(构造) → 属性填充 → 初始化(Aware/BeanPostProcessor.postProcessBeforeInitialization/@PostConstruct/InitializingBean.afterPropertiesSet/init-method) → BeanPostProcessor.postProcessAfterInitialization → 使用 → 销毁；L2: 每一步可扩展的点(MergedBeanDefinitionPostProcessor等)；L3: 循环依赖在"哪一步"被检测到的？|@Autowired和@Resource注入时机？BeanPostProcessor执行几次？|**FlowPulse自定义BeanPostProcessor做权限检查**|
|B6-03|**循环依赖三级缓存解决**|★★★|L1: singletonObjects(一级缓存，完整Bean) → earlySingletonObjects(二级缓存，早期暴露对象) → singletonFactories(三级缓存，ObjectFactory lambda)；L2: 流程：A创建→发现依赖B→B创建→发现依赖A→从三级缓存拿到A的工厂→放入二级缓存→B完成→A完成；3: 为什么需要三级缓存？(二级只能存对象，三级能存代理对象的factory——AOP代理需要延迟生成)|构造器循环依赖能解决吗？@Lazy能打破吗？prototype循环依赖呢？|**FlowPulse工作流节点间的循环引用场景**|
|B6-04|**BeanDefinition 加载与解析**|★★☆|L1: ConfigurationClassPostProcessor处理@Configuration/@ComponentScan/@Import/@Bean等注解；2. ComponentScanScanner扫描classpath→注册BeanDefinition到Registry；3. BeanDefinition包含：scope/lazyInit/initMethod/destroyMethod/autowireMode等元数据|@Configuration的full模式和lite模式区别？条件注解怎么生效的？|FlowPulse @ConditionalOnProperty 条件装配配置|
|B6-05|**@Autowired 注入原理**|★★★|L1: AutowiredAnnotationBeanPostProcessor.postProcessProperties()阶段处理；2. 解析策略：byType → byName → @Qualifier限定；3. required=false + Optional/Nullable处理|@Autowired和@Resource的区别？集合类型怎么注入？|FlowPulse服务层依赖注入全链路|
|B6-06|**FactoryBean 与 BeanFactory**|★★☆|L1: BeanFactory是IOC容器(生产Bean的工厂)，FactoryBean是特殊Bean(能生产另一种Bean的对象)；2. FactoryBean接口：getObject()/getObjectType() isSingleton()；3. 常见应用：Mybatis的SqlSessionFactoryBean、ProxyFactoryBean(AOP代理)|怎么获取FactoryBean本身而不是它生产的Bean？getObject()何时调用？|MyBatis-Plus集成FlowPulse时的Mapper代理创建|
|B6-07|**@Conditional 条件装配**|★★☆|L1: @Conditional(接受Condition接口实现) → @ConditionalOnProperty/OnClass/OnBean/OnMissingBean等派生注解；2. ConditionEvaluator.matches()在ConfigurationClass解析阶段评估；3. Spring Boot Auto-Configuration的核心机制(自动装配的条件判断)|自定义Condition怎么写？条件匹配顺序？|FlowPulse多环境配置切换(Datasource/Nacos地址)|
|B6-08|**Profile 与环境抽象**|★☆☆|L1: @Profile("prod") 根据激活的profile决定是否加载Bean；2. Environment接口：PropertySource链(系统环境→应用配置→命令行覆盖)；3. @ConfigurationProperties类型安全绑定|