ConcurrentHashMap 1.7 分段锁 vs 1.8 CAS+synchronized

热ConcurrentHashMap put 全流程源码

CopyOnWriteArrayList 写时复制原理

ConcurrentLinkedQueue 无锁 CAS 队列

热BlockingQueue 生产者消费者模型

DelayQueue 定时延迟队列

ConcurrentSkipListMap 跳表有序 Map



|#|知识点|重要度|三层笔记建议|面试追问|FlowPulse 结合|
|---|---|---|---|---|---|
|B2-26|**ConcurrentHashMap 1.7 vs 1.8 架构差异**|★★★|L1: 1.7: Segment分段锁(16个Segment继承ReentrantLock) + HashEntry链表；1.8: Node数组 + CAS+synchronized(只锁首节点)+红黑树(链表>8)；L2: 1.8为何弃用Segment？(粒度更细、锁竞争更低、内存占用更少)；L3: size()实现：1.7累加Segment.count(弱一致) / 1.8 CounterCell + baseCount(sumCount())|1.8为什么用synchronized而不是ReentrantLock？红黑树阈值为什么是8？|**FlowPulse全局流程定义缓存用ConcurrentHashMap**|
|B2-27|**ConcurrentHashMap 扩容机制**|★★★|L1: 多线程协助扩容——ForwardingNode标识已迁移桶，其他线程帮忙迁移；L2: transfer()过程：逆序迁移(减少头插法冲突) + 分块stride分配；L3: sizeCtl控制扩容：-(1+N)表示N个线程在扩容|扩容时还能put/get吗？ForwardingNode的作用？|高流量下ConcurrentHashMap扩容对FlowPulse的影响|
|B2-28|**CopyOnWriteArrayList**|★★☆|L1: 写时复制——修改时复制整个底层数组，读无锁；L2: 适用场景：读多写极少(如监听器列表/配置列表)；L3: 弱点：数据一致性(读到旧数据)、内存开销(每次写复制全量数组)、只能保证最终一致性|迭代器是强一致还是弱一致？写操作的性能问题怎么缓解？|FlowPulse流程监听器列表(注册后很少变更)|
|B2-29|**BlockingQueue 实现对比**|★★☆|L1: ArrayBlockingQueue(单锁+Condition) / LinkedBlockingQueue(双锁+两Condition) / PriorityBlockingQueue(堆排序) / DelayQueue(延迟元素) / SynchronousQueue(零容量)；L2: put/take的阻塞唤醒机制(Condition.await/signal)；L3: LinkedBlockingQueue的两把锁(takeLock/putLock)分离读写|哪个 BlockingQueue 吞吐最高？DelayQueue底层怎么实现的？|**FlowPulse延迟任务队列用DelayQueue / 任务缓冲用LinkedBlockingQueue**|
|B2-30|**ConcurrentLinkedQueue / ConcurrentSkipListMap**|★☆☆|L1: 无锁并发队列(CAS+自旋实现)；ConcurrentSkipListMap跳表(跳表O(logn)查找)；L2: ConcurrentLinkedQueue的head/tail更新松弛(relaxed)策略——允许短暂不一致提升吞吐；L3: SkipListMap vs ConcurrentHashMap：前者有序+支持范围查询，后者无序但更快|无锁队列为什么比有锁快？跳表和红黑树的优劣？|有序流程任务调度可用ConcurrentSkipListMap|
|B2-31|**ThreadLocal 原理与内存泄漏**|★★★|L1: ThreadLocalMap以ThreadLocal为key(WeakReference)、value为强引用存储在线程对象内；L2: 泄漏原因：ThreadLocal销毁但Thread还在→key(null)无法GC(value强引用链不断)；L3: 解决：finally{threadLocal.remove()} 或使用TransmittableThreadLib(TTL)跨线程池传递|ThreadLocal的key为什么设计成弱引用？InheritableThreadLocal的问题？|**FlowPulse WfContext(用户上下文/租户ID/TraceId)通过ThreadLocal传递**|