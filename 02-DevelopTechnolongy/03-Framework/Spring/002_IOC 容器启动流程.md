容器启动核心步骤概览（refresh() 十二大步）

核BeanDefinition 加载与注册（扫描 @Component 过程）

核Bean 实例化流程（doCreateBean / createBeanInstance / populateBean / initializeBean）

核三级缓存解决循环依赖（singletonFactories / earlySingletonObjects / singletonObjects）

热后置处理器执行顺序汇总

事件发布机制 ApplicationEvent / @EventListener

国际化 MessageSource 与资源加载 ResourceLoader

Environment 与 PropertySource 配置源抽象