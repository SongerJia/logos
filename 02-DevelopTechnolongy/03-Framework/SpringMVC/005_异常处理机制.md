@ControllerAdvice + @ExceptionHandler 全局异常处理

热异常处理的优先级匹配规则（精确匹配 > 继承关系最近）

ResponseEntityExceptionHandler 与 DefaultHandlerExceptionResolver

@ResponseStatus 自定义 HTTP 状态码

统一返回结果封装 R模式设计

核Spring MVC 异常处理链路（resolveException → ExceptionHandlerExceptionResolver）