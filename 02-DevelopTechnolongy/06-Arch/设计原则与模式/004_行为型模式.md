|#|模式|核心结构|关键要点|面试追问|FlowPulse 实战|
|---|---|---|---|---|---|
|**1.4.1**|**策略 Strategy**|定义算法族，使它们可以互相替换|策略 vs 状态的区别(算法选择 vs 状态变迁)；Context持有Strategy引用|"Comparator是策略模式吗？"|**★ 自适应节点调度算法**：根据节点类型选SYNC/ASYNC_THREAD/ASYNC_QUEUE/CACHED_EVAL|
|**1.4.2**|**模板方法 Template Method**|父类定义算法骨架，子类实现步骤|钩子方法(Hook Method)；好莱坞原则体现；final约束骨架|"AbstractQueuedSynchronizer(AQS)用了模板方法吗？"|**★ 流程引擎主流程**：ProcessEngine.execute()定义骨架，各Handler实现细节|
|**1.4.3**|**观察者 Observer / 发布订阅**|一对多依赖，状态变化自动通知所有依赖者|观察者 vs 发布订阅(耦合度差异)；推模型 vs 拉模型|"Spring Event和消息队列有什么关系？"|**★ 工作流事件发布**：TaskCreatedEvent → MessageService监听 → 发通知 / 监听器模式|
|**1.4.4**|**责任链 Chain of Responsibility**|将请求沿链传递，直到有人处理|Filter/Interceptor链；链的中断与继续；顺序控制|"Servlet Filter、Spring Interceptor、Netty Pipeline都是责任链？"|**★ Gateway Pipeline**：认证→限流→路由→日志，逐级处理HTTP请求|
|**1.4.5**|**迭代器 Iterator**|提供一种方法顺序访问聚合元素，不暴露内部实现|fail-fast vs fail-safe；Iterable/Iterator接口契约|"foreach底层是怎么工作的？"|分页查询结果遍历 / MyBatis游标Cursor|
|**1.4.6**|**命令 Command**|将请求封装为对象，支持撤销/重排/延迟|Command vs Strategy的区别(整体操作 vs 算法)；事务回滚 = 撤销|"Runnable是命令模式吗？"|任务调度：JobInfo封装成Command对象交给Executor执行|
|**1.4.7**|**状态 State**|对象状态改变时行为也跟着变|状态模式 vs 策略模式(状态机 vs 算法选择)；有限状态机(FSM)|"流程实例的状态流转用什么模式？"|**★ 流程实例状态机**：RUNNING→COMPLETED/CANCELLED/SUSPENDED / 任务PENDING→COMPLETED|
|**1.4.8**|**中介者 Mediator**|封装对象间交互，降低耦合|中介者 vs 观察者的区别(中心协调 vs 广播通知)|"MVC的Controller是中介者吗？"|**流程引擎(ProcessEngine)** 作为各Node之间的中介者协调推进|
|**1.4.9**|**备忘录 Memento**|捕获并恢复对象的内部状态，支持撤销|命令模式常配合备忘录实现undo；宽接口问题|"数据库的Undo Log是备忘录模式吗？"|**wf_history_activity表**：记录每次状态变更前的变量快照(variables_before/after)|
|**1.4.10**|**访问者 Visitor**|将算法与对象结构分离，在不修改结构的前提下定义新操作|双分派(Double Dispatch)；增加新操作容易 vs 增加新对象困难|"Element.accept(Visitor)为什么叫双分派？"|流程定义导出(ExportVisitor)：同一套节点结构 → 导出JSON/PDF/SVG|
|**1.4.11**|**解释器 Interpreter**|给定语言定义文法表示，并定义解释器|组合模式+递归；实际应用场景有限(规则引擎/SQL解析)|"Spring SpEL解释器怎么实现的？"|**流程条件表达式解析**：`${days > 3 && dept == 'tech'}` 的求值引擎|
|**1.4.12**|**空对象 Null Object**|返回空对象代替null，消除null检查|Optional vs NullObject；NPE防护|"Optional.ofNullable和NullObject的区别？"|默认角色分配 / 空流程变量Map初始化|
|**1.4.13**|**模式协作案例：工作流引擎**|策略+模板+观察者+责任链+状态+中介者 的综合运用|"你能说清楚这些模式在引擎里各自扮演的角色吗？"|**这是面试核心亮点！** 见下方详细对照表 ↓||