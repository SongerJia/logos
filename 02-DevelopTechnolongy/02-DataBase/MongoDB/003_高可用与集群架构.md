|序号|知识点|笔记写什么|重要度|
|---|---|---|---|
|M3.1|副本集（Replica Set）架构|Primary + Secondary + Arbiter 三角色；选举过程(Raft协议)；读写分离(readPreference)；写入传播(oplog)|🔥🔥🔥|
|M3.2|Oplog 与数据同步机制|capped collection（固定大小循环写）；Initial Sync 全量同步 + Incremental 增量同步；oplog 大小配置建议|核|
|M3.3|分片集群（Sharded Cluster）|mongos 路由 + config server 配置 + shard 分片节点；chunk（默认64MB）& balancer 自动均衡；分片键(ShardKey)选择策略|核|
|M3.4|存储引擎 WiredTiger|文档级锁（vs MMAPv1集合级锁）；压缩(Snappy/Zstd)；CheckPoint机制；Cache（WiredTiger Cache vs filesystem cache）|一般|

**📌 架构追问**：什么场景下选 MongoDB 而不是 MySQL？什么场景下反过来？画一张技术选型决策树。