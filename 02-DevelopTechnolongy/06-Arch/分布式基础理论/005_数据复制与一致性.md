|#|知识点|核心内容|面试追问|FlowPulse / 中间件|
|---|---|---|---|---|
|**2.5.1**|**主从复制 Master-Slave**|写Master → 同步/异步复制到Slave → 读可从Slave分担；半同步(至少1个Slave确认)平衡性能与可靠性|"MySQL主从复制的三种binlog格式区别？GTID解决了什么问题？"|**MySQL主从架构**：读写分离减轻读压力 / Redis主从|
|**2.5.2**|**多主复制 Multi-Master**|任意节点都可写；节点间双向复制；冲突解决是核心难题(最后写入胜出/版本向量/CRDT)|"多主复制最大的挑战是什么？冲突有哪些解决策略？"|CockroachDB / MongoDB Replica Set / Galera Cluster(MySQL多主)|
|**2.5.3**|**去中心化复制 (Dynamo风格)**|无中心节点；一致性哈希分片；Gossip协议同步；NWR模型调优；Hinted Handoff|"Dynamo论文的核心设计思想？Amazon DynamoDB和Dynamo的关系？"|Cassandra / Riak / Redis Cluster(去中心化+Gossip)|
|**2.5.4**|**Quorum 机制 NWR**|N = 副本总数，W = 写副本数，R = 读副本数；W+R > N 保证读到最新写入|"N=3,W=2,R=2 能保证强一致吗？W+R>N的条件？"|**Seata全局锁的Quorum思想**：多数节点确认才提交|
|**2.5.5**|**Read-Repair & Anti-Entropy**|Read-Repair：读时发现不一致立即修复(惰性)；Anti-Entropy：后台定期Merkle Tree比对修复(主动)|"Merkle Tree是什么？怎么高效比较两个节点的数据差异？"|Cassandra / Dynamo 风格存储的后台修复机制|
|**2.5.6**|**一致性级别全景图谱**|Strong(线性一致性) → Sequential Causal Eventual Weak(最终一致) → 6级逐级放宽|"你们项目用了哪种一致性级别？举例说明？"|**FlowPulse一致性矩阵** ↓|

```
┌──────────────────┬──────────────┬──────────────────────────────┐
│     数据类型       │  一致性要求    │         FlowPulse 实现方式       │
├──────────────────┼──────────────┼──────────────────────────────┤
│ 用户权限变更       │  最终一致     │ 权限变更→清Redis缓存→下次请求刷新  │
│ 流程状态推进       │  强一致       │ Seata AT 本地事务+全局锁          │
│ 流程变量更新       │  因果一致     │ 引擎串行推进→天然有序             │
│ 消息通知发送       │  最终一致     │ MQ异步投递→重试+补偿              │
│ 文件上传元数据     │  最终一致     │ MinIO写入→DB记录→Canal同步ES     │
│ 调度任务执行日志   │  最终一致     │ Executor回调上报→异步写入DB       │
│ 操作审计日志       │  强一致       │ AOP拦截→同步写入DB(事务内)        │
│ 在线用户Session   │  最终一致     │ Redis心跳续期→过期自动清理         │
└──────────────────┴──────────────┴─────────────────────────
```