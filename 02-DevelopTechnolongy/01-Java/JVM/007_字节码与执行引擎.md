class 文件结构（魔数/版本号/常量池/访问标志等）

核字节码指令集基础（加载/存储/运算/类型转换/控制流/对象操作）

核解释执行 vs JIT 即时编译（C1/C2 编译器）

分层编译 TieredCompilation

热点代码探测（方法调用计数器 / 回边计数器）

编译优化技术（内联 / 逃逸分析 / 标量替换 / 锁消除）

JIT 编译阈值与 -XX 参数调优

AOT 编译 GraalVM / Jaotc


|#|知识点|重要度|三层笔记建议|面试追问|FlowPulse 结合|
|---|---|---|---|---|---|
|B1-27|**解释器 vs 编译器分层编译**|★★★|L1: 解释器(启动快)+ C1 Client Compiler(简单优化)+ C2 Server Compiler(深度优化)；L2: 分层编译(Tier0-4)：解释→C1→C2的热度晋升机制(InvocationCounter + BackEdgeCounter)；L3: -Xmixed(默认) vs -Xint vs -Xcomp 三种模式性能差异|什么叫逃逸分析？C2的内联条件是什么？热点阈值怎么调整？|高频执行的流程引擎核心路径会被C2编译优化|
|B1-28|**即时编译优化技术**|★★☆|L1: 方法内联(Inlining)是最重要的优化；L2: 逃逸分析(Escape Analysis)→标量替换/栈上分配/锁消除；L3: OSR(On Stack Replacement)栈上替换——循环体热点编译|内联失败的场景有哪些？逃逸分析后对象会在栈上吗？|工作流节点执行方法的内联优化可能性|
|B1-29|**Code Cache 与 JIT 回退**|★☆☆|L1: 编译后的本地代码存放在Code Cache；L2: ReservedCodeCacheSize不足时JIT停止编译回退到解释模式(严重性能降级)；L3: -XX:+PrintCompilation 观察编译活动|Code Cache满了会怎样？怎么监控Code Cache使用率？|大型微服务项目注意Code Cache调大|
|B1-30|**AOT(Ahead-of-Time)编译 GraalVM**|★★☆|L1: 运行前将字节码编译为本地机器码(graalvm native-image)；L2: 启动速度提升(秒级)、内存降低(无需JVM)，但封闭世界假设限制反射；L3: Spring Native(AOT)对FlowPulse这类Spring Cloud应用的适配挑战|AOT和JIT的本质区别？GraalVM native-image的局限性？|FlowPulse未来容器化部署可考虑AOT镜像缩小体积|
|B1-31|**JIT编译指标观测**|★☆☆|L1: -XX:+PrintAssembly打印汇编(hsdis插件)；L2: jstat -compiler查看编译统计；L3: JITWatch可视化分析热点方法|如何找到最耗CPU的方法？反编译结果怎么看？|性能瓶颈定位工具链|