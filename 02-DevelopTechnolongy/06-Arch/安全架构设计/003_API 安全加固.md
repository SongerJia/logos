|#|知识点|核心手段|实现方式|面试追问|FlowPulse 实现|
|---|---|---|---|---|---|
|**6.3.1**|**HTTPS/TLS 全链路加密**|Nginx SSL终止 → 内网HTTP通信；TLS 1.2+/1.3；证书管理(Let's Encrypt/自签CA)|TLS握手过程：ClientHello → ServerHello+证书 → 密钥交换(ECDHE) → 对称加密通信|"ECDHE和RSA密钥交换的区别？为什么推荐ECDHE？"|Nginx配置SSL + Let's Encrypt自动续期；内部集群HTTP(内网隔离已足够)|
|**6.3.2**|**请求签名防篡改**|对敏感接口做HMAC-SHA256签名：`sign = HMAC(key, method + url + timestamp + body)`；服务端验签防止中间人篡改参数|API Key + Secret分发；Timestamp防重放(±5分钟窗口)|"签名和HTTPS不冲突吗？什么时候需要额外的签名？"|开放API接口(如Webhook回调)使用HMAC签名验证|
|**6.3.3**|**接口幂等与防重放**|幂等Key(Idempotency-Key Header) + Redis去重 + 请求时间戳校验(Nonce + Timestamp)|`Idempotency-Key: uuid` → Redis SETNX EX 600s → 重复请求直接返回缓存结果|"前端点了两次提交按钮怎么办？后端怎么防重放？"|所有POST写操作接口强制要求 `X-Idempotency-Key` Header 或前端表单防重复提交|
|**6.3.4**|**参数校验与输入过滤**|Spring Validation(@NotNull/@Pattern/@Size等) + 白名单过滤(只允许合法字符) + SQL注入防护(MyBatis参数化#{})|XSS防御(输出编码HtmlUtils.htmlEscape) + CSRF(Token同步模式)|"MyBatis的#{}和区别？为什么区别？为什么{}有SQL注入风险？"|全局异常处理 + 统一参数校验注解 + MyBatis严格使用`#{}`|