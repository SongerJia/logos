| 序号   | 知识点              | 笔记写什么                                                                                                 | important          |
| ---- | ---------------- | ----------------------------------------------------------------------------------------------------- | ------------------ |
| P2.1 | JSONB 类型与操作      | JSON（存原文）vs JSONB（二进制解析后存）；@> 包含查询 / ? 存在性查询 / jsonb_path_query (JSONPath)；GIN索引加速；vs MySQL JSON类型的对比 | 🔥🔥🔥 **最常用高级特性** |
| P2.2 | GIN 倒排索引         | 原理：posting list（倒排列表）；适用全文搜索(tsvector) / 数组包含 / JSONB查询；GiST索引对比；与 ES 的边界                             | 核                  |
| P2.3 | 窗口函数与分析计算        | ROW_NUMBER() / RANK() / DENSE_RANK() / LAG() / LEAD() / OVER(PARTITION BY ... ORDER BY ...)；复杂分析报表场景  | 热                  |
| P2.4 | CTE（WITH子句）与递归查询 | WITH recursive 公式表(递归CTE)实现层级查询（组织树/评论楼）；vs 子查询的性能优势（执行计划优化）                                          | 核                  |
| P2.5 | 扩展生态（Extensions） | postgis（地理信息）/ pg_stat_statements（SQL统计）/ citus（分布式）/ timescaledb（时序数据）；CREATE EXTENSION 安装使用         | 热                  |