Controller 返回值类型（String / ModelAndView / void / Object/@ResponseBody）

热@ResponseBody 与 @RestController 原理

热视图解析器 ViewResolver（InternalResourceViewResolver / Thymeleaf / FreeMarker）

内容协商 ContentNegotiation（根据 Accept 头选择返回格式）

转发 forward vs 重定向 redirect（前缀 "forward:" / "redirect:"）

异常处理：@ControllerAdvice + @ExceptionHandler