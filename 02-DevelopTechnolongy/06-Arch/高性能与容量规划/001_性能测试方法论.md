|#|知识点|核心内容|工具/方法|面试追问|FlowPulse 实践|
|---|---|---|---|---|---|
|**5.1.1**|**性能测试分类**|基准测试(单接口极限) → 负载测试(逐步加压找到拐点) → 压力测试(超过容量看系统行为) → 稳定性测试(长时间运行验证内存泄漏) → 峰值测试(突发流量冲击)|JMeter / Wrk / Gatling / Apache Bench(ab)|"负载测试和压力测试的区别？为什么要做稳定性测试？"|**流程引擎压测计划**：基准(单流程启动延迟) + 负载(QPS递增找拐点) + 稳定(24h运行查内存泄漏)|
|**5.1.2**|**JMeter 压测实战**|Thread Group(线程数/Ramp-Up/循环次数) → Sampler(HTTP/JDBC/MQ请求) → Listener(聚合报告/图形结果/响应时间图) → 断言(校验返回正确性)|关键指标：Throughput(吞吐量)、Avg RT、P90/P95/P99、Error%、吞吐量-响应时间曲线|"JMeter的Ramp-Up Period设多少合适？怎么模拟真实用户场景？"|压测FlowPulse核心接口：`POST /api/v1/workflow/start` / `GET /api/v1/workflow/my-tasks`|
|**5.1.3**|**压测数据准备**|生产级压测需要**足够的数据量**：表行数、数据分布(热点数据比例)、参数化(不同用户/不同流程模板)；避免"缓存命中100%"的假象|CSV Data Set Config / User Defined Variables / 随机数生成器|"压测时缓存命中率100%说明什么？是好事还是坏事？"|准备100个用户 × 50个流程定义 × 10万条历史实例数据|
|**5.1.4**|**性能目标设定 (SLI/SLO)**|根据业务需求设定可量化的性能目标；核心接口 P99 < 200ms / 吞吐量 > 500 QPS / 错误率 < 0.1% / CPU < 70% @峰值|USE方法(Utilization/Saturation/Error) + RED方法(Rate/Errors/Duration)|"怎么确定合理的性能目标？拍脑袋还是基于什么依据？"|**FlowPulse 性能基线(V1.0)** ↓|

> **📊 FlowPulse 核心接口性能基线**

```
┌──────────────────────────┬───────┬──────────┬──────────┬──────────┬──────────┐
│ 接口                       │ 目标QPS│ P50(ms)  │ P99(ms)   │ 错误率    │ 备注      │
├──────────────────────────┼───────┼──────────┼──────────┼──────────┼──────────┤
│ POST /auth/login          │  200  │   30     │  150     │  <0.01%  │ 含BCrypt  │
│ GET /workflow/list        │  500  │   20     │  100     │  <0.05%  │ 分页查询   │
│ POST /workflow/start      │  300  │   50     │  200     │  <0.1%   │ 引擎核心!  │
│ POST /task/complete       │  400  │   40     │  150     │  <0.1%   │ 任务完成   │
│ GET /workflow/instance/:id│  800  │   15     │   80     │  <0.05%  │ 缓存优先   │
│ WS /realtime/push         │ 1000  │   10     │   50     │  <0.01%  │ Netty长连接 │
│ POST /file/upload         │  100  │  200     │ 2000     │  <0.5%   │ 大文件上传  │
│ GET /schedule/jobs        │  200  │   25     │  120     │  <0.05%  │ 管理后台   │
└──────────────────────────┴───────┴──────────┴──────────┴──────────┴──────────┘

压测环境: 3节点 K8s (4C8G each) + MySQL MGR + Redis Sentinel + RocketMQ 2M2S
```