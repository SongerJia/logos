@SpringBootApplication 组合注解拆解

热@EnableAutoConfiguration 工作原理

热spring.factories 与 AutoConfigurationImportSelector 加载机制

核@Conditional 系列条件注解（@ConditionalOnClass / @ConditionalOnBean / @ConditionalOnProperty / @ConditionalOnMissingBean 等）

核自动配置类的加载流程（候选 → 过滤 → 匹配 → 注册）

热如何排查自动配置是否生效（--debug / conditions report）

@AutoConfigureBefore / @AutoConfigureAfter / @AutoConfigureOrder

自定义 Starter 的自动配置类编写规范



| #     | 知识点                          | 重要度 | 三层笔记建议                                                                                                                                                                                                                            | 面试追问                                                         | FlowPulse 结合                                       |
| ----- | ---------------------------- | --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------ | -------------------------------------------------- |
| B6-14 | **SPI 机制与 spring.factories** | ★★★ | L1: META-INF/spring.factories(旧版) / META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports(新版)；2. SpringFactoriesLoader.loadFactoryNames()扫描所有jar的spring.factories；3. EnableAutoConfiguration注解触发自动装配流程 | spring.factories和Java SPI(service loader)的区别？自定义Starter怎么开发？ | FlowPulse自定义Starter(flowpulse-spring-boot-starter) |
| B6-15 | **自动装配流程**                   | ★★☆ | L1: @EnableAutoConfiguration → AutoConfigurationImportSelector.selectImports() → 加载所有候选配置类 → @Conditional过滤 → 注册BeanDefinition；2. 配置元数据文件(spring-configuration-metadata.json)提供IDE提示；3. auto-configuration report(--debug查看启用了哪些) | 怎么排除某个自动装配？@SpringBootApplication(exclude=...)？              | FlowPulse Starter的自动装配类编写                          |
| B6-16 | **Embedded Server 启动**       | ★☆☆ | L1: TomcatEmbeddedServletContainerFactory(或Jetty/Undertow)创建内嵌Servlet容器；2. ServletWebServerApplicationContext.onRefresh()启动Web服务器；3. 端口随机分配(server.port=0) + 健康检查端点                                                               | 怎么换成Jetty？Tomcat线程池怎么配？                                      | FlowPulse内嵌Tomcat配置优化                              |
| B6-17 | **Spring Boot 3.x 变化**       | ★☆☆ | L1: Jakarta EE 9+ (javax.* → jakarta.*) / Java 17最低要求 / GraalVM Native Image支持；2. MicrometerObservation自动观测性 / 虚拟线程(Project Loom)初步支持；3. 从javax迁移到jakarta的兼容性问题                                                                   | Spring Boot 2.x能直接升级到3.x吗？虚拟线程对Web应用的提升                      |                                                    |