|#|方案/知识点|核心原理|优缺点对比|面试追问|FlowPulse 应用|
|---|---|---|---|---|---|
|**2.2.1**|**UUID**|128位随机数(v4)或基于时间+MAC(v1)；Java `UUID.randomUUID()`|✅ 简单无依赖 ❌ 36字符太长/无序导致B+树分裂/索引性能差|"UUID做主键有什么问题？MySQL InnoDB为什么不喜欢它？"|不用于数据库主键；可用于Token(jti)/TraceId等非索引字段|
|**2.2.2**|**数据库自增步长**|每个DB实例分配不同步长(如1,3,5和2,4,6)；`auto_increment_increment + auto_increment_offset`|✅ 简单实现 ❌ 步长暴露数据量/难扩容/ID不连续|"步长方案如果需要新增第3个数据库怎么办？"|备用方案；不适合FlowPulse高并发场景|
|**2.2.3**|**Redis INCR**|Redis原子递增 `INCR key`；可配批量获取减少RTT|✅ 性能好 ❌ 依赖Redis持久化/重启丢失/单点瓶颈|"Redis集群怎么做全局递增？"|可用于流程实例编号(PI前缀+序号)的序号部分|
|**2.2.4**|**Snowflake 雪花算法 ★★★**|64bit = 1符号位 + 41位时间戳(ms,69年) + 10位机器ID(1024节点) + 12位序列号(ms内4096个)|✅ 趋势递增/高性能/无需协调 ❌ 依赖系统时钟(时钟回拨问题)|"时钟回拨怎么解决？美团Leaf是怎么处理的？"|**★ 全局主键生成策略首选！** 用户ID/流程实例ID/任务编号都用Snowflake变种|
|**2.2.5**|**号段模式 (Segment)**|从DB预分配一个号段(e.g., [1000, 2000])放内存用，用完再取下一个段；Leaf-segment / VUidGenerator|✅ 吞吐极高(内存分配) ❌ ID不连续/段尾浪费/重启丢未用ID|"号段模式的Double Buffer是什么意思？"|高并发场景备选；比Snowflake更简单但信息量少|
|**2.2.6**|**UID Generator 选型决策框架**|综合对比维度：唯一性/有序性/吞吐量/长度/可用性/复杂度/可维护性；按场景选型|不同场景的最优解不同|"你们项目用什么方案？为什么选它？"|FlowPulse选型结论表 ↓|

```
┌─────────────┬──────────┬──────────┬──────────┬──────────────┐
│   使用场景    │ 推荐方案  │   原因   │ FlowPulse位置│   格式示例   │
├─────────────┼──────────┼──────────┼──────────┼──────────────┤
│ 数据库主键   │ Snowflake│ 趋势递增  │ 所有表自增ID │ 123456789012│
│ 流程实例编号 │ Redis+前缀│ 可读性好  │ instance_no │PI20260610001│
│ 任务编号     │ Snowflake│ 全局唯一  │ task_no      │T1686384000001│
│ Token JTI    │ UUID     │ 安全随机  │ JWT Payload  │550e8400-e29b│
│ TraceId链路  │ UUID/Snow│ 透传方便  │ SkyWalking    │abc123def456 │
│ 消息唯一Key  │ 业务组合  │ 幂等判断  │ MQ MessageKey │order:10086│
└─────────────┴──────────┴──────────┴──────────┴────────────
```