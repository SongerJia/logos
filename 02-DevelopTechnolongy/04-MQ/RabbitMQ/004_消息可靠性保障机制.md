| 序号  | 知识点                      | 笔记写什么                                                                                                                        | important                 |
| --- | ------------------------ | ---------------------------------------------------------------------------------------------------------------------------- | ------------------------- |
| 4.1 | 生产者确认(Publisher Confirm) | 异步确认方式：普通Confirm / 等待所有confirm waitForConfirms / 异步监听addConfirmListener（推荐）；mandatory参数（无法路由时return给生产者）；**三步组合达到生产端100%可靠** | 🔥🔥🔥🔥 **RabbitMQ王牌功能** |
| 4.2 | 消费者手动ACK                 | autoAck=false（手动确认）/ basicAck（成功）/ basicNack（失败，可重入队列）/ basicReject（拒绝单条）；multiple参数含义（批量确认）；**为什么不要用自动ACK**                 | 🔥🔥🔥🔥                  |
| 4.3 | 持久化三层保障                  | Exchange持久化(durable=true) + Queue持久化(durable=true) + Message持久化(delivery_mode=2)；三者缺一不可的关系；持久化对性能的影响（磁盘IO）                   | 🔥🔥🔥                    |
| 4.4 | 消费端限流(QoS)               | basicQos(prefetchCount) 设置；一次推送多少条消息给消费者再等ACK；防止消费者被压垮；prefetchCount=1的含义和影响                                                 | 🔥🔥🔥                    |
| 4.5 | 幂等性与去重                   | 与RMQ一样的问题——网络抖动导致重复投递；解决方案相同：唯一键 / DB唯一索引 / Redis Set；RabbitMQ自身不提供内置去重                                                      | 核                         |