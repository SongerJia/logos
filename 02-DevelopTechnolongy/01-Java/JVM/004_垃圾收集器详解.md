  
Serial 收集器（Stop-The-World 特点）

热Parallel Scavenge / Parallel Old（吞吐量优先）

核CMS 并发标记清除（三色标记 / 浮动垃圾 / Concurrent Mode Failure）

核G1 Garbage First（Region 划分 / RSet / CSet / Young/Mixed GC）

ZGC（染色指针 / 读屏障 / Load Barrier）

Shenandoah（Brooks Pointer）

收集器的组合规则与选择策略

各收集器适用场景总结对比表

|#|知识点|重要度|三层笔记建议|面试追问|FlowPulse 结合|
|---|---|---|---|---|---|
|B1-09|**引用计数 vs 可达性分析**|★★★|L1: 引用计数(循环引用问题) vs 可达性分析(GC Roots枚举)；L2: 四种引用类型(Strong/Soft/Weak/Phantom)及使用场景；L3: GC Roots具体包含哪些？(栈/静态变量/JNI/同步锁/Maintained Class)|WeakHashMap为什么能自动清理？虚引用有什么实际用途？|流程缓存用WeakReference还是SoftReference？|
|B1-10|**标记-清除算法(Mark-Sweep)**|★★☆|L1: 标记存活对象→清除未标记；L2: 缺点：碎片化问题导致大对象分配困难；L3: CMS Old Gen使用的就是此变种(标记清除)|为什么CMS用标记清除不用标记整理？碎片化如何影响分配速度？|老年代大对象(流程归档数据)碎片影响|
|B1-11|**标记-复制(Copying)**|★★★|L1: 将存活对象复制到另一块区域；L2: 新生代Eden:S0:S1 = 8:1:1 的理由(只浪费10%空间)；L3: 分配担保(Handle Promotion Failure)机制——Survivor空间不够时直接进老年代|为何是8:1:1不是1:1？对象年龄阈值15是怎么来的？|流程临时对象的快速回收|
|B1-12|**标记-整理(Mark-Compact)**|★★☆|L1: 标记后将存活对象向一端移动；L2: 解决碎片化但移动成本高；L3: Old Gen主要采用此策略|移动对象时地址引用怎么更新？STW时间与整理的关系？|长生命周期流程实例的内存整理|
|B1-13|**Serial / Serial Old 收集器**|★★☆|L1: 单线程、STW、Client模式默认；L2: Serial用于New Gen(Copying)，Serial Old用于Old Gen(Mark-Compact)；L3: 适用场景：客户端应用、小内存(<100MB)|什么时候还会用Serial？它有什么优势？|开发环境调试FlowPulse时的默认GC配置|
|B1-14|**Parallel Scavenge / Parallel Old (吞吐量优先)**|★★★|L1: 多线程并行、关注吞吐量(=用户时间/(用户+GC时间))；L2: 自适应调节策略(-XX:+UseAdaptiveSizePolicy)；L3: -XX:GCTimeRatio / -XX:MaxGCPauseMillis 参数权衡关系|吞吐量和停顿时间如何取舍？自适应调节怎么工作的？|批量处理任务(批量导入流程模板)的GC选型|
|B1-15|**ParNew 收集器**|★★★|L1: Serial的多线程版，只能配合CMS；L2: 与Parallel Scavenge的区别(后者有自适应策略)；L3: CMS唯一搭配的新生代收集器——为什么？|为什么CMS只能搭配ParNew不能搭配Parallel Scavenge？|FlowPulse生产环境ParNew+CMS组合|
|B1-16|**CMS(Concurrent Mark Sweep)**|★★★|L1: 目标：降低STW时间；L2: 四步流程：初始标记(STW)→并发标记→重新标记(STW)→并发清除；L3: 三大缺陷：CPU敏感/浮动垃圾/Concurrent Mode Failure|CMS并发标记阶段用户线程变了怎么办？浮动垃圾怎么处理？什么时候发生Concurrent Mode Failure？|低延迟要求的工作流API接口GC配置|
|B1-17|**G1(Garbage First) 收集器** ★★★|L1: 面向服务端，Region划分取代物理分代；L2: Remembered Set(RSet)+Collection Set(CSet)解决跨Region引用；L3: Mixed GC过程：Initial Mark→Concurrent Mark→Remark→Copy/Clean|G1的Region大小怎么定？RSet维护开销？何时触发Mixed GC？推荐G1的原因？|**FlowPulse生产环境首选G1** — 大堆(>4GB)低延迟需求||
|B1-18|**ZGC(Z Garbage Collector)**|★★☆|L1: JDK11引入，目标：<10ms停顿(无论堆多大)；L2: Colored Pointers(染色指针)+Load Barrier读屏障技术；L3: 支持TB级堆，但JDK17前实验性质|ZGC染色指针怎么做到并发的？读屏障对性能的影响？|未来超大流量场景下的GC选择|
|B1-19|**Shenandoah GC**|★☆☆|L1: OpenJDK的类似ZGC的GC；L2: 使用 Brooks Forwarder Pointer(转发指针)而非染色指针；L3: 与ZGC对比：平台支持更广但成熟度略低|Shenandoah和ZGC的核心区别是什么？|备选方案了解|
|B1-20|**GC 日志分析与调优工具**|★★★|L1: -Xlog:gc* / -XX:+PrintGCDetails 参数；L2: GC日志解读(YGC/FGC频率、停顿时间、各代使用率)；L3: 工具链：GCViewer/GCEasy/Eclipse MAT分析Dump|怎么判断是否需要调优？Full GC频繁的常见原因和排查思路？|**FlowPulse上线前的GC基线测试与监控告警设置**|