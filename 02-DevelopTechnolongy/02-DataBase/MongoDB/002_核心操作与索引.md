|序号|知识点|笔记写什么|important|
|---|---|---|---|
|M2.1|CRUD 操作详解|insertOne/insertMany（writeConcern参数）；find/query语法（比较/逻辑/元素/数组查询符）；update操作符（set/set/unset/inc/inc/push）；deleteOne/deleteMany|核|
|M2.2|聚合管道（Aggregation Pipeline）|match→match→group → project→project→sort → limit五大阶段典型用法；limit五大阶段典型用法；lookup 多表关联（类似SQL JOIN）；$unwind 展开数组|🔥🔥🔥 **高频考点**|
|M2.3|索引类型与原理|单字段索引 / 复合索引 / 多键索引(数组字段) / 文本索引(Text) / 地理空间索引(2dsphere)；B-Tree vs B+Tree（MongoDB用B-Tree，为什么？）|核|
|M2.4|索引优化与执行计划|`.explain("executionStats")` 解读；COLLSCAN vs IXSCAN；覆盖索引Covered Query；索引选择性原则；复合索引前缀匹配规则|热|
|M2.5|事务支持（Multi-document Transaction）|4.0版本才支持多文档事务；副本集/分片集群前提条件；ACID保证级别；性能开销对比单文档原子性；什么时候该用/不该用|🔥🔥|