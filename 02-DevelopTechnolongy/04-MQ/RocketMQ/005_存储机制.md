|序号|知识点|笔记写什么|important|
|---|---|---|---|
|5.1|CommitLog 存储设计（核心中的核心）|所有Topic的Message混合写入同一个CommitLog文件（顺序写）；文件大小默认1G；MappedFile/MappedFileQueue 内存映射机制；**为什么这样设计而不是按Topic分文件**|🔥🔥🔥🔥🔥 **灵魂问题**|
|5.2|ConsumeQueue 索引构建|CommitLog 写入后异步构建 ConsumeQueue（索引条目：8字节commitLog偏移量 + 4字节size + 8字节tag hashcode）；固定20字节/条；为什么需要两层索引|🔥🔥🔥🔥|
|5.3|消息写入流程完整链路|Producer → Broker 接收 → CommitLog 写入（MappedFile + PageCache）→ 刷盘策略（同步ASYNC_FLUSH / 同步SYNC_FLUSH）→ 返回结果给Producer；**画完整流程图**|🔥🔥🔥🔥|
|5.4|刷盘策略|SYNC_FLUSH（同步刷盘，单机可靠）/ ASYNC_FLUSH（异步刷盘，高性能）；GroupCommitService 原理；如何选择；与 MySQL redo log 刷盘策略的类比|🔥🔥🔥🔥|
|5.5|主从复制(HA)机制|主Broker写完CommitLog后同步到从Broker（同步双写SYNC_MASTER / 异步复制ASYNC_MASTER）；HAConnection / HAService 工作原理；主从切换对消息的影响|核|
|5.6|消息拉取(Pull)流程|Consumer 向 Broker 请求 → Broker 查ConsumeQueue获取CommitLog offset → 从CommitLog读取完整消息 → 返回Consumer；PullResult 结构解析|🔥🔥🔥|
|5.7|文件清理与过期删除|默认72小时过期 / 文件大小检查 / 定时清理任务（deleteExpiredFiles）；凌晨低峰期执行；与 Kafka 日志保留策略对比|热|


|#|知识点|important度|三层笔记建议|面试追问|FlowPulse 结合|
|---|---|---|---|---|---|
|B5-01|**CommitLog + ConsumeQueue 架构**|★★★|L1: 所有消息写入CommitLog(顺序写，1G一个文件) + ConsumeQueue索引(记录偏移量/大小/taghash)；L2: 为什么分开？CommitLog顺序写极大提升IO性能 + ConsumeQueue支持快速查找；L3: 消息读取路径：先查ConsumeQueue获取CommitLog偏移量→再到CommitLog读取完整消息|CommitLog文件名命名规则？ConsumeQueue每个条目固定20字节？|**FlowPulse消息存储架构完全参考此模式**|
|B5-02|**零拷贝 mmap 与 fileChannel**|★★★|L1: RocketMQ读消息使用mmap(内存映射)+ write(零拷贝)技术；L2: 传统IO(4次上下文切换+2次数据拷贝) vs mmap(2次切换+1次拷贝) vs sendfile(2次切换+1次CPU拷贝)；3: TransientStorePool(堆外内存池)——消息先写入堆外内存再异步刷盘(减少GC压力)|mmap和sendfile的区别？TransientStorePool为什么能减少GC？|消息投递性能优化核心|
|B5-03|**消息刷盘策略**|★★★|L1: SYNC_FLUSH(同步刷盘，每条消息落盘后返回成功——最高可靠性)/ASYNC_FLUSH(异步刷盘，OS Cache积累后刷盘——高性能)；2: GroupCommitService(同步刷盘使用Group Commit合并多个刷盘请求)；3: Broker配置：flushDiskType=SYNC/ASYNC|同步刷盘性能损耗多大？什么业务必须同步刷盘？|**FlowPulse支付相关消息SYNC_FLUSH / 其他ASYNC_FLUSH**|
|B5-04|**消息高可用(同步复制/异步复制)**|★★☆|L1: SYNC_MASTER(同步复制到Slave后再返回成功——主备一致)/ASYNC_MASTER(异步复制——主备可能有短暂不一致)；2: HaService负责主从同步(HA Connection传输CommitLog偏移量)；3: Dledger模式(基于Raft的多副本共识)——Broker故障自动切换|同步复制的RT增加多少？DLeger怎么实现自动切换？|**FlowPulse核心Topic用SYNC_MASTER / DLeger模式做自动故障转移**|
|B5-05|**消息索引(IndexFile)**|★★☆|L1: IndexFile提供按Key或时间范围查询消息的能力(如TraceId追踪)；2: Hash索引结构(Hash Slot→Index Entry链表) + 时间范围约束；3: 查询流程：hash(key)%SlotNum找到slot→遍历Entry链匹配key和时间|IndexFile和ConsumeQueue的区别？消息ID(MsgId)是怎么生成的？|**FlowPulse消息追踪(根据Request ID查询整条调用链消息)**|
|B5-06|**Broker 存储清理机制**|★☆☆|L1: 文件过期删除(默认72h) + 文件超出空间删除；2: CommitLog引用计数(ConsumeQueue还有引用则不能删)；3: 定时任务(clean文件) + 手动执行(admin工具)|过期时间怎么设？引用计数怎么实现的？|日志消息自动清理策略|
|B5-07|**消息重复与幂等设计**|★★★|L1: MQ无法保证 Exactly-Once —— 至少一次(At-Least-Once)可能重复；2: 幂等解决方案：唯一约束(DB unique key)/去重表(Redis Set记录msgid)/状态机判重(同一状态不重复处理)；3: 消息ID全局唯一生成 + 消费端去重表TTL|At-Most-Once和At-Least-Once怎么选？分布式事务消息的重复问题？|**FlowPulse每个消费者都有去重Redis Key: dedupe:{topic}:{msgId} TTL=48h**|