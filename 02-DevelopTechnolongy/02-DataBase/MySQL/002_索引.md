|知识点|笔记时重点写什么|
|---|---|
|**B+ 树结构与原理**|热 **必须能手画**。非叶子节点只存键值（不存数据），叶子节点存全部数据（或主键），叶子之间用双向链表连接。为什么用 B+ 不用 B 树？（查询稳定 O(log n)、范围查询好、磁盘 IO 少因为更矮胖）。3 层 B+ 树能存多少条数据？（16KB 页 × 非叶子节点每行 ~1KB → 约 1170 子节点 → 叶子层 1170×1170 ≈ 137 万条。3 层 ≈ 2000 万条！**这个数字要背下来**）|
|**聚簇索引 vs 非聚簇索引（二级索引）**|热 **最高频概念题之一**。InnoDB 的聚簇索引就是主键索引（叶子节点存完整行数据）；二级索引的叶子节点存的是**主键值**（不是行地址！）。所以二级索引查数据需要"回表"。画一张图：二级索引 B+树找到主键 → 回到聚簇索引找整行数据|
|**回表优化：覆盖索引**|热 如果 SELECT 的字段全部包含在索引中（如 `SELECT id, name FROM user WHERE name='xx'`，且 (name) 是索引），则无需回表。这就是覆盖索引。`EXPLAIN` 中 Extra 列会显示 `Using index`。**工程上最常见的优化手段之一**|
|**联合索引与最左前缀原则**|热 **必背规则**。建立联合索引 `(a, b, c)`，则 `WHERE a=1` ✅、`WHERE a=1 AND b=2` ✅、`WHERE b=2` ❌、`WHERE a=1 AND c=3` ✅（a 用了，c 跳过了 b 但 a 还是用到了）。**给每种组合一个例子**，特别是跳过中间列的情况|
|**索引下推 ICP（Index Condition Pushdown）**|热 MySQL 5.6 引入的优化。在索引遍历过程中，对索引中包含的字段先做 WHERE 条件判断，过滤掉不符合条件的记录，减少回表次数。`EXPLAIN` 的 Extra 显示 `Using index condition`。典型场景：联合索引 `(name, age)`，`WHERE name LIKE '张%' AND age > 18` — 在遍历 name 索引时就过滤掉 age<=18 的|
|**索引失效的 N 种场景**|热 **必背清单**：①对索引列使用函数 `YEAR(create_time)=2024` ❌ ②隐式类型转换（字符串字段传数字）❌ ③`LIKE '%xx'` 左模糊 ❌ ④`OR` 连接的条件中有未索引列 ❌ ⑤`<>` / `NOT IN` / `NOT EXISTS` 可能不走索引 ⑥优化器评估全表扫描更快时主动放弃索引。**每个失效场景配一条正确写法**|
|**前缀索引与索引选择性**|字符串过长时取前 N 个字符建索引（如 `INDEX idx_name(name(10))`）。**选择性 = distinct count / total rows**，越接近 1 越好。前缀索引不能用于覆盖索引（无法做 ORDER BY / GROUP BY）、不能用于 JOIN ON 条件。什么时候该用？长文本字段（URL / 邮箱 / 身份证）|
|**Hash 索引 vs B+Tree 索引**|InnoDB 自适应 Hash Index（自动维护，不可手动干预）：等值查询 O(1)，但不支持范围查询、不支持排序、不支持最左前缀。Memory 引擎才显式支持 Hash 索引。**对比表**是标准考法|
|**唯一索引与普通索引的查询差异**|核 唯一索引查到一条就返回；普通索引可能查到匹配值后继续向右遍历（因为可能有重复值）。**插入时的差异更大**：唯一索引需要判断是否冲突，涉及"change buffer"和磁盘读取的权衡——普通索引可以利用 change buffer 延迟写入，唯一索引不行（必须读盘判断唯一性）|
|**索引设计原则**|选择性高的列优先放联合索引前面、区分度低的列不放索引（如性别）、小表不建索引（全表更快）、单表索引不超过 5~6 个、避免冗余索引（`(a)` 和 `(a,b)` 中 `(a)` 冗余了）。**做成检查清单形式**|

> **笔记技巧**：索引这一块建议你准备**三张图**：
> 
> 1. B+ 树结构图（标注层级高度和数据量关系）
> 2. 聚簇索引 vs 二级索引对比图（回表过程）
> 3. 联合索引最左前缀示意图（哪些情况命中、哪些没命中）
> 
> 这三张图画好了，索引模块 80% 的分数就到手了。




|#|知识点|重要度|三层笔记建议|面试追问|FlowPulse 结合|
|---|---|---|---|---|---|
|B3-14|**B+ 树结构与InnoDB索引实现**|★★★|L1: 非叶子节点存Key+指针(不存数据)，叶子节点存所有数据+双向链表；L2: 聚簇索引(主键，叶子存整行数据) vs 二级索引(叶子存主键值+回表)；L3: B+树高度计算：16KB页/主键8B+指针6B≈1170叉 → 3层可存约13亿条|为什么不用B树(非节点存数据)？B+树层级怎么算？|**FlowPulse主键设计：雪花算法BIGINT聚簇索引**|
|B3-15|**覆盖索引(Covering Index)**|★★★|L1: 二级索引包含了查询所需的所有列→无需回表；L2: Index Condition Pushdown(ICP)：MySQL5.6+将条件下推到存储引擎层过滤；L3: 使用EXPLAIN的Extra字段确认：Using index(覆盖)/Using index condition(ICP)|如何设计覆盖索引？联合索引列顺序原则(最左前缀)？|**流程列表查询优化：联合索引(status,create_time)覆盖常用条件**|
|B3-16|**最左前缀匹配原则**|★★★|L1: (a,b,c)联合索引可用范围：a / a,b / a,b,c (跳过a直接查b无效)；L2: = > < >= <= BETWEEN LIKE前缀匹配 的组合规则；L3: 索引下推优化——对(a,b) WHERE a=1 AND b>2 只用a的范围定位，b的条件ICP|(a,b)索引WHERE b=1 AND a=2能用吗？LIKE '%abc'能用索引吗？|流程搜索条件的索引命中分析|
|B3-17|**索引失效的12种情况**|★★★|L1: 运算/函数/隐式转换/OR(非索引列)/LIKE前缀%/NOT IN/IS NULL(有时)/!=/<>/类型不一致/排序方向不一致/MVS跳过中间列；L2: 每种情况的原理(优化器无法在索引树上定位范围)；L3: 用EXPLAIN验证(type/key/rows/Extra四列)|IS NULL一定不走索引吗？UTF8和Latin1字符集转换会导致失效？|**FlowPulse SQL审核规范：禁止索引列上使用函数**|
|B3-18|**索引选择性(Cardinality)**|★★☆|L1: Cardinality/总行数 = 选择性(越接近1越好，通常>0.1才有价值)；L2: ANALYZE TABLE更新统计信息(统计不准导致优化器选错索引)；L3: 前缀索引：对长字符串列取前N个字符建索引(平衡选择性和索引大小)|Cardinality怎么查看？前缀索引N怎么确定？|流程名称(VARCHAR(200))的前缀索引设计|
|B3-19|**Force Index 与 Hint**|★★☆|L1: FORCE INDEX强制指定索引 / IGNORE INDEX忽略某索引 / USE INDEX建议索引；L2: 场景：优化器选错索引时临时修正；L3: 注意：Hint是临时方案，根因应修复统计信息或调整SQL|优化器为什么会选错索引？Hint会影响后续SQL吗？|临时解决慢SQL的应急手段|
|B3-20|**Order By / Group By 索引优化**|★★☆|L1: Using filesort(文件排序) vs Using index(索引有序)；L2: Order By利用索引的条件：WHERE列+OrderBy列构成最左前缀+同排序方向；L3: Group By同理——索引有序时可直接扫描分组|Filesort一定很慢吗？内存排序vs磁盘排序阈值？|流程列表按创建时间倒序排列优化|
|B3-21|**索引碎片与维护**|★☆☆|L1: 大量UPDATE/DELETE导致页分裂→索引碎片(物理顺序与逻辑顺序不一致)；L2: ANALYZE TABLE(更新统计) / OPTIMIZE TABLE(重建表+碎片整理) / ALTER TABLE ... Engine=InnoDB；L3: 维护窗口期安排——低峰执行|碎片率怎么看？OPTIMIZE TABLE的代价？|定期维护计划|