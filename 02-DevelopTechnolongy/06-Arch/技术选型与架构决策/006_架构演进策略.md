| #         | 知识点                                       | 核心思想                                                                    | 演进路径                                             | 面试追问                                                             | FlowPulse 路线图                                                                                         |
| --------- | ----------------------------------------- | ----------------------------------------------------------------------- | ------------------------------------------------ | ---------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- |
| **8.6.1** | **演进式架构 Evolutionary Architecture**       | Neal Ford提出；架构应像生物一样逐步进化而非一步到位；每个增量步都应保持架构Fitness Functions(适应性函数)达标    | 单体 → 模块化单体 → 微服务(按需拆分) → Service Mesh(按需引入)      | "什么叫Fitness Function？举例说明？"                                      | FlowPulse V1.0 = 模块化单体(Maven Multi-Module) 各模块独立部署 → V2.0 = 细粒度微服务 + K8s编排 → V3.0 = 引入Istio(如果服务数>20) |
| **8.6.2** | **绞杀者模式 Strangler Fig Pattern**           | Martin Fowler提出；新旧系统共存期间，逐步用新系统替换旧系统的功能；像绞杀榕包裹老树一样逐步替代                  | 识别边界 → 代理层路由(new功能走old/旧功能走new) → 逐步迁移 → 最终下线旧系统 | "绞杀者模式和Big Bang重写的区别？"                                           | 不适用FlowPulse(新项目)。但如果将来要把某模块从单体剥离成微服务时可用此模式                                                           |
| **8.6.3** | **架构适应度函数 Architecture Fitness Function** | 可自动化验证的架构质量指标；每次CI/CD都跑一遍确保架构没有退化；示例：循环依赖检测 / 包大小限制 / 测试覆盖率门禁 / 接口兼容性检查 | "你们的CI流水线里有架构门禁吗？检查哪些指标？"                        | FlowPulse CI门禁：ArchUnit(包依赖规则) + SonarQube(质量门禁) + 依赖漏洞扫描(OWASP) |                                                                                                       |