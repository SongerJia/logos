三种注入方式（构造器 / Setter / 字段）对比与选择

热@Autowired 工作原理（byType → byName → @Qualifier）

热@Resource 与 @Autowired 区别（JSR-250 vs Spring）

@Value 注入值（配置文件 / SpEL / 默认值）

热循环依赖及其解决（三级缓存机制）

核构造器循环依赖为什么无法解决？

@Primary / @Conditional 条件化注入

ObjectProvider / Provider延迟注入