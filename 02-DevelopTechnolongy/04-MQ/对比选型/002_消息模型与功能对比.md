|序号|知识点|笔记写什么|重要度|
|---|---|---|---|
|2.1|消息路由灵活性对比|RMQ：Exchange 4种类型 + routingKey + Header 过滤（最灵活）/ RMQ：Topic+Tag+SQL92 过滤（较灵活）/ Kafka：无路由，全靠 Partition 分配（最不灵活，但足够简单高效）|🔥🔥🔥|
|2.2|延迟消息能力对比|RMQ：18级固定延迟（4.x）或任意定时（5.0 TimerWheel）/ RMQ：TTL+DLX 实现（有排序缺陷）/ Kafka：自身不支持，需外部 State Store（如 Kafka Streams 的 Punctuator）|🔥🔥|
|2.3|消息回溯能力对比|Kafka：offset 任意指定，支持"时光倒流"（最强）/ RMQ：支持按时间回溯（误差秒级）/ RMQ：不支持回溯（消息确认即删除）|🔥🔥|
|2.4|顺序消息对比|RMQ：分区有序（MessageQueueSelector）/ Kafka：Partition 内有序（Key hash）/ RMQ：不支持（AMQP 协议无此概念）|🔥🔥🔥|



|#|知识点|重要度|三层笔记建议|面试追问|FlowPulse 结合|
|---|---|---|---|---|---|
|B5-20|**顺序消息实现方案**|★★★|L1: 全局有序(单一Partition/Queue) vs 分区有序(相同Key路由到同一Queue)；2. RocketMQ: MessageQueueSelector按业务Key hash取模选择队列；3. 消费端：MessageListenerOrderly(顺序监听，单线程消费该Queue)|全局有序和分区有序怎么选？顺序消息对吞吐量的影响？|**FlowPulse同一流程实例的状态变更消息保持顺序(shard_key=instance_id)**|
|B5-21|**消息过滤(Tag/SQL92/Tag)**|★★☆|L1: RocketMQ Tag过滤(服务端过滤，Consumer订阅Tag) / SQL92过滤(Broker端SQL表达式)；2. Kafka: 无服务端过滤(Consumer自行过滤，浪费网络带宽)；3. Tag设计与Topic规划的关系(一个业务域一个Topic + Tag区分事件类型)|Tag数量有限制吗？SQL92过滤性能开销？|**FlowPulse消息规范：Topic=flow_event + Tag=created/approved/completed/rejected**|
|B5-22|**消息轨迹(Message Trace)**|★★☆|L1: RocketMQ: Trace消息记录生产/消费的全生命周期(存储在RMQ_SYS_TRACE_TOPIC)；2. OpenTelemetry集成：消息系统Span传播(traceparent header)；3. 查询工具：RocketMQ Console的Trace查询界面|Trace消息会不会很多？怎么控制采样率？|**FlowPulse全链路追踪：消息Span纳入Jaeger**|
|B5-23|**广播消费 vs 集群消费**|★☆☆|L1: 集群消费(Clustering)：一组Consumer均摊Queue(默认模式，消息只被一个Consumer消费)；2. 广播消费(Broadcasting)：每个Consumer消费全部消息(本地缓存更新/配置刷新)；3. 广播消费不持Offset(每次从头开始)也不支持顺序消息|广播消费会重复消费吗？什么时候用广播？|**FlowPulse配置变更通知用广播消费**|
|B5-24|**死信队列(Dead Letter Queue)设计**|★★☆|L1: 消费失败达到最大重试次数→进入DLQ；2. DLQ的消费策略：单独的Consumer/人工处理平台/自动重试延迟加大；3. DLQ监控 + 告警(死信消息积压说明系统性问题)|DLQ消息怎么处理？要不要自动重入队？|**FlowPulse DLQ处理：死信告警 + 运营后台手动重试/废弃**|
|B5-25|**消息队列容量规划**|★☆☆|L1: 峰值QPS预估 × 消息平均大小 × 保留时长 = 存储容量；2. Partition/Queue数量规划(考虑并发度和扩展性)；3. 网络带宽规划(峰值流量 × 消息大小 × 副本数)|怎么预估峰值QPS？存储成本怎么算？|FlowPulse MQ容量规划文档|