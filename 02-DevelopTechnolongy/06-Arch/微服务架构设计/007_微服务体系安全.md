|#|知识点|核心机制|面试追问|FlowPulse 实现|
|---|---|---|---|---|
|**3.7.1**|**统一认证中心**|认证逻辑集中在auth-service；JWT Access Token(15min) + Refresh Token(7天)；双Token机制平衡安全性和体验|"Access Token和Refresh Token为什么要分开？安全性考虑？"|**auth-service** 是唯一的认证颁发者；其他服务只验Token不发Token|
|**3.7.2**|**网关鉴权 + 服务白名单**|Gateway统一校验JWT有效性 + 提取用户身份；后端服务通过Internal API白名单信任内部调用(不二次验JWT)|"服务间调用(Dubbo)还需要带Token吗？内部调用怎么区分？"|Gateway鉴权后，Dubbo内部调用使用`InternalContext`传递用户身份(不走公开JWT)|
|**3.7.3**|**RBAC 权限模型落地**|User → Role(角色) → Permission(权限)；菜单权限(前端控制可见性) + 按钮权限(`@PreAuthorize`接口控制) + 数据权限(数据范围)|"数据权限怎么实现的？MyBatis拦截器还是SQL改写？"|sys_permission表 + `@PreAuthorize('hasAuthority("process:start")')` + MyBatis-Plus DataPermissionInterceptor|
|**3.7.4**|**接口安全加固**|HTTPS/TLS 1.3 全链路加密；请求签名(HMAC防篡改)；参数加密(敏感字段AES)；接口幂等(Idempotency Key)；防重放(Nonce+Timestamp)|"HTTPS握手过程详细描述一下？TLS 1.3相比1.2改进了什么？"|Nginx SSL终止 + 内网HTTP + 敏感接口(支付类)额外HMAC签名|
|**3.7.5**|**安全最佳实践清单**|密码BCrypt加密存储 + 盐值；Token黑名单(Redis)；登录失败限制(5次锁定15min)；SQL注入防护(MyBatis参数化)；XSS防御(前端转义)；CSRF(Token校验)|"BCrypt为什么不需要自己加盐？它的内部机制？"|全部在auth-service中实现；SecurityConfig全局配置|