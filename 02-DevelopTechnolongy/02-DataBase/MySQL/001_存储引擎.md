|知识点|笔记时重点写什么|
|---|---|
|**InnoDB vs MyISAM 对比**|热 做一张完整对比表：事务支持、行级锁、外键、崩溃恢复、MVCC、存储方式（聚簇 vs 非聚簇）、索引实现（B+树 vs B树）。**MySQL 5.5 之后默认 InnoDB，MyISAM 已基本淘汰，但对比题仍然必问**|
|**InnoDB 数据页结构**|核 一页默认 16KB。内部：File Header / Page Header / Infimum + Supremum 虚拟记录 / User Data（实际数据记录） / Free Space / Page Directory（槽） / File Tailer。**理解页结构是理解 B+ 树的基础**|
|**InnoDB 表空间与段区页**|表空间（.ibd 文件）→ 段（数据段/索引段/回滚段）→ 区（64 个连续页 = 1MB）→ 页（16KB）→ 行。这个层级关系面试常用来考查对底层组织的理解程度|
|**Memory / TokuDB / MyRocks 简介**|Memory（堆表，Hash 索引，适合临时表/字典表）、TokuDB（Fractal Tree 压缩率高，适合日志归档场景）、MyRocks（LSM-Tree 结构，写入性能好）。了解即可，知道选型依据|

> **笔记提示**：01 模块不需要太长，核心就是"InnoDB 为什么是默认引擎"——围绕这个问题把它的优势说透就够了。



|#|知识点|重要度|三层笔记建议|面试追问|FlowPulse 结合|
|---|---|---|---|---|---|
|B3-01|**InnoDB 内存架构(Buffer Pool)**|★★★|L1: BP占物理内存60-80%，缓存数据页+索引页；L2: Page管理：Free List(空闲)→Flush List(脏页)→LRU List(热数据，分Young/Old区)；L3: 预读机制(Read Ahead线性/随机预读) + 刷脏策略(Flush Neighbor)|BP大小怎么设？LRU算法和MySQL的变种区别？InnoDB的LRU为什么分新旧区？|**FlowPulse流程实例高频查询的数据页全部在BP中**|
|B3-02|**Change Buffer(变更缓冲)**|★★☆|L1: 对二级索引的INSERT/UPDATE/DELETE操作先记录在CB中(减少随机IO)；L2: 后台线程或访问时合并(Merge)到磁盘；L3: 适用场景：写多读少的表(如果立即读取则CB反而降低性能——要立即Merge)|Change Buffer什么时候有效？什么情况下应该关闭？|流程日志表的二级索引更新走Change Buffer|
|B3-03|**Log Buffer / Redo Log**|★★★|L1: Redo Log保证崩溃恢复(CRASH-SAFE)，WAL(Write-Ahead Logging)先写日志再写盘；L2: Redo Log文件是循环写入的，write position追上checkpoint=redo满了；L3: innodb_flush_log_at_trx_commit参数(0每秒/1每次commit刷盘/2每次提交OS缓冲)|为什么用Redo Log而不是直接刷数据页？三种flush模式选哪个？|**FlowPulse核心业务设为1(强一致性)，日志类设为0(允许丢1秒)**|
|B3-04|**Undo Log 回滚段**|★★☆|L1: 记录数据修改前的版本，用于回滚事务+MVCC版本读取；L2: Undo Log存储在undo表空间(system tablespace或独立undo文件)；L3: 长事务导致Undo Log膨胀→purge线程清理速度跟不上|Undo Log太大怎么处理？purge线程的作用？|工作流长时间运行的事务Undo空间控制|
|B3-05|**InnoDB 磁盘结构**|★★☆|L1: 表空间(.ibd文件)=数据段+索引段+回滚段；段=区(1MB=64个16KB页)；页是最小IO单位；L2: 数据页内部：File Header/Page Header/Infimum/Supremum/User Records/Free Space/Page Directory/File Trailer；L3: 行格式：COMPACT/DYNAMIC/COMPRESSED(行溢出处理差异)|一页能存多少行数据？VARCHAR(65535)存哪里了(行溢出)？|流程定义大字段(TEXT存流程描述)的行溢出现象|
|B3-06|**DoubleWrite 双写缓冲**|★★☆|L1: 解决部分写失败问题(torn page)：先写到DoubleWrite缓冲区再写到真正位置；L2: DoubleWrite = 2MB(128个页)，系统崩溃后从中恢复完整页；L3: 开销：额外一次写——但比crash后数据损坏代价小得多|能关闭DoubleWrite吗(有ZFS等文件系统时)？性能影响多大？|FlowPulse生产环境保持开启|