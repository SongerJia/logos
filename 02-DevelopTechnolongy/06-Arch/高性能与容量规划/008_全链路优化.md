| #         | 层级         | 优化方向               | 具体手段                                                                                               | 收益             | FlowPulse 应用                                            |
| --------- | ---------- | ------------------ | -------------------------------------------------------------------------------------------------- | -------------- | ------------------------------------------------------- |
| **5.8.1** | **前端/网关层** | 减少传输体积 + 加速静态资源    | HTTP/2多路复用 + Gzip/Brotli压缩 + CDN加速(JS/CSS/图片) + 浏览器缓存(Cache-Control) + DNS预解析 + 预连接(Preconnect)    | RT减少30-60%     | Nginx开启Gzip + Vue3打包CDN + API响应gzip压缩                   |
| **5.8.2** | **网络/协议层** | 减少往返次数(RTT) + 连接复用 | Keep-Alive连接池 + HTTP/2 Server Push + gRPC/Protobuf替代JSON(体积小3-5倍) + TCP参数调优(net.core.somaxconn)    | 每次请求省1-3个RTT   | Dubbo长连接复用(优于HTTP短连接) / Feign连接池复用                      |
| **5.8.3** | **应用服务层**  | 减少计算开销 + 并行处理      | 对象池复用(StringBuilder/ByteBuffer) + 异步非阻塞(Netty/WebFlux) + 本地缓存(Caffeine) + 批量操作替代循环单条(INSERT BATCH) | CPU利用率降低20-50% | 流程引擎批量创建Task(一条INSERT多条VALUES) + Caffeine本地缓存权限数据       |
| **5.8.4** | **数据存储层**  | 减少IO等待 + 提升查询效率    | Redis Pipeline(一次网络RT发送多条命令) + MySQL批量提交(Batch Size=500) + 索引覆盖(避免回表) + 只查必要字段(SELECT *)           | IO等待时间减少40-70% | Redis Pipeline批量获取任务列表相关缓存 + MyBatis-Plus Batch批量插入审计日志 |