  
Bean 完整生命周期流程（实例化 → 属性赋值 → 初始化 → 销毁）

热BeanPostProcessor 前后置处理（核心扩展点）

热InstantiationAwareBeanPostProcessor

热InitializingBean / @PostConstruct / init-method 三种初始化方式及顺序

热DisposableBean / @PreDestroy / destroy-method 三种销毁方式

Aware 接口系列（BeanName/BeanFactory/ApplicationEventPublisher 等）

核@Autowired 字段注入的触发时机（在哪个处理器中完成）

单例 Bean 的懒加载 @Lazy

prototype Bean 的生命周期差异（不管理销毁）

工厂方法创建 Bean（@Bean / FactoryBean）