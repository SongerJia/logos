CountDownLatch 原理与应用场景

热CyclicBarrier 与 CountDownLatch 对比

热Semaphore 信号量限流

Exchanger 线程间数据交换

热Phaser 分阶段任务同步

核ThreadLocal 原理与内存泄漏

InheritableThreadLocal 父子传递

TransmittableThreadLocal 线程池传递


|#|知识点|重要度|三层笔记建议|面试追问|FlowPulse 结合|
|---|---|---|---|---|---|
|B2-32|**CountDownLatch 倒计时门闩**|★★★|L1: countDown()递减计数器，await()阻塞直到=0；L2: 不可重置(一次性) vs CyclicBarrier可重复使用；L3: 典型场景：并行计算汇总/服务启动预热完成确认|CountDown到负数会怎样？await超时后Latch还能用吗？|**FlowPulse并行启动多个微服务模块后统一对外提供服务**|
|B2-33|**CyclicBarrier 循环屏障**|★★☆|L1: N个线程到达屏障点后汇合+执行barrierCommand+重置；L2: 支持reset()重复使用 + getNumberWaiting()查询等待数；L3: broken异常：一个线程中断/超时→屏障破损(broken)→其他线程 BrokenBarrierException|CyclicBarrier和CountDownLatch的本质区别？broken后如何恢复？|流程多阶段审批(需所有审批人完成后进入下一阶段)|
|B2-34|**Semaphore 信号量**|★★☆|L1: permits许可数——acquire()获取许可(减1)，release()归还(加1)；L2: 用于限流(令牌桶思路)或资源池限制(数据库连接数)；L3: 公平 vs 非公平(默认非公平)|Semaphore做限流有什么局限？acquire(n)获取多个许可证？|**FlowPulse API网关层用Semaphore做粗粒度限流(Sentinel细粒度的补充)**|
|B2-35|**Exchanger 数据交换器**|★☆☆|L1: 两个线程在汇合点交换各自的数据；L2: exchange()阻塞直到另一个线程也调用exchange()；L3: 底层实现：单个Slot + Spinning(自旋)优化|超时怎么办？超过两个线程调用会怎样？|了解即可，FlowPulse暂无直接应用|
|B2-36|**Phaser 阶段器(JDK7)**|★☆☆|L1: CountDownLatch + CyclicBarrier增强版，支持动态注册参与者 + 多阶段同步；L2: arrive() / arriveAndAwaitAdvance() / awaitAdvance() 操作族；L3: onAdvance()钩子方法决定是否进入下一阶段|Phaser相比CyclicBarrier的优势？动态注册参与者的场景？|复杂多阶段工作流的阶段同步控制|