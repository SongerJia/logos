|序号|知识点|笔记写什么|重要度|
|---|---|---|---|
|2.1|四种交换机类型详解（核心中的核心）|Direct（精确匹配 routingKey）/ Fanout（广播，忽略routingKey）/ Topic（模式匹配通配符 `*` 和 `#`）/ Headers（消息头匹配，性能差少用）；每种类型的适用场景和图解|🔥🔥🔥🔥 **必考必背**|
|2.2|Default Exchange 默认交换机|每个 Queue 自动绑定到 `""(空字符串)Direct Exchange，binding key = queue name；`channel.basicPublish("", queueName, ...)` 的底层原理|热|
|2.3|Dead Letter Exchange (DLX) 死信交换机|消息进入死信队列的四种条件：TTL过期 / 队列满 / 消费者拒绝(nack+requeue=false) / 达到最大长度；DLX配置方式；与RMQ死信队列的对比|🔥🔥🔥|
|2.4|TTL 消息过期|消息级别TTL（设置expiration属性）/ 队列级别TTL（x-message-ttl参数）；两者同时存在取最小值；延迟队列实现方案（TTL + DLX）及其缺陷（消息堆积时排序问题）|🔥🔥🔥|
|2.5|延迟队列方案|官方插件 rabbitmq_delayed_message_exchange（基于交换机的延迟）；TTL + DLX 方案 vs 插件方案 vs 外部定时任务方案的优劣对比|核|
|2.6|备份交换机(AE, Alternate Exchange)|当消息无法路由到任何Queue时转发到AE（而非丢弃）；`alternate-exchange` 参数设置；与 DLX 的区别（DLX是消费失败后，AE是路由失败后）|热|