|#|知识点|核心内容|面试追问|FlowPulse Gateway 实现|
|---|---|---|---|---|
|**3.5.1**|**网关核心职能**|统一入口 / 路由转发(Request Routing) / 协议转换 / 认证鉴权 / 限流熔断 / 负载均衡 / 日志审计 / 缓存|"网关和反向代理(Nginx)的区别？为什么两者都要？"|**gateway-service = Spring Cloud Gateway** + 外层Nginx(L4负载+SSL终止)|
|**3.5.2**|**路由机制详解**|Predicate(断言匹配：Path/Method/Header/Query/Time/Cookie/RemoteAddr) + Filter(过滤器链) → 路由到目标URI；动态路由(从Nacos/Redis加载)|"Spring Cloud Gateway和Zuul 1.x的本质区别？WebFlux模型的优势？"|`/api/v1/workflow/**` → lb://workflow-service ; `/api/v1/auth/**` → lb://auth-service|
|**3.5.3**|**认证鉴权在网关层**|JWT Token校验(白名单放行login/register) → 解析用户信息放入Header → 后端服务信任网关不再重复校验；RBAC权限预判|"网关只做JWT格式验证还是也做权限校验？怎么做？"|白名单路径(/login/** /register/**)放行 → 其他路径校验JWT → 提取userId转发|
|**3.5.4**|**★ 智能限流算法 (创新点四)**|令牌桶(Token Bucket)：允许突发、匀速限流；漏桶(Leaky Bucket)：平滑输出、拒绝突发；混合策略：用户ID维度的令牌桶 + IP维度的漏桶兜底；Sentinel集成|"令牌桶和漏桶的区别？各自适用什么场景？"|`/api/v1/auth/login` → 20QPS防暴力破解；`/api/v1/workflow/**` → 200QPS突发400；**动态阈值调整(历史流量预测)**|
|**3.5.5**|**过滤器链设计**|GlobalFilter(全局) → GatewayFilter(路由级) → Ordered接口控制顺序(值越小越优先)；自定义Filter：AuthFilter / RateLimitFilter / LogFilter / TraceIdInjectFilter|"过滤器之间怎么传参？Exchange对象的生命周期？"|**责任链模式**：AuthFilter(100) → RateLimiterFilter(200) → RouteFilter(300) → LogFilter(999)|
|**3.5.6**|**网关高可用与性能**|多实例部署 + Nginx upstream负载均衡(least_conn)；连接池复用；路由缓存(本地+Caffeine)；WebSocket代理(proxy_read_timeout=3600s)|"网关会成为瓶颈吗？怎么避免？"|K8s Deployment 3副本 + G1GC调优 + liveness/readiness探针|