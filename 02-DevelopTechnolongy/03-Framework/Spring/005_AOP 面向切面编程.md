AOP 核心概念（切面 / 切点 / 通知 / 连接点 / 织入）

热五种通知类型（Before / AfterReturning / AfterThrowing / After / Around）

热JDK 动态代理 vs CGLIB 动态代理原理与区别

热什么情况下用 CGLIB？（类代理 / 无接口）

核AOP 创建代理的时机（postProcessAfterInitialization 中 AbstractAutoProxyCreator）

核Spring AOP 调用链路（JdkDynamicAopProxy / CglibAopProxy invoke 方法）

切点表达式 Pointcut（execution / within / @annotation / this/target）

@Aspect 注解驱动的 AOP 流程（AspectJAutoProxyRegistrar）

AOP 中的 this 拦截问题（同类方法调用不走代理）

AOP 与循环依赖的关系（提前暴露引用用于 AOP 对象）



|#|知识点|重要度|三层笔记建议|面试追问|FlowPulse 结合|
|---|---|---|---|---|---|
|B6-09|**JDK 动态代理 vs CGLIB**|★★★|L1: JDK：基于接口(InvocationHandler + Proxy.newProxyInstance)，只能代理接口方法；CGLIB：基于继承(Enhancer + MethodInterceptor)，生成目标类的子类；2. Spring AOP选择策略：有接口用JDK，无接口用CGLIB(proxyTargetClass=true强制CGLIB)；3. CGLIB无法代理final类/method(因为要生成子类)|CGLIB性能一定比JDK好？为什么Spring Boot 2.0后默认CGLIB？|FlowPulse全局事务切面(@Transactional)的代理方式|
|B6-10|**AOP 核心概念与源码**|★★☆|L1: Pointcut(切入点) + Advice(通知) + Advisor(切面) + Join Point(连接点)；2. 创建代理：AbstractAutoProxyCreator.createProxy()→ProxyFactory.getProxy()→JdkDynamicAopProxy/CglibAopProxy；3. 调用链：ReflectiveMethodInvocation.proceed()递归执行Advice链|Around通知怎么控制目标方法的调用？多个Advice的执行顺序？|**FlowPulse自定义AOP：操作日志记录 + 接口耗时统计 + 权限校验**|
|B6-11|**@Transactional 事务传播**|★★★|L1: 7种传播行为(REQUIRED/SUPPORTS/MANDATORY/REQUIRES_NEW/NOT_SUPPORTED/NEVER/NESTED)；2. 底层实现：TransactionAspect.invokeWithinTransaction()根据传播行为决定是否开启新事务/加入已有事务/挂起/抛异常；3. NESTED(Savepoint)：嵌套子事务，回滚不影响外层事务|REQUIRED和REQUIRES_NEW的本质区别？NESTED什么时候有用？|**FlowPulse工作流事务边界设计：REQUIRED主事务 + REQUIRES_NEW日志独立事务**|
|B6-12|**@Transactional 失效场景**|★★★|L1: 方法非public /同类自调用(不走代理)/异常被catch未抛出/数据库引擎不支持事务(MyISAM)/传播行为设置不当/rollbackFor配置错误(RuntimeException才默认回滚)；2. 根因：Spring AOP基于代理——只有通过代理调用的方法才会触发切面；3. 解决：自调用改为注入自身/AopContext.currentProxy()/TransactionTemplate编程式事务|rollbackFor = Exception.class 还是 Throwable.class？|**FlowPulse@Transactional规范：rollbackFor=Exception.class + public方法 + 避免同类自调用**|
|B6-13|**Spring 事件机制**|★★☆|L1: ApplicationEventPublisher.publishEvent() → ApplicationListener.onApplicationEvent()；2. @EventListener注解驱动(异步@Async支持)；3. 事务绑定事件(@TransactionalEventListener(phase=AFTER_COMMIT))|