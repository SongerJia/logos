|#|知识点|核心机制|面试追问|FlowPulse 应用|
|---|---|---|---|---|
|**2.6.1**|**故障分类**|Crash Failure(进程崩溃) → 可检测；Omission Failure(消息遗漏) → 重试；Byzantine Failure(恶意节点) → PBFT；最常见的是**网络分区(Partition)**|" Byzantine Fault在实际项目中常见吗？"|主要应对Crash + Network Partition两类故障|
|**2.6.2**|**超时 Timeout 是最重要的容错手段**|超时 ≠ 故障，超时 = 不知道对方是否还活着；超时设置是一门艺术(太快误判/太慢影响体验)|"RPC超时设多少合适？Dubbo默认超时是多少？"|Dubbo RPC超时配置 / Gateway路由超时 / RocketMQ消费超时|
|**2.6.3**|**重试 Retry 与幂等性**|重试必须配合幂等(多次执行结果相同)；指数退避(Exponential Backoff)；重试次数上限；重试风暴(雪崩)防范|"什么是重试风暴？怎么避免？(Jitter)"|Scheduler执行器失败重试(配置retryCount) / MQ消费者重试|
|**2.6.4**|**电路熔断器 Circuit Breaker**|三态模型：Closed(正常放行) → Open(熔断，快速失败) → Half-Open(探针放行一个)；恢复探测；Sentinel实现|"Sentinel的三种限流策略区别？熔断和降级的区别？"|**Gateway层Sentinel集成**：保护后端服务不被突发流量击垮|
|**2.6.5**|**Phi Accrual 故障检测**|AWS Cassandra使用；不是简单的"超时就认为死了"；而是用历史心跳延迟计算**死亡概率(Phi值)**；自适应阈值|"Phi Accrual比固定超时好在哪？Phi值怎么计算的？"|理解层面：Scheduler执行器的存活检测可以借鉴此思路|