|#|知识点|重要度|笔记三层建议|跨模块关联|
|---|---|---|---|---|
|4.1|**proxy_cache 工作原理**|🟡 应掌握|① 缓存键设计（默认 `$scheme$proxy_host$request_uri`）、缓存区定义（proxy_cache_path）、缓存生效条件（响应头 Cache-Control/Status Code）② 缓存失效：purge 模块主动清理 / 过期时间到 / 变更 cache_key ③ 面试：Nginx 缓存和 Redis 缓存分别用在什么场景？|→ Redis 缓存体系、→ 浏览器缓存（Cache-Control/Expires）|
|4.2|**缓存命中率优化**|🟢 了解|① 合理设置 cache_key（去掉无用 query param）、过期时间分层（热点数据长/冷数据短）② 通过 `stub_status` 或日志分析命中率 ③ 面试：如何监控和提升 Nginx 缓存命中率？|→ MySQL 缓存命中率类比|
|4.3|**CDN 架构与 Nginx 边缘节点**|🟢 了解|① CDN = 分布式 Nginx 集群 + DNS 智能解析，边缘节点就近返回静态资源 ② 回源策略：CDN 未命中时回源站获取 ③ 面试：你们的静态资源走 CDN 吗？回源策略怎么设计的？|→ DNS 解析、→ 对象存储 OSS/MinIO|
|4.4|**Browser Cache 控制**|🟡 应掌握|① `expires` / `add_header Cache-Control` 指令控制浏览器缓存；静态资源强缓存 + HTML 协商缓存 ② ETag / Last-Modified 配置 ③ 面试：前端资源的缓存策略怎么设计的？更新后用户看到旧版本怎么办？|→ HTTP 协议缓存头、→ 前端构建（hash 文件名）|