|#|知识点|核心手段|技术选型|面试追问|FlowPulse 应用|
|---|---|---|---|---|---|
|**6.5.1**|**敏感数据加密存储**|密码：BCrypt(自带盐值，不可逆) / 手机号/身份证：AES-256-GCM加密(可逆，需秘钥) / 银行卡号：脱敏存储只保留后4位|BCrypt(spring-security-crypto) / AES(JDK Cipher/GCM模式) / 国密SM4(金融合规需求)|"BCrypt为什么不需要自己加盐？它的work factor参数有什么用？"|**sys_user.password**: BCrypt(成本因子cost=12) / **phone/id_card**: AES-256加密列|
|**6.5.2**|**数据脱敏 Data Masking**|展示层数据脱敏：手机号138****1234 / 身份证320********_**1234 / 邮箱a**_@qq.com；日志中也需脱敏(不能明文打印敏感字段)|注解驱动 `@Desensitize(type = DesensitizeType.PHONE)` + 序列化拦截器 / Logback MaskingFilter|"数据库里存的也是脱敏的吗？还是只是展示时脱敏？"|统一脱敏注解 + Jackson序列化器 + Logback自定义Converter(日志自动脱敏)|
|**6.5.3**|**审计日志 Audit Trail**|记录谁(Who)在什么时间(When)做了什么(What)从哪里(Where)操作的结果(Result)；满足合规要求 + 安全追溯 + 问题排查|AOP拦截 `@AuditLog` + 异步写入(sys_oper_log表) + 不可篡改(追加写/只读归档)|"审计日志的数据量很大怎么处理？需要保留多久？"|`sys_oper_log`: 操作模块/操作类型/请求方法/请求参数/操作状态/耗时/操作IP|