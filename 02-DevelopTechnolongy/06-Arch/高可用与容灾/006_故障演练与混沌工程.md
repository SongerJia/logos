|#|知识点|核心内容|工具/方法|面试追问|FlowPulse 应用|
|---|---|---|---|---|---|
|**4.6.1**|**ChaosBlade 故障注入**|阿里开源混沌工程工具；支持：CPU满载/内存泄漏/磁盘满/网络延迟/丢包/进程杀死/Java异常注入/Dubbo超时等|`blade create cpu fullload` / `blade create network delay --time 3000` / `blade create dubbo timeout`|"你们做过故障演练吗？怎么做的？"|开发阶段模拟：MySQL宕机(验证降级)、Redis挂掉(验证缓存穿透)、网络延迟(验证超时级联)|
|**4.6.2**|**常见故障场景矩阵**|硬件故障(磁盘/网卡/CPU/内存) → 网络故障(分区/延迟/丢包/DNS) → 软件故障(OOM/死锁/FullGC/线程池满) → 依赖故障(DB/MQ/Cache/第三方API全部不可用)|逐一列出每个故障的影响范围和应对措施|"如果MySQL主从都挂了，你们的应急方案是什么？"|**FlowPulse 故障预案** ↓|
|**4.6.3**|**Game Day 演练方法论**|计划性演练：提前通知参与人员 → 设定演练目标 → 注入故障 → 观察系统行为 → 记录恢复时间 → 复盘改进|不定期突袭演练(Game Day)更真实；演练要覆盖：检测时间(发现) → 响应时间(定位) → 恢复时间(修复)|"Game Day演练需要注意什么？怎么避免影响线上用户？"|先在Staging环境全自动演练 → 生产环境选择低峰期(凌晨2-4点)小范围注入|
|**4.6.4**|**韧性度量 Resilience Metrics**|故障发现时间(MTTD: Mean Time To Detect) + 故障响应时间(MTTR细分: Acknowledge + Mitigate + Resolve) + 演练覆盖率|目标：MTTD < 1min(自动化告警) + MTTR < 10min(自动恢复为主)|"怎么衡量你的系统够不够'韧'？"|Prometheus AlertManager + Grafana 告警面板 → 目标 P0告警1分钟内触达On-Call|

> **📋 FlowPulse 核心故障场景预案**

```
┌──────────────┬──────────────┬──────────────────┬─────────────────────────┐
│   故障场景     │   影响范围     │   应急措施          │   恢复目标时间         │
├──────────────┼──────────────┼──────────────────┼─────────────────────────┤
│ MySQL主库宕机  │ 全局不可用     │ MGR自动切换到新主库   │ < 30s (自动)           │
│ MySQL全集群挂  │ 全局不可用     │ 降级: 只读Redis缓存  │ < 1min (手动启降级开关)   │
│ Redis全挂      │ 登录/Session受 │ JWT本地校验(关掉Redis │ < 30s (自动降级)        │
│              │ 影响           │  黑名单+权限缓存功能)  │                       │
│ Nacos挂了      │ 服务发现失效    │ 本地缓存服务列表     │ < 2min (客户端本地缓存)   │
│ RocketMQ挂    │ 消息通知延迟    │ 同步发送替代MQ异步    │ < 1min (自动切换通道)     │
│ MinIO不可用    │ 文件上传失败    │ 本地磁盘临时存储     │ < 10s (自动fallback)    │
│ 网络分区       │ 跨服务调用中断   │ Sentinel熔断+降级    │ < 5s (自动)            │
│ 流程引擎OOM    │ 流程推进停止    │ K8s自动重启Pod       │ < 60s (自动)           │
│ Gateway过载    │ 全站不可访问    │ Nginx返回503静态页   │ 即时 (L7 LB兜底)        │
└──────────────┴──────────────┴──────────────────┴────
```